!if $status=waiting
  !if $click_cnt=0
    $name_click1
    !item $proj of $name_list3
.
  !else
    !if $proj=2
      $name_click2 :
    !endif
  !endif

  !form reply
  !distribute item alt="Click",1,form click  into ins_attr,ins_border,ins_tag
  <div class="wimscenter">
  !insdraw\
  $dessin\
  $dessin1
  </div>
  !formend
!endif
