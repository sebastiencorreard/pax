%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/airepolygone.cpp.

\title{Formules pour les aires}

\author{Marie-Claude, David}
\email{Marie-Claude.David@universite-paris-saclay.fr}


\language{fr}
\range{-5..5}
\format{html}

\computeanswer{no}
\precision{100}
\integer{k=random(1..6)}
integer{k=6}
\integer{p=random(1..2)}
\integer{n=random(1..9)}
\integer{m=random(2..10)}
\integer{q=random(1..7)}
\matrix{angle=30,1/2
45,sqrt{2}/2
60,sqrt{3}/2
90,1
120,sqrt{3}/2
135,sqrt{2}/2
150,1/2}
\if{\n=\m}{\integer{m=\n+1}}
\if{\k=1}{
	\integer{a=\n^2}
	\text{enonce= carré \(ABCD\). On sait que }
	\if{\p=1}
		{\text{question= la longueur de son côté vaut \(\n\) \(cm\). Son aire vaut}
		\text{unit=\(cm^2\)}
		\integer{rep=\a}
		}
	\if{\p=2}
		{\text{question= son aire vaut \(\a\) \(cm^2\). Son côté a pour longueur}
		\text{unit=\(cm)}
		\integer{rep=\n}
		}
}
\if{\k=2}{
	\integer{a=\n*\m}
	\integer{mm=\m}
	\integer{nn=\n}
	\if{\n<\m}{\integer{mm=\n}
	\integer{nn=\m}}
	\text{enonce= rectangle \(ABCD). On sait que }
	\if{\p=1}
		{\text{question= sa longueur est égale à \(\nn\) \(cm\) et
		sa largeur à \(\mm\) \(cm\). Son aire vaut}
		\text{unit=\(cm^2\)}
		\integer{rep=\a}
		}
	\if{\p=2}
		{\text{question= son aire vaut \(\a\) \(cm^2\) et sa longueur \(\nn\) \(cm\).
		 Sa largeur vaut }
		\text{unit=\(cm\)}
		\integer{rep=\mm}
	}
}
\if{\k=3}{
	\text{enonce= triangle \(ABC\). On sait que }
	\if{\p=1}
		{\text{question= sa hauteur est égale à \(\n\) \(cm\) et sa base à \(\m\) \(cm\).
		 Son aire vaut}
		\text{unit=\(cm^2\)}
		\real{rep=\n*\m/2}
	}
	\if{\p=2}
		{\text{question= \(AB\) est égal \(\n\) \(cm),  \(AC\) à \(\m\) cm et
		 l'angle \(\widehat{BAC}\) à \(\angle[\q;1]^\circ\). Son aire vaut}
		\text{unit=\(cm^2)}
		\real{s=sin(\angle[\q;1]*pi/180)}
		\real{rep=\n*\m*\s/2}
	}
}
\if{\k=4}{
	\text{enonce= losange \(ABCD\). On sait que }
	\if{\p=1}{
	  \text{question=  ses diagonales sont égales à \(\n) \(cm\) et \(\m\) \(cm\).
		Son aire vaut}
		\text{unit=\(cm^2\)}
		\real{rep=\n*\m/2}
		}
	\if{\p=2}{
	  \text{question= la longueur de son côté est égale à \(\n) \(cm\) et
		 l'un de ses angles  à \(\angle[\q;1]^\circ\). Son aire vaut}
		\text{unit=\(cm^2\)}
		\real{s=sin(\angle[\q;1]*pi/180)}
		\real{rep=\s*\n^2}
	}
}
\if{\k=5}{
	\text{enonce= parallélogramme \(ABCD\). On sait que }
	\if{\p=1}
		{\text{question= sa hauteur est égale à \(\n\) \(cm\) et sa base à \(\m\) \(cm\).
		 Son aire vaut}
		\text{unit=\(cm^2\)}
		\integer{rep=\n*\m}
		}
	\if{\p=2}
		{\text{question= \(AB\) est égal \(\n\) \(cm\),  \(AC\) à \(\m\) \(cm\)
		et l'angle \(\widehat{BAC}\) à \(\angle[\q;1]^\circ\). Son aire vaut}
		\text{unit=\(cm^2\)}
		\real{s=sin(\angle[\q;1]*pi/180)}
		\real{rep=\n*\m*\s}
		}
	}
\if{\k=6}{
	\text{enonce= secteur circulaire d'angle \(\angle[\q;1]^\circ\) dans un cercle de rayon
	 \(\n\) \(cm\).}
	\text{question= Son aire vaut}
	\text{unit=\(cm^2\)}
	\real{rep=pi*\angle[\q;1]*(\n^2)/360}
	}
\statement{Dans le plan affine euclidien, on considère un
\enonce <label for="reply1">\question</label> \embed{reply1,7} \unit.}
\answer{}{\rep}{type=numeric}

