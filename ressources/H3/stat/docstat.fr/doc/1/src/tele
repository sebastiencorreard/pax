\def{text n=randint(40..70)}
\def{text a=slib(stat/binomial \n,5,1/4)}
\def{text a=wims(declosing \a)}
\comment{\for{i=5 to 10}{
\def{text a=wims(replace internal \i by 2 in \a)}
}
}
\def{text tableau=<table class="tabledocstat wimscenter wimsborder">}
\for{ i=0 to ceil(\n/10)}{
\if{ 10*\i+1 <=\n }{
\def{text tableau= \tableau <tr>}
	\for{ j=1 to 10}{
	\if{ 10*\i+\j <=\n }{\def{real u=item(10*\i+\j, \a)}
\def{text tableau= \tableau <td>\u</td>}
	}}
\def{text tableau= \tableau </tr>}
}
}
\def{text liste=[0,1,2,3,4,5,6]}
\def{text liste1=[0,1,2,3,4,5]}
\def{text tableau=\tableau </table>}
\def{text c=slib(stat/effectif [\a],\liste)}
\def{text c=item(1,\c)}

\def{text b=pari(print(\c/\n))}
\def{text c=wims(declosing \c)}
\def{text b=wims(declosing \b)}
\def{real freq1=ceil(item(1,\b)*1000)/10}
\def{real freq2=ceil(item(2,\b)*1000)/10}
\def{real freq3=ceil(item(3,\b)*1000)/10}
\def{real freq4=ceil(item(4,\b)*1000)/10}
\def{real freq5=ceil(item(5,\b)*1000)/10}
\def{real freq6=ceil(item(6,\b)*1000)/10}
\def{text eff1=item(1,\c)}
\def{text eff2=item(2,\c)}
\def{text eff3=item(3,\c)}
\def{text eff4=item(4,\c)}
\def{text eff5=item(5,\c)}
\def{text eff6=item(6,\c)}
\def{text histo=slib(stat/histo [\c],\liste1,ytics 5,xtics,baton)}

\reload{<img src="gifs/doc/etoile.gif" alt="rechargez" width="20" height="20" /> }
Lors d'une enquête, on a posé à \n familles la question
suivante&nbsp;: <strong>"De combien de téléviseurs dispose votre foyer&nbsp;?"</strong> On a
obtenu les résultats suivants&nbsp;:

\tableau

L'analyse statistique de ces données permet de remplir le tableau suivant :
<table class="tabledocstat wimscenter wimsborder">
<tr><th>Télévision</th>
<td>0</td>
<td>1</td>
<td>2</td>
<td>3</td>
<td>4</td>
<td>5</td>
</tr>
<tr><th>Effectif</th>
<td>\eff1</td>
<td>\eff2 </td>
<td>\eff3 </td>
<td>\eff4 </td>
<td>\eff5 </td>
<td>\eff6 </td>
</tr>
<tr><th>Fréquence (%)</th>
<td>\freq1 </td>
<td>\freq2 </td>
<td>\freq3 </td>
<td>\freq4 </td>
<td>\freq5 </td>
<td>\freq6 </td>
</tr>
</table>
et voici l'histogramme des effectifs
\draw {400,200}{\histo}
