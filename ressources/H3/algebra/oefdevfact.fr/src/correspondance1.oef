%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/correspondance.cpp.

\title{Correspondance (a+b)(c+d) 1}
\author{Paul,Byache}
\email{byachepaul@yahoo.fr}
\computeanswer{no}
\format{html}
\language{fr}
\text{name_answer=Réponse}
\text{name_enonce=Faire correspondre chaque expression factorisée avec une expression développée égale.}
\text{size=40x220x220}
\integer{confparm1=\confparm1=?2}
\text{value=wims(values v for v=1 to 10)}
\text{value=shuffle(\value)}
\integer{b=\value[1]}
\integer{d=\value[2]}
\text{x=random(x,y)}
\matrix{lt=0,0,0,0
0,1,0,1
0,1,0,0
0,0,0,1}
\text{col1=}
\text{col2=}
\for{i=1 to 4}{
  \text{s=row(\i,\lt)}
  \text{fact=(\x^\confparm1+(-1)^(\s[2])*\b)*(\x+(-1)^(\s[4])*\d)}
  \text{fact=simplify(\fact)}
  \text{dev=maxima(expand(\fact))}
  \text{dev=texmath(\dev)}
  \text{dev=\(\dev)}
  \text{fact=texmath(\fact)}
  \text{fact=\(\fact)}
  \text{col1=wims(append item \fact to \col1)}
  \text{col2=wims(append item \dev to \col2)}
}
\statement{
  \name_enonce
  <div class="wimscenter">
  \embed{r1,\size}
  </div>
}
\answer{}{\col1;\col2}{type=correspond}
