%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/Equation1.cpp.

\language{fr}
\author{Fabrice,Guerimand}
fwguerima@free.fr
\computeanswer{no}
\format{html}
maintaineur: WIMS EDU



\title{Équation ax+b&#61;0#}

\text{name_question=Résoudre l'équation}
\text{name_instruction=Donner votre résultat sous une forme aussi simplifiée que possible.}
\text{name_guide=On vous propose de résoudre l'exercice en vous guidant.
  Complétez les différentes étapes de calcul qui vous sont proposées}
\text{name_question2=En ajoutant, à chacun des membres de l'égalité puis en réduisant on obtient }
\text{name_answer=Réponse}






\text{name_question2=En ajoutant, à chacun des membres de l'égalité puis en réduisant on obtient }
\text{name_question3=En divisant chacun des membres de l'égalité par , on obtient}
\text{name_question4=On termine en simplifiant le résultat }

\precision{10000}
\integer{sizef=4}
\integer{confparm1=\confparm1=?1}
\integer{confparm2=\confparm2=?1}
\text{champ=wims(record \confparm1 of src/generateur)}
\text{A=row(2,\champ)}
\text{B=row(3,\champ)}
\text{c=item(3,\A)}
\if{\B=}{
\text{a=item(1,\A)}
\text{b=item(2,\A)}
\if{\c<0}{
\text{valeurs=wims(values \c*v,-(\c)*v for v=\a to \b)}
}{
\text{valeurs=wims(values \c*v for v=\a to \b)}
}
}{
\text{valeurs=\B}
\if{\c<0}
{\text{tmp=wims(replace internal , by ,- in \valeurs)}
\text{valeurs=wims(listunion \valeurs and \tmp)}
}
}
\text{valeurs=wims(listuniq \valeurs)}
\text{valeurs=shuffle(\valeurs)}
\text{a=item(1,\valeurs)}
\text{b=item(2,\valeurs)}
\text{c=simplify(-(\b))}

\if{\confparm1>4}{
  \integer{t1=(\c)*100}
  \integer{t2=(\a)*100}
  \text{d=simplify(\t1/\t2)}
}{
  \text{d=simplify((\c)/(\a))}
}

\text{x=random(x,y,z,a,b)}
\text{enonce=texmath(\a*\x+(\b)=0)}
\if{\confparm2=1}{
\matrix{type=r1
r2
r3}
}{
\matrix{type=r3}
}
\steps{\type}
\statement{\name_question \(\enonce\).
<div class="wims_instruction">\name_instruction</div>
\if{\confparm2=1}{\name_guide:
  <ul><li>
    \name_question2[1] \(\c\) \name_question2[2]:
\if{\step=1}{\(\a \x=\) \embed{r1,\sizef} </li></ul>}
{\(\a \x=\c\).</li>
<li>\name_question3[1] \(\a\) \name_question3[2]:
\if{\step=2}{
  <div class="wimscenter">
    \special{mathmlinput [\x= \frac{\c}{reply2}],5,noanswer
    reply 2}
  </div>
</li></ul>}
{\(\x=\frac{\c}{\a}\).</li>
<li>\name_question4 :
<label for="reply3">\(\x =\)</label> \embed{reply 3,\sizef}</li></ul>
}
}
}
}
\answer{\name_answer 1}{\c}{type=numexp}
\answer{\name_answer 2}{\a}{type=numexp}
\answer{\(\x\)}{\d}{type=numexp}

