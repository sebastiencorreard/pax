%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/equation.cpp.

\language{fr}
\author{Fabrice,Guerimand}
fwguerima@free.fr
\computeanswer{no}
\format{html}
maintaineur: WIMS EDU

\precision{10000}


\title{Équation x+a&#61;b}

\text{name_question=Résoudre l'équation}



\text{name_answer=Réponse}
\text{name_instruction=Donner votre résultat sous une forme aussi simplifiée que possible.}

\integer{sizef=4}
\integer{confparm1=\confparm1=?1}
\integer{confparm2=\confparm2=?1}

\text{champ=wims(record \confparm1 of src/generateur)}
\text{A=row(2,\champ)}
\text{B=row(3,\champ)}
\text{c=item(3,\A)}
\if{\B=}{
\text{a=item(1,\A)}
\text{b=item(2,\A)}
\if{\c<0}{
  \text{valeurs=wims(values \c*v,-(\c)*v for v=\a to \b)}
}{
  \text{valeurs=wims(values \c*v for v=\a to \b)}
}
}{
  \text{valeurs=\B}
  \if{\c<0}{
    \text{tmp=wims(replace internal , by ,- in \valeurs)}
    \text{valeurs=wims(listunion \valeurs and \tmp)}
  }
}
\text{valeurs=wims(listuniq \valeurs)}
\text{valeurs=shuffle(\valeurs)}

\text{a=item(1,\valeurs)}
\text{b=item(2,\valeurs)}
\if{\confparm1>=5}{
 \integer{ai=100*\a}
 \integer{bi=100*\b}
}{
 \text{ai=\a}
 \text{bi=\b}
}
\text{x=randitem(x,y,z,a,b)}

\if{\confparm1>=5}{
 \integer{tmp=(\bi)-(\ai)}
 \text{d=slib(decimal \tmp,2)}
}{
  \text{d=simplify((\b)-(\a))}
}
\text{enonce=\x+(\a)=\b}
\integer{test=1}

\text{enonce=texmath(\enonce)}
\statement{
\if{\test=1}{\name_question[1] \(\enonce).
<div class="wimscenter">
<label for="reply1">\(\x =\)</label> \embed{reply 1,\sizef}.
</div>
<div class="wims_instruction">\name_instruction</div>}
{\name_question[1]:
<label for="reply1">\(\enonce1\)</label> \embed{reply 1,\sizef} \(\enonce2).}}
\answer{\name_answer}{\d}{type=numexp}

