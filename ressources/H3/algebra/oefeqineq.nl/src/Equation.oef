\title{ax+b&#61;0#}
\language{nl}
\range{-5..5}
\author{Guerimand Fabrice}
\email{fwguerima@free.fr}
\translator{Reinie Erne}
\computeanswer{no}
\format{html}
\precision{10000}

\integer{sizef=4}
\integer{confparm1=\confparm1=?1}
\integer{confparm2=\confparm2=?1}


\text{champ=wims(record \confparm1 of src/generateur)}
\text{A=row(2,\champ)}
\text{B=row(3,\champ)}
\text{c=item(3,\A)}
\if{\B=}{
\text{a=item(1,\A)}
\text{b=item(2,\A)}
\if{\c<0}{
\text{valeurs=wims(values \c*v,-(\c)*v for v=\a to \b)}
}{
\text{valeurs=wims(values \c*v for v=\a to \b)}
}
}{
\text{valeurs=\B}
\if{\c<0}
{\text{tmp=wims(replace internal , by ,- in \valeurs)}
\text{valeurs=wims(listunion \valeurs and \tmp)}
}
}
\text{valeurs=wims(listuniq \valeurs)}
\text{valeurs=shuffle(\valeurs)}
\text{a=item(1,\valeurs)}
\text{b=item(2,\valeurs)}
\text{c=simplify(-(\b))}
\text{d=simplify((\c)/(\a))}

\text{x=random(x,y,z,a,b)}

\text{enonce=texmath(\a*\x+(\b)=0)}

\if{\confparm2=1}{
\matrix{type=r1
r2
r3}
}{
\matrix{type=r3}
}
\steps{\type}

\statement{Los de vergelijking \(\enonce) op. <p>Geef het resultaat in de vorm van een vereenvoudigde breuk. <p>
\if{\confparm2=1}{We stellen voor om deze oefening met begeleiding te maken. Vul de voorgestelde stappen in: <p>
<ul>
<li> Door bij ieder lid van de gelijkheid \(\c) op te tellen en te vereenvoudigen krijgt men:  <p> 
\if{\step=1}{\(\a \x=) \embed{r1,\sizef} </li></ul>}
{\(\a \x=\c) </li>
<li>  Door ieder lid van de gelijkheid door \(\a) te delen en te vereenvoudigen  krijgt men:<p>
\if{\step=2}{
<center>
  <table border=0>
    <tr> 
      <td align=center rowspan=3>\(\x)\big1 = \big2</td>
      <td align=center> \(\c)</td>
    </tr>
    <tr> 
      <td align=center width=\size> 
        <hr noshade size=2>
      </td>
    </tr>
    <tr> 
      <td align=center> \embed{reply 2,\sizef}</td>
    </tr>
  </table>
</center>
</li></ul>}
{\(\x=\frac{\c}{\a}) </li>
<li> We maken het af door het resultaat als vereenvoudigde breuk op te schrijven: <p>
\(\x =) \embed{reply 3,\sizef}</li></ul>
}
}
}
}
\answer{Antwoord 1}{-\b}{type=algexp}
\answer{Antwoord 2}{\a}{type=algexp}
\answer{Antwoord }{\d}{type=algexp}
