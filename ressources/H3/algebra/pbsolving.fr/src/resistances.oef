%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/resistances.cpp.

\language{fr}
\author{Jean-Baptiste, Frondas}
\email{jbfrondas@gmail.com}
\format{html}
\precision{10000}
\computeanswer{no}
\integer{R1=random(1..99)}
\integer{R2=random(1..99)}
\rational{R=(\R1+\R2)/(\R1*\R2)}
\title{2 - Résistances en dérivation}
\text{titretex=TIT}
\text{lang=slib(oef/env lang)}
\text{enonce=Dans un circuit électrique, on a deux résistances \( R_{1}= \R1 \) \(\Omega\) et \( R_{2}=\R2 \) \(\Omega\) montées en dérivation. On veut remplacer ces deux résistances par une résistance équivalente \( R \). Quelle est la valeur de \( R \) ?}
\text{monaide=On rappelle que dans ce cas, la valeur de la résistance équivalente est donnée par la formule : \( \dfrac{1}{R}=\dfrac{1}{R_1}+\dfrac{1}{R_2}\)}
\statement{\enonce}
\hint{\monaide}
\answer { R \( (\Omega) \)}{ \R }
\solution{}
\latex{
\begin{statement}
\enonce \monaide
\end{statement}
\begin{solution}
\(R= \R \Omega\)
\end{solution}
}
