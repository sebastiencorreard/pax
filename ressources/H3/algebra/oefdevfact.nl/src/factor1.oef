%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/factoriser.cpp.

\language{nl}
\range{-5..5}
\author{Guerimand Fabrice}
\email{fwguerima@free.fr}
\computeanswer{no}
\format{html}
\precision{10000}
\integer{a=random(2..15)}
\integer{b=random(2..15)}
\integer{c=random(2..15)}
\integer{g=maxima(gcd(\b,\c);)}
\integer{b=\b/\g}
\integer{c=\c/\g}
\if{\b=\c}{
\integer{b=2}
\integer{c=3}}
\integer{d=random(2..15)}
\integer{e=random(2..15)}
\integer{f=random(2..15)}
\text{x=randitem(x,y,a,b)}
\integer{confparm=\confparm=?1}

\title{Ontbind in factoren ka+kb 1}
\integer{B=random(1,-1)*\a*\c}
\integer{A=random(1,-1)*\a*\b}
\text{pui=\confparm1=1?1,\x:1,\x,\x^2,\x^3,\x^4}
\text{pui=shuffle(\pui)}
\text{v1=item(1,\pui)}
\text{v2=item(2,\pui)}
\matrix{donnees=\A*\v1+\B*\v2}
\text{enonce=randrow(\donnees)}
\text{rep=maxima(factor(\enonce);)}
\text{enonce=texmath(\enonce)}
\text{tmp=wims(char 1 of \rep)}
\if{\tmp=-}{
   \text{rep=wims(char 2 to -1 of \rep)}
   \text{tmp=wims(char 1 to -2 of *())}
   \text{tmp2=wims(char 1 to -2 of ,())}
   \text{rep2=wims(replace internal \tmp by \tmp2 in \rep)}
   \integer{nb=wims(itemcnt \rep2)}
   \text{rep=}
   \for{i=1 to \nb}{
      \text{tmp=item(\i,\rep2)}
      \text{tmp2=wims(listcomplement \tmp in \rep2)}
      \text{tmp2=wims(replace , by * in \tmp2)}
      \text{tmp2=-\tmp*\tmp2}
      \text{rep=wims(append item \tmp2 to \rep)}
   }
}
\statement{<br>
Ontbind in factoren:&nbsp;&nbsp;<font size="+1">
\(\enonce)
</font>
<br>
Elke factor moet zover mogelijk ontbonden zijn. <br>
Bijv. (-2a-4) wordt niet geaccepteerd.<br>
-2(a+2) wel.
<br>
<center><hr width=50%></center>
<table class=reply>
<tr><td>
<u><b>Jouw antwoord :</b></u><br>
\(\enonce = )\embed{r1,20}.
</td></tr></table>
}
\answer{Antwoord}{\rep}{type=algexp}
