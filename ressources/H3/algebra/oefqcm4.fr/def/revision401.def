!set title=Géométrie 1 4ème
!if $wims_read_parm!=$empty
 !goto $wims_read_parm
!endif
oef2wims_version=3.22
author=Jean-Luc, Donadoni
email=jluc.donadoni@laposte.net
keywords=geometry

ansorder=r,r,r,r,r
varcnt=77
prevarcnt=76
postvarcnt=0
replycnt=5
choicecnt=0
conditioncnt=0
feedbackcnt=0
format=html

val1=$imagedir
val2=$confparm1
val3=$confparm2
val4=$confparm3
val5=$confparm4

!if $testcondition=yes
 !goto test
!endif
!if $status=waiting
 !exit
!endif

val6=Cliquez sur les bonnes réponses.	<div class="wims_instruction">Une seule bonne réponse par question.</div>
tmp0=!record 1 of revision401

tmp=!trim $(tmp0)
val7=!translate internal $	\
$ to ;; in $tmp
tmp0=!record 2 of revision401

tmp=!trim $(tmp0)
val8=!translate internal $	\
$ to ;; in $tmp
tmp0=!record 3 of revision401

tmp=!trim $(tmp0)
val9=!translate internal $	\
$ to ;; in $tmp
tmp0=!record 4 of revision401

tmp=!trim $(tmp0)
val10=!translate internal $	\
$ to ;; in $tmp
tmp0=!record 5 of revision401

tmp=!trim $(tmp0)
val11=!translate internal $	\
$ to ;; in $tmp
val12=$(val7[1;])
val13=$(val8[1;])
val14=$(val9[1;])
val15=$(val10[1;])
val16=$(val11[1;])
tmp=!trim 
val17=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val18=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val19=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val20=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val21=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val22=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val23=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val24=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val25=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val26=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val27=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val28=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val29=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val30=!translate internal $	\
$ to ;; in $tmp
tmp=!trim 
val31=!translate internal $	\
$ to ;; in $tmp
!for val32 =2 to 5 
tmp0=!append item $(val7[$val32;]) to $val17

tmp=!trim $(tmp0)
val17=!translate internal $	\
$ to ;; in $tmp
!next
tmp0=!shuffle $val17

tmp=!trim $(tmp0)
val17=!translate internal $	\
$ to ;; in $tmp
!for val32 =2 to 5 
tmp0=!append item $(val8[$val32;]) to $val18

tmp=!trim $(tmp0)
val18=!translate internal $	\
$ to ;; in $tmp
!next
tmp0=!shuffle $val18

tmp=!trim $(tmp0)
val18=!translate internal $	\
$ to ;; in $tmp
!for val32 =2 to 5 
tmp0=!append item $(val9[$val32;]) to $val19

tmp=!trim $(tmp0)
val19=!translate internal $	\
$ to ;; in $tmp
!next
tmp0=!shuffle $val19

tmp=!trim $(tmp0)
val19=!translate internal $	\
$ to ;; in $tmp
!for val32 =2 to 5 
tmp0=!append item $(val10[$val32;]) to $val20

tmp=!trim $(tmp0)
val20=!translate internal $	\
$ to ;; in $tmp
!next
tmp0=!shuffle $val20

tmp=!trim $(tmp0)
val20=!translate internal $	\
$ to ;; in $tmp
!for val32 =2 to 5 
tmp0=!append item $(val11[$val32;]) to $val21

tmp=!trim $(tmp0)
val21=!translate internal $	\
$ to ;; in $tmp
!next
tmp0=!shuffle $val21

tmp=!trim $(tmp0)
val21=!translate internal $	\
$ to ;; in $tmp
!for val32 =1 to 4 
tmp0=!char 1 of $(val17[$val32])

val33=$[rint($(tmp0))]
tmp0=!char 3 to -1 of $(val17[$val32])

val34=$(tmp0)
!if $val33=1 
tmp0=!append item $val32 to $val27

tmp=!trim $(tmp0)
val27=!translate internal $	\
$ to ;; in $tmp
!endif
tmp0=!append item $val34 to $val22

tmp=!trim $(tmp0)
val22=!translate internal $	\
$ to ;; in $tmp
!next
!for val32 =1 to 4 
tmp0=!char 1 of $(val18[$val32])

val33=$[rint($(tmp0))]
tmp0=!char 3 to -1 of $(val18[$val32])

val34=$(tmp0)
!if $val33=1 
tmp0=!append item $val32 to $val28

tmp=!trim $(tmp0)
val28=!translate internal $	\
$ to ;; in $tmp
!endif
tmp0=!append item $val34 to $val23

tmp=!trim $(tmp0)
val23=!translate internal $	\
$ to ;; in $tmp
!next
!for val32 =1 to 4 
tmp0=!char 1 of $(val19[$val32])

val33=$[rint($(tmp0))]
tmp0=!char 3 to -1 of $(val19[$val32])

val34=$(tmp0)
!if $val33=1 
tmp0=!append item $val32 to $val29

tmp=!trim $(tmp0)
val29=!translate internal $	\
$ to ;; in $tmp
!endif
tmp0=!append item $val34 to $val24

tmp=!trim $(tmp0)
val24=!translate internal $	\
$ to ;; in $tmp
!next
!for val32 =1 to 4 
tmp0=!char 1 of $(val20[$val32])

val33=$[rint($(tmp0))]
tmp0=!char 3 to -1 of $(val20[$val32])

val34=$(tmp0)
!if $val33=1 
tmp0=!append item $val32 to $val30

tmp=!trim $(tmp0)
val30=!translate internal $	\
$ to ;; in $tmp
!endif
tmp0=!append item $val34 to $val25

tmp=!trim $(tmp0)
val25=!translate internal $	\
$ to ;; in $tmp
!next
!for val32 =1 to 4 
tmp0=!char 1 of $(val21[$val32])

val33=$[rint($(tmp0))]
tmp0=!char 3 to -1 of $(val21[$val32])

val34=$(tmp0)
!if $val33=1 
tmp0=!append item $val32 to $val31

tmp=!trim $(tmp0)
val31=!translate internal $	\
$ to ;; in $tmp
!endif
tmp0=!append item $val34 to $val26

tmp=!trim $(tmp0)
val26=!translate internal $	\
$ to ;; in $tmp
!next
tmp0=!nonempty items $val27

tmp=!trim $(tmp0)
val27=!translate internal $	\
$ to ;; in $tmp
tmp0=!nonempty items $val22

tmp=!trim $(tmp0)
val22=!translate internal $	\
$ to ;; in $tmp
tmp0=!nonempty items $val28

tmp=!trim $(tmp0)
val28=!translate internal $	\
$ to ;; in $tmp
tmp0=!nonempty items $val23

tmp=!trim $(tmp0)
val23=!translate internal $	\
$ to ;; in $tmp
tmp0=!nonempty items $val29

tmp=!trim $(tmp0)
val29=!translate internal $	\
$ to ;; in $tmp
tmp0=!nonempty items $val24

tmp=!trim $(tmp0)
val24=!translate internal $	\
$ to ;; in $tmp
tmp0=!nonempty items $val30

tmp=!trim $(tmp0)
val30=!translate internal $	\
$ to ;; in $tmp
tmp0=!nonempty items $val25

tmp=!trim $(tmp0)
val25=!translate internal $	\
$ to ;; in $tmp
tmp0=!nonempty items $val31

tmp=!trim $(tmp0)
val31=!translate internal $	\
$ to ;; in $tmp
tmp0=!nonempty items $val26

tmp=!trim $(tmp0)
val26=!translate internal $	\
$ to ;; in $tmp
replytype1=mark
replyoption1=split noanalyzeprint 
replyweight1=1 
replyname1=
replygood1=$val27;$val22
replytype2=mark
replyoption2=split noanalyzeprint 
replyweight2=1 
replyname2=
replygood2=$val28;$val23
replytype3=mark
replyoption3=split noanalyzeprint 
replyweight3=1 
replyname3=
replygood3=$val29;$val24
replytype4=mark
replyoption4=split noanalyzeprint 
replyweight4=1 
replyname4=
replygood4=$val30;$val25
replytype5=mark
replyoption5=split noanalyzeprint 
replyweight5=1 
replyname5=
replygood5=$val31;$val26
question=__EXECUTED_1692754_EXERCISE__
solution=!nosubst Dans le tableau,les bonnes réponses sont coloriées en vert, les mauvaises en rouge et les réponses oubliées en bleu


!goto stat

!exit

:question
$()$val6	<table class="wimsborder">	<tr><th>QUESTIONS</th><th>A</th>	<th>B</th><th>C</th>	<th>D</th></tr>	<tr><th>$val12</th>	 
!for m_r=1 to 4 
$()<td> 
!read oef/embed.phtml r1,\r 
$()</td> 
!next
$()</tr>	<tr><th>$val13</th>	 
!for m_r=1 to 4 
$()<td> 
!read oef/embed.phtml r2,\r 
$()</td> 
!next
$()</tr>	<tr><th>$val14</th>	 
!for m_r=1 to 4 
$()<td> 
!read oef/embed.phtml r3,\r 
$()</td> 
!next
$()</tr>	<tr><th>$val15</th>	 
!for m_r=1 to 4 
$()<td> 
!read oef/embed.phtml r4,\r 
$()</td> 
!next
$()</tr>	<tr><th>$val16</th>	 
!for m_r=1 to 4 
$()<td> 
!read oef/embed.phtml r5,\r 
$()</td> 
!next
$()</tr>	</table>
$()
!exit

:hint
$()
$()
!exit

:help
$()
$()
!exit

:solution
$()Dans le tableau,les bonnes réponses sont coloriées en vert, les mauvaises en rouge et les réponses oubliées en bleu
$()
!exit

:latex
$()
$()
!exit

:feedback

!exit

:test

!exit

:stat
vsavelist=6,12,13,14,15,16
embedcnt=5
