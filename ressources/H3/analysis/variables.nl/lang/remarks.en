: Rewrite as simple as possible:<br /> addition & substraction with one variable.
: Rewrite as simple as possible:<br /> multiplication  & division with one variable.
: Rewrite as simple as possible:<br /> addition & substraction with two variables.
: Rewrite as simple as possible:<br /> multiplication  & division with one or two variables.
: Simplify and write <b>without</b> parenthesis.
: Fill in and write with <b>without</b> parenthesis <br /><small>remove the brackets</small>
: Fill in and write with brackets <br /><small>(factorization or factoring)</small>
: Rewrite the next power.<br /><small>And simplify as far as possible.</small> 
: Click on the graph that belongs to the next equation:
: Solve the next equation in $m_RR:<br />(e.g. calculate <em>x</em>=.....)<br />
: Determine the coordinates of all intersection points<br /> between the graph of <em>f</em> and the <em>x</em>-axis 
: The values are <em>x=$x1</em> and <em>x=$x2</em><br />so the intersection points with the x-axis are ($x1:$y1) and ($x2:$y2)
: the values are <em>x=$x1</em> ,<em>x=$x2</em> and <em>x=$x3</em><br />so the intersection points with the x-axis are:<br /> ($x1:$y1) and ($x2:$y2) and ($x3:$y3)
: No, you should rewrite <b>without</b> parenthesis.
: No, you should rewrite using <b>parenthesis</b><br />I didn't see any in your answer...
: In my view <kbd>$s*$s</kbd> can be rewritten into something simpler...  
: Your answer ("$testvar" in exercise $p ) doesn't make any sense<br />A typo ?
: Determine the <em>coordinates of the intersection points </em> of<ul><li>parabola <em>$var2</em><span style="color:$color1"> ($color1 curve)</span></li><li>and the line <em>$var3</em><span style="color:$color2"> ($color2 line)</span></li></ul>
: Determine the <em>coordinates of the intersection points </em> of<ul><li>parabola <em>$var2</em></li><li>and the line <em>$var3</em></li></ul>
: The points are ($x1:$y1) and ($x2:$y2)
: Your answer <em>$testvar</em> can be simplified further...
: Factorize.<br /><small>write with brackets.</small>
: Rewrite as <b>one</b> fraction.
: Simplify and rewrite without brackets.
: Simplify and rewrite.
: Simplify and reduce.
: Attention:<br />In sub_question $p you've forgotten the variable $testvar ... 
: Factorize.<br /><small>write with brackets</small>
: Rewrite as <b>one</b> fraction.
: Ok, this parenthesis can't be rejected<br />But they are more or less superfluous...
: No, we want to see just <b>one</b> fraction in your answer.
: You can simplify this answer even more !
:<span style="color:blue">How</span> should I answer this ?
: Very clever indeed !<br /> But <kbd>$ant</kbd> is not factorizing !! <br /> <kbd>x^2 + 3*x + 2 == (x + 1)(x + 2)</kbd>
:
:
:
:
:
:
: Powers and Variables (V)
: Powers and Variables (IV)
: Powers and Variables (III)
: Powers and Variables (II)
: Variables and brackets
: Powers and Variables (I)
: Intersection points of parabola and line
: Solving second & third degree equations
: Which graph? (multiplechoice)
: Write without brackets (V)
: Factorizing (II) [H4]
: Write without brackets (III)
: Factorizing (III) (H2/H3)
: Write without brackets (IV)
: Write without brackets (II)
: Factorizing (I) (H2/H3)
: Write without brackets (I)
: Simplify (with +,-,&times;,/)
