%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/CORequ.cpp.

\title{Corrispondenza funzione-rappresentazione 4}
\integer{type=1}
\integer{number=4}
\language{it}
\range{-5..5}
\author{Fabrice,Guerimand}
\email{fwguerima@free.fr}
\computeanswer{no}
\format{html}
\precision{10000}
\if{\type=1}{
\text{question=Metti in corrispondenza ogni funzione con il colore della sua rappresentazione grafica nel disegno a fianco.}
\text{ord=f(x)}
\text{size=40x150x75}
}{
\text{question=Metti in relazione ogni equazione con il colore della retta corrispondente nel disegno a fianco.}
\text{ord=y}
\text{size=40x100x75}
}
\text{a=1,2,3,4}
\integer{b=item(1,\a)}
\integer{a=item(2,\a)}
\text{choix=\a;#;\b,\a;#;-\b,-\a;#;\b,-\a;#;-\b,\b;#;\a,-\b;#;\a,\b;#;-\a,-\b;#;-\a}
\text{choix=shuffle(\choix)}
\text{co=red;#;rosso,green;#;verde,yellow;#;giallo,purple;#;violetto,blue;#;blu,black;#;nero}
\text{co=shuffle(\co)}
\text{co=wims(item 1 to \number of \co)}
\matrix{tmp=wims(items2lines \co)}
\matrix{tmp=wims(replace internal ;#; by , in \tmp)}
\text{co=column(1,\tmp)}
\text{nom=column(2,\tmp)}
\text{dessin=wims(record 2 of src/graph.don)}
\text{coord=}
\for{i=1 to \number}{
\text{don=item(\i,\choix)}
\text{tmp=\don}
\text{don=wims(replace internal ;#; by , in \don)}
\integer{A=item(1,\don)}
\integer{B=item(2,\don)}
\integer{xpt1=150+(-5*30)}
\integer{xpt2=150+(5*30)}
\function{dg=-(\A*(x)+(\B))*30+150}
\integer{ypt1=evalue(\dg,x=-5)}
\integer{ypt2=evalue(\dg,x=5)}
\text{dessin=\dessin
line \xpt1,\ypt1,\xpt2,\ypt2,\co[\i]}
\text{val=texmath(\A*x+(\B))}
\text{tmp=\type=1?\(x \mapsto \val):\(y=\val)}
\text{coord=wims(append item \tmp to \coord)}
}
\text{dessin=\dessin
text black,290,150,medium,x
text black,155,2,medium,\ord}
\statement{\question
<div class="float_right">\draw{301,301}{\dessin}</div>
<div class="wimscenter">\embed{r1,\size}</div>}
\answer{Risposta}{\coord;\nom}{type=correspond}
