n=$counter
mathview=0
embed=1
inputs=1.1
cols=10
make_tr=1
varlist=x
rounding=100
!readproc $remarkdir/rounding.$taal
checkfile=$checkdir/new_number.proc
helptext=Als er geen grondtal wordt vermeld...veronderstellen we grondtal 10<br />log(a) == <sup>10</sup>log(a)
g=!randint 2,10
var2=$empty
var1=$empty
description=
g=!randint 2,10
a=!randint 11,21
a=!exec moneyprint $[$a/10] 1
answer$n=$[$g^($a)]
rans=$[(round($rounding*($(answer$n))))/$rounding]
ss=Bereken :<br />Geef je antwoord op in twee decimalen nauwkeurig
formula$n=^{$g} \log(x) = $a \rightarrow x = [?]

texanswer$n=\begin{array}{l} \textrm{We gebruiken de rekenregel :} \; ^{g} \log(x) = a \rightarrow x = g^{a} \\ ^{$g} \log(x) = $a \rightarrow x = $g^{$a} = $(answer$n) = $rans \end{array} \right.

question$n=<a onmouseover="return escape('<img src=\'$module_dir/gifs/hfst2.s33.jpg\' alt=\'logaritme\' />')">$ss</a>

#formula$n=\left\{ \begin{array}{l} a = b^{^{b}\log(a)} \\ a^{-c} =\left( b^{^{b}\log(a)} \right)^{-c} = b^{-c \cdot ^{b}\log(a)} \end{array} \right.
# help ^{a}\log(b) = \frac{\log(b)}{\log(a)} \rightarrow b = a^{^{a}\log(b)}









