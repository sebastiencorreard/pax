%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/binary2.cpp.

\language{fr}
\range{-5..5}
\computeanswer{no}
\format{html}
\precision{10000}

\author{Bernadette, Perrin-Riou}
\email{bernadette.m.riou@orange.fr}

\text{lang=slib(oef/env lang)}


\title{Conversion en binaire II}
\title_nl{Binaire getallen II}


\text{name_puissance=Puissances de }

\text{name_enonce=La décomposition en binaire de l'entier, est}

\text{name_question=Remplir les cases.}

\if{\lang=nl}{


\text{name_puissance=Machten van }

\text{name_enonce=De binaire representatie van het getal,is}

\text{name_question=Vul de tabel verder in.}

}

\integer{k=8}
\integer{n=0}
\integer{nbin=0}
\text{liste=2}
\text{choix1=randint(1..4)}
\text{choix2=randint(5..8)}
\for{i=1 to \k-1}{
  \integer{n1=random(1,0)}
  \integer{n= \n+\n1*2^(\i-1)}
  \integer{nbin=\nbin+\n1*10^(\i-1)}
  \text{liste=\liste, \n1}
  }
\integer{n1=random(1,1,1,1,0)}
\integer{n= \n+\n1*2^(\k-1)}
\integer{nbin=\nbin+\n1*10^(\k-1)}
\text{liste= \liste, \n1}
\text{choix1=randint(5..8)}
\text{choix2=randint(1..4)}
\text{repa= item(\choix1+1,\liste)}
\text{repb= item(\choix2+1,\liste)}
\text{nbin=pari(\nbin)}
\text{rep1=item(\k+1,\liste)}
\text{rep2=item(\k,\liste)}
\text{rep3=item(\k-1,\liste)}
\text{rep4=item(\k-2,\liste)}
\text{rep5=item(\k-3,\liste)}
\text{rep6=item(\k-4,\liste)}
\text{rep7=item(\k-5,\liste)}
\text{rep8=item(\k-6,\liste)}
\statement{\name_enonce[1] \n \name_enonce[2]
<table class="wimscenter wimsborder">
 <tr>
  <th>\name_power 2</th>
  <th> \(2^7)</th>
  <th> \(2^6)</th>
  <th> \(2^5)</th>
  <th> \(2^4)</th>
  <th> \(2^3)</th>
  <th> \(2^2)</th>
  <th>\(2^1)</th>
  <th>\(2^0) </th>
</tr><tr>
  <td> \n</td>
  <td>\if{\choix1=8}{\embed{reply 1,2}}{\rep1}</td>
  <td>\if{\choix1=7}{\embed{reply 1,2}}{\rep2}</td>
  <td>\if{\choix1=6}{\embed{reply 1,2}}{\rep3}</td>
  <td>\if{\choix1=5}{\embed{reply 1,2}}{\rep4}</td>
  <td>\if{\choix2=4}{\embed{reply 2,2}}{\rep5}</td>
  <td>\if{\choix2=3}{\embed{reply 2,2}}{\rep6}</td>
  <td>\if{\choix2=2}{\embed{reply 2,2}}{\rep7}</td>
  <td>\if{\choix2=1}{\embed{reply 2,2}}{\rep8}</td>
</tr>
</table>
\name_question}
\answer{}{\repa}{type=numeric}
\answer{}{\repb}{type=numeric}

