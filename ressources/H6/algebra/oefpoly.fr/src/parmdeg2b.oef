%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/parmdeg2b.cpp.

\text{lang=slib(oef/env lang)}
\language{fr}
\author{Gang, XIAO}
\email{xiao@unice.fr}
\format{html}

\range{-5..5}
\computeanswer{no}
\precision{10000}

\title{Paramétré deg 2 II}
\title_it{Parametrizzazione grado 2 II}
\title_en{Parametrized deg 2 II}

\integer{a=random(1..10)*random(-1,1)}
\integer{b=random(1..10)*random(-1,1)}
\integer{c=random(1..10)*random(-1,1)}
\integer{r=random(-5..5)}
\integer{u=random(2..5)*random(-1,1)}
\integer{v=random(2..5)*random(-1,1)}
\integer{w=random(2..5)*random(-1,1)}
\integer{s=(\r)^2*(\u)+(\r)*(\v)+(\w)}
\if{\s = 0}{
 \integer{w=\w+sign(\w)}
 \integer{s=(\r)^2*(\u)+(\r)*(\v)+(\w)}
}
\text{A=\a>0?+\a:\a}
\text{B=\b>0?+\b:\b}
\text{C=\c>0?+\c:\c}
\real{mm=-((\r)^2*(\a)+(\r)*(\b)+(\c))/(\s)}





























































note for translators: in the following keep commas
\text{name_statement=Pour quelles valeurs réelles du paramètre,le polynôme,a-t-il une racine égale à,sous condition que}















































































































\if{\lang=it}{





























































note for translators: in the following keep commas
\text{name_statement=Per quali valori del parametro reale,il polinomio,ha una radice uguale a,a condizione che}















































































































}
\if{\lang=en}{





























































note for translators: in the following keep commas
\text{name_statement=For which real value of the parameter,the polynomial,has a root equal to,Under the condition that}















































































































}

\statement{\name_statement[1] \(m\) \name_statement[2]
<div class="wimscenter">
\((\u m\A)X^2 + (\v m\B)X + (\w m\C)\)
</div>
\name_statement[3] \(\r\)&nbsp;? (\name_statement[4] \(\u{}m\A \ne 0\).)}
\answer{\(m)}{\mm}

