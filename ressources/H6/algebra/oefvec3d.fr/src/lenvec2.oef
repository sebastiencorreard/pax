%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/lenvec2.cpp.

\language{fr}
\range{-5..5}
\author{Gang XIAO}
\email{xiao@unice.fr}
\computeanswer{no}
\format{html}

\text{lang=slib(oef/env lang)}


\title{Produit vectoriel et longueur II}
\title_en{Vector product and length II}

\precision{10000}

\text{name_enonce=Soit, un vecteur dans l'espace. Soit un vecteur \(v\) de longueur ,
  Étant donné que le produit scalaire \(\langle u,v \rangle\) est égal à ,
  quelle est la longueur du produit vectoriel}
\text{name_answer=Longueur}
\text{name_hint=Décomposez \(v\) en somme de deux vecteurs \(v_1\) et \(v_2\),
  où \(v_1\) est perpendiculaire à \(u\), et \(v_2\) est proportionnel à \(u\).}



\if{\lang=en}{

\text{name_enonce=Let, be a space vector. We have another vector \(v\) whose length is ,
 Given that the scalar product \(\langle u,v \rangle\) equals ,
  what is the length of the vector product}
\text{name_answer=Longueur}
\text{name_hint=Decompose \(v\) into the sum of two vectors \(v_1\) and \(v_2\),
  where \(v_1\) is perpendicular to \(u\), and \(v_2) is proportional to}

}

\integer{range=15}
\integer{a1=random(1..\range)*random(-1,1)}
\integer{b1=random(1..\range)*random(-1,1)}
\integer{c1=random(1..\range)*random(-1,1)}
\integer{l=random(3..\range)}
\real{ll=sqrt((\a1)^2+(\b1)^2+(\c1)^2)}
\integer{ss=random(1..floor(\l*\ll)-2)*random(-1,1)}
\real{lon=sqrt(\l^2-(\ss/\ll)^2)*\ll}
\statement{\name_enonce[1] \(u=(\a1,\b1,\c1)\) \name_enonce[2] \l.
  \name_enonce[3] \(\ss \), \name_enonce[4] \(u\wedge v)?
}
\answer{\name_answer}{\lon}
\hint{\name_hint}

