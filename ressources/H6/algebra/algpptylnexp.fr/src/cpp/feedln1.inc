 Formons d'abord l'intersection des deux premiers intervalles :
<div class="wimscenter">\(I_1 \cap I_2\) = \def1 \cap \def2 = \def
</div>
 \if{\ensdef=1}{
    <p>Cet ensemble est vide, donc a fortiori l'intersection
    \((I_1 \cap \I_2) \cap I_3\) est vide.
    <br>Conclusion : L'inéquation (I) n'a aucune solution.</p>
  }{
  \if{\enssol=1}{
    <p>L'intersection de \(I_2 \cap I_3\) avec \(I_3\) est vide.
    On conclut alors que l'inéquation (I) n'a  aucune solution.</p>}
    {<p> Formons ensuite l'intersection avec le troisième intervalle :</p>
    <div class="wimscenter">\(I_1 \cap I_2 \cap I_3\) = \def \cap \def3 = \intsol </div>
    <p>On en conclut que
    l'ensemble des solutions de (I) est \intsol.</p>}
  }
