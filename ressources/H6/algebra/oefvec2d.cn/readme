在模块内创建 OEF 练习:

 1. 创建OEF类型的模块.

 2. 在你的模块里对每个练习建立一个新文件,
以 oef 为后缀, 放在目录 src/ 内(例如: src/euclide.oef).
把你写好或粘贴的源码放在文件能看到的文本区域, 将修改存盘.

 3. 测试模块.

 4. 修改 intro.phtml 与 endhook.phtml, 重新测试.

%%%%%%%%%%%%如果你要在本地服务器里直接使用

为建立 OEF 程序:

1. 把此样板目录(template)复制到该去的地方, 改用一个新的名称.

2. 修改 intro.phtml

3. 修改 INDEX.

4. 建立源文件.

5. 创建源文件.

6. 执行脚本程序 $wims_home/other/bin/src2def. (每当源文件修改后, 就要重新执行一次.)


