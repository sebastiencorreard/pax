\title{Suite géométrique}
\language{fr}
\range{-5..5}
\author{Eric, Sénéchal}
\email{ericsen@wanadoo.fr}
\computeanswer{no}
\format{html}
\precision{100}
\integer{b=random(1..100)}
\real{q=(\b/randitem(1,10,100))*randitem(-1,1)}
\integer{a=random(1..1000)}

\real{u0=\a}
\real{u1=\u0*\q}
\real{u2=\u1*\q}
\real{u3=\u2*\q}
\real{u4=\u3*\q}

\statement{Voici les premiers termes d'une suite géométrique :
<div class="wimscenter">
 \(u_0) = \u0 ; \(u_1) = \u1 ; \(u_2) = \u2 ; \(u_3) = \u3 </div>
 Quelle est sa raison ? Quel est le terme suivant ?
<ul>
  <li>Raison = \embed{reply 1,10} </li>
  <li>\(u_4) = \embed{reply 2,10}.</li>
</ul>
}
\answer{Raison}{\q}{type=default}
\answer{\(u_4)}{\u4}{type=default}
