%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/cubrt.cpp.

\language{en}
\range{-5..5}
\precision{10000}
\format{html}
\author{Gang, XIAO}
\email{xiao@unice.fr}
\computeanswer{yes}
\text{lang=slib(oef/env lang)}
\title{Racine cubique}
\title{Cubic root}
\title_it{Radice cubica}
\title_es{Raíz cúbica}
\title_fr{Racine cubique}
\title_nl{Vierkants wortel I}
\integer{a=random(1..20)*random(-1,1)}
\integer{b=random(2..20)*random(-1,1)}
\complex{z1=\a+\b*i}
/* note for translators: keep commas */
\text{name_statement=Find a cubic root \(w\) of the complex number,
by giving its real part and imaginary part}
\if{\lang=it}{
/* note for translators: keep commas */
\text{name_statement=Calcolare una radice cubica \(w\) del numero complesso,e dire quali siano la sua parte reale e immaginaria}
}
\if{\lang=es}{
/* note for translators: keep commas */
\text{name_statement=Encuentre una raíz cúbica \(w\) del número complejo,
entregando su parte real y parte imaginaria}
}
\if{\lang=fr}{
/* note for translators: keep commas */
\text{name_statement=Trouver une racine cubique \(w\) du nombre complexe,en donnant sa partie réelle et partie imaginaire}
}
\if{\lang=nl}{
/* note for translators: keep commas */
\text{name_statement=Bepaal het re&euml;le en imaginaire deel van de vierkants wortel
  \(w\) van het complexe getal}
}
\statement{\name_statement[1] \(z=\z1\), \name_statement[2].}
\answer{\(Re(w)\)}{\re}
\answer{\(Im(w)\)}{\im}
\condition{\(Re(w^3)=\a\)}{(\re)^3-3*(\re)*(\im)^2=\a}
\condition{\(Im(w^3)=\b\)}{3*(\re)^2*(\im)-(\im)^3=\b}
