%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/argsum.cpp.

\language{en}
\range{-5..5}
\precision{10000}
\format{html}
\author{Gang, XIAO}
\email{xiao@unice.fr}
\computeanswer{yes}
\text{lang=slib(oef/env lang)}
\title{Argument de somme}
\title{Argument of sum}
\title_it{Argomento della somma}
\title_es{Argumento de la suma}
\title_fr{Argument de somme}
\title_nl{Modulus en Argument III}
\integer{a1=random(-20..20)}
\integer{b1=random(2..20)*random(-1,1)}
\integer{a2=random(-20..20)}
\integer{b2=random(2..20)*random(-1,1)}
\integer{b2=\b1+(\b2)=0?\b1}
\integer{a=\a1+(\a2)}
\integer{b=\b1+(\b2)}
\complex{z1=\a1+\b1*i}
\complex{z2=\a2+\b2*i}
\real{mod=sqrt((\a)^2+(\b)^2)}
\real{ar=\b>0?pi/2:-pi/2}
\real{at=\a=0?\ar:arctan(\b/(\a))}
\real{Arg=\a<0?\at+pi:\at}
/* note for translators: keep commas */
\text{name_statement=Let,Give the module and an argument of}
\text{name_condition=is an argument of}
\if{\lang=it}{
/* note for translators: keep commas */
\text{name_statement=Siano,Calcolare il modulo e l'argomento di}
\text{name_condition=è l'argomento di}
}
\if{\lang=es}{
/* note for translators: keep commas */
\text{name_statement=Sean, Entregue el módulo y un argumento de}
\text{name_condition=es un argumento de}
}
\if{\lang=fr}{
/* note for translators: keep commas */
\text{name_statement=Soient,Donner le module et un argument de}
\text{name_condition=est un argument de}
}
\if{\lang=nl}{
/* note for translators: keep commas */
\text{name_statement=Gegeven,Geef modulus en argument vanf}
\text{name_condition=is een argument van }
}
\statement{\name_statement[1] \(z_1=\z1\), \(z_2=\z2\).
\name_statement[2] \(z=z_1+z_2\).}
\answer{\(|z|\)}{\mod}
\answer{\(Arg(z)\)}{\arg}
\condition{\arg \name_condition \(z\)}
 {abs((\arg-\Arg)/(2*pi)-round((\arg-\Arg)/(2*pi)))<0.0001}
