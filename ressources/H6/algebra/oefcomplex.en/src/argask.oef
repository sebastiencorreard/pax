%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/argask.cpp.

\language{en}
\range{-5..5}
\precision{10000}
\format{html}
\author{Gang, XIAO}
\email{xiao@unice.fr}
\computeanswer{no}
\text{lang=slib(oef/env lang)}
\title{Argument demandé}
\title{Asked argument}
\title_it{Trova l'argomento}
\title_es{Encontrar el argumento}
\title_fr{Argument demandé}
\title_nl{Modulus en Argument I}
\integer{a=random(-20..20)}
\integer{b=random(2..20)*random(-1,1)}
\complex{z=\a+\b*i}
\real{mod=sqrt((\a)^2+(\b)^2)}
\real{ar=\b>0?pi/2:-pi/2}
\real{at=\a=0?\ar:arctan(\b/(\a))}
\real{Arg=\a<0?\at+pi:\at}
\text{name_statement=Give the module and argument of the complex number}
\text{name_condition=is an argument of}
\if{\lang=it}{
\text{name_statement=Dare il modulo e l'argomento del numero complesso}
\text{name_condition=è un argomento di}
}
\if{\lang=es}{
\text{name_statement=Entregue el módulo y un argumento del número complejo}
\text{name_condition=es un argumento de}
}
\if{\lang=fr}{
\text{name_statement=Donner le module et un argument du nombre complexe}
\text{name_condition=est un argument de}
}
\if{\lang=nl}{
\text{name_statement=Geef Modulus en Argument van het complexe getal}
\text{name_condition=is het argument van}
}
\statement{\name_statement \(\z\).}
\answer{\(|z|\)}{\mod}{type=numeric}
\answer{\(Arg(z)\)}{\arg}{type=numeric}
\condition{\arg \name_condition \(z\)}
 {abs((\arg-\Arg)/(2*pi)-round((\arg-\Arg)/(2*pi)))<0.0001}
