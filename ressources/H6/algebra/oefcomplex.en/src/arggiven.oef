%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/arggiven.cpp.

\language{en}
\range{-5..5}
\precision{10000}
\format{html}
\author{Gang, XIAO}
\email{xiao@unice.fr}
\computeanswer{no}
\text{lang=slib(oef/env lang)}
\title{Argument donné}
\title{Given argument}
\title_it{Argomento dato}
\title_es{Argumento dado}
\title_fr{Argument donné}
\title_nl{Modulus en Argument II}
\real{mod=randint(1..100)/10}
\real{a1=randint(1..100)/10*random(-1,1)}
\integer{type=random(1,2)}
\real{arg=\type=1?\a1:\a1*pi}
\text{Arg=\type=1?\a1:\a1*pi}
\real{re=cos(\Arg)*\mod}
\real{im=sin(\Arg)*\mod}
/* note for translators: keep commas */
\text{name_statement=Compute the real part and imaginary part of the complex number \(z\) having,as module and having,as an argument}
\if{\lang=it}{
/* note for translators: keep commas */
\text{name_statement=Calcolare la parte reale e la parte immaginaria del numero complesso \(z\) avente,come modulo e,come argomento}
}
\if{\lang=es}{
/* note for translators: keep commas */
\text{name_statement=Calcule la parte real e imaginaria del número complejo \(z\) que tiene a, como módulo y que tiene a,como un argumento}
}
\if{\lang=fr}{
/* note for translators: keep commas */
\text{name_statement=Calculer la partie réelle et la partie imaginaire du nombre
complexe \(z\) ayant,comme module et,comme argument}
}
\if{\lang=nl}{
/* note for translators: keep commas */
\text{name_statement=CBepaal het re&euml;le en imaginaire deel van het complexe getal
  \(z\) met als modulus,en argument}
}
\statement{\name_statement[1] \(\mod\) \name_statement[2] \(\Arg\) \name_statement[3].}
\answer{\(Re(z)\)}{\re}{type=numeric}
\answer{\(Im(z)\)}{\im}{type=numeric}
