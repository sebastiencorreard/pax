%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/centercircle.cpp.

\text{lang=slib(oef/env lang)}



\title{Center of circle}
\title_fr{Centre de cercle}

\language{fr}
\range{-5..5}
\author{XIAO, Gang}
\email{xiao@unice.fr}
\computeanswer{yes}
\format{html}
\precision{10000}







\text{name_question=Find the center, of the circle passing through the three points}
\text{name_hint1=The center of the circle is the point at equal distance towards the
  three given points.}
\text{name_hint2=A point M is at equal distance towards two points A and B if and only if the
  vectorial sum AM + BM is perpendicular to the vector AB, which can be
  translated into a linear equation on the coordinates of M.}
\text{name_condition=is at equal distance towards the 3 points}

\if{\lang=fr}{







\text{name_question=Trouvez le centre,du cercle passant par les trois points}
\text{name_hint1=Le centre du cercle est le point qui est à distance égale vers les
  trois points donnés.}
\text{name_hint2=Un point M est à égale distance des deux points A et B si et seulement si la
  somme vectorielle AM + BM est perpendiculaire au vecteur AB, ce qui se traduit
  par une équation linéaire en les coordonnées de M.}
\text{name_condition= est à distance égale vers les 3 points}

}

\integer{x1=random(10..50)}
\integer{y1=random(10..30)}
\integer{x2=random(-50..-10)}
\integer{y2=random(10..30)}
\integer{x3=random(-20..20)}
\integer{y3=random(-50..-10)}
\statement{\name_question[1] \(p_0 =(x_0,y_0)\) \name_question[2]
<div class="wimscenter">
\(p_1=(\x1,\y1)\),
\(p_2=(\x2,\y2)\),
\(p_3=(\x3,\y3)\).
</div>}
\hint{\name_hint1
<p>
\name_hint2
</p>
}
\answer{\(x_0\)}{\x0}
\answer{\(y_0\)}{\y0}
\condition{\(p_0\) \name_condition}
  {(\x0-(\x1))^2+(\y0-(\y1))^2=(\x0-(\x2))^2+(\y0-(\y2))^2 and
    (\x0-(\x1))^2+(\y0-(\y1))^2=(\x0-(\x3))^2+(\y0-(\y3))^2}

