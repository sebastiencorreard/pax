%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/planplan.cpp.

\author{Bernadette, Perrin-Riou}
\language{fr}
\range{-5..5}
\email{bpr@math.u-psud.fr}
\computeanswer{yes}
\format{html}
\precision{10000}

\text{lang=slib(oef/env lang)}
\title{Distance ou angle entre deux plans}
\title_en{Distance or Angle Between two Planes}
\title_es{Distancia o ángulo entre dos planos}
\title_ca{Distància o angle entre dos plans}
\text{choix=parallèles, non parallèles}
\text{name_choix=distance,cosinus}
\matrix{name_instruction=Considérons les deux plans de \(\RR^3) d'équations respectives
  et
  Ils sont
  Calculer la distance entre les deux plans parallèles.
  Calculer le cosinus de l'angle que font les deux plans non parallèles.
  Les deux plans sont
  Une distance est un réel positif.
  Le cosinus d'un angle est compris entre -1 et 1. }
\text{name_hint1=Pour calculer l'angle que forment entre eux
  deux plans non parallèles, on peut calculer l'angle que font entre eux
  deux vecteurs \(\vec{n}_1) et \(\vec{n}_2) normaux respectivement
  aux plans. Le cosinus de l'angle cherché est alors donné par}
\text{name_hint2=Pour calculer la distance entre deux plans parallèles,
  on peut calculer la distance entre un point d'un des deux plans et l'autre plan.
  On peut aussi ramener les équations des deux plans sous la forme similaire
  \(a x + b y + c z = d_1) et \(a x + b y + c z = d_2).
  La distance entre les deux plans est alors égale à}
\if{\lang=en}{
\text{choix=parallel, non-parallel}
\text{name_choix=distance,cosine}
\matrix{name_instruction=Consider the two planes in \(\RR^3) with equations
and
They are
Compute the distance between the two parallel planes.
Compute the cosine of the angle between the two non-parallel planes.
The two planes are
A distance is a nonnegative real number.
The cosine of an angle lies between -1 and 1. }
\text{name_hint1=To compute the angle between two
non-parallel planes, one can compute the angle between
two vectors \(\vec{n}_1) and \(\vec{n}_2) orthogonal to
the planes. Le cosine sought for is then given by}
\text{name_hint2=To compute the distance between two parallel planes,
 one can compute the distance between a point on one of the two planes and the other plane.
 One may also write similar equations for the two planes
 \(a x + b y + c z = d_1) and \(a x + b y + c z = d_2).
 The distance between the two planes is then}
}
\if{\lang=es}{
\text{choix=paralelos, no paralelos}
\text{name_choix=distancia, coseno}
\matrix{name_instruction=Consideremos los dos planos de \(\RR^3) de ecuaciones respectivas
y
Estos planos son
Calcular la distancia entre los dos planos paralelos.
Calcular el coseno del ángulo formado por los dos planos no paralelos.
Ambos planes son
Una distancia es un real positivo.
El coseno de un ángulo está comprendido entre -1 y 1. }
\text{name_hint1=Para calcular el ángulo entre ellos
dos planos no paralelos, podemos calcular el ángulo entre los
dos vectoress \(\vec{n}_1) i \(\vec{n}_2) normales respectivamente
a los planos. El coseno del ángulo buscado viene dado por}
\text{name_hint2=Para calcular la distancia entre dos planos paralelos,
  se puede calcular la distancia entre un punto de uno de los dos planos y el otro plano.
  También podemos pasar las ecuaciones de los dos planos a la forma general
  \(a x + b y + c z = d_1) y \(a x + b y + c z = d_2).
  La distancia entre los dos planos es entonces igual a}
}
\if{\lang=ca}{
\text{choix=paral·lels, no paral·lels}
\text{name_choix=distància,cosinus}
\matrix{name_instruction=Considerem els dos plans de \(\RR^3) d'equacions respectives
i
Aquests plans són
Calcular la distància entre els dos plans paral·lels.
Calcular el cosinus de l'angle que formen els dos plans no paral·lels.
Tots dos plans són
Una distància és un real positiu.
El cosinus d'un angle està comprès entre -1 i 1. }
\text{name_hint1=Per calcular l'angle que formen entre ells
  dos plans no paral·lels, podem calcular l'angle entre els
  dos vectors \(\vec{n}_1) i \(\vec{n}_2) normals respectivament
  als plans. El cosinus de l'angle cercat ve donat per}
\text{name_hint2=Per calcular la distància entre dos plans paral·lels,
  es pot calcular la distància entre un punt d'un dels dos plans i l'altre pla.
  També podem passar les equacions dels dos plans a la forma general
  \(a x + b y + c z = d_1) i \(a x + b y + c z = d_2).
  Llavors la distància entre els dos plans és igual a}
}
\integer{a=randint(-10..10)}
\integer{b=randint(-10..10)}
\integer{c=randint(-10..10)}
\if{(\a)^2+(\b)^2+(\c)^2=0}{\integer{c=randint(1..4)}}
\integer{x1=randint(-5..5)}
\integer{y1=randint(-5..5)}
\integer{z1=randint(-1..1)}
\integer{r=randint(1..5)*random(1,-1)}
\real{d=(\a)*(\x1)+(\b)*(\y1)+(\c)*(\z1)}
\text{par=random(1,2)}
\integer{u=\par=1 ? randint(-10..10): \r*(\a)}
\integer{v=\par=1 ? randint(-10..10): \r*(\b)}
\integer{w=\par=1 ? randint(-10..10): \r*(\c)}
\integer{u=(\u)^2+(\v)^2+(\w)^2=0? randint(1..4)}
\integer{h=randint(-10..10)}
\function{equ1=simplify((\u)*x + (\v)*y + (\w)*z)}
\function{equ2=simplify((\a)*x + (\b)*y + (\c)*z)}
\real{cond=((\u)*(\b)-(\v)*(\a))^2+((\w)*(\b)-(\v)*(\c))^2+((\u)*(\c)-(\w)*(\a))^2}
\text{para=\cond=0 ? item(1,\choix): item(2,\choix)}
\real{sol=\cond=0 ?
abs((\u)*(\x1)+(\v)*(\y1)+(\w)*(\z1)-(\h))/((\u)^2+(\v)^2+(\w)^2)^(1/2):
(abs((\a)*(\u)+(\b)*(\v)+(\c)*(\w)))/(((\u)^2+(\v)^2+(\w)^2)^(1/2)*((\a)^2+(\b)^2+(\c)^2)^(1/2))}
\text{distan=\cond=0 ? \name_choix[1]:\name_choix[2]}
\steps{choice 1
reply 1}
\statement{\name_instruction[1;]\(\equ1= \h) \name_instruction[2;] \(\equ2 = \d).
\if{\step <= 1}{\name_instruction[3;] \embed{choice 1}.
}
\if{\step=2}{
  \if{\cond=0}{\name_instruction[4;]
  }{\name_instruction[5;]}
  }
}
\choice{\name_instruction[6;]}{\para}{\choix}
\answer{\distan}{\sol}{type=numeric}
\feedback{\reply1 <0 and \cond=0}{\name_instruction[7;]}
\feedback{abs(\reply1)>1 and \cond<>0}{\name_instruction[8;] }
\hint{
\if{\cond <>0 and \step=2}{\name_hint1
<div class="wimscenter">
 \(cos u= \frac{\vec{n}_1 . \vec{n}_2}{||\vec{n}_1||\; ||\vec{n}_2||})
</div>}
\if{\cond =0 and \step=2}{\name_hint2
<div class="wimscenter">\(\frac{|d_1 - d_2|}{\sqrt{a^2 + b^2 + c^2}})
</div>}
}
