%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/plan.cpp.

\author{Adeline,Grelot;Bernadette, Perrin-Riou}
\language{fr}
\range{-5..5}
\email{bpr@math.u-psud.fr}
\computeanswer{yes}
\format{html}
\precision{10000}

\text{lang=slib(oef/env lang)}
\title{Projection orthogonale inverse}
\title_en{Inverted Orthogonal Projection}
\title_es{Proyección ortogonal inversa}
\title_ca{Projecció ortogonal inversa}
Choix des équations et du vecteur v
\matrix{M=slib(matrix/invertible 3,2)}
\text{C1=column(1,\M)}
\text{C2=column(2,\M)}
\text{v=column(3,\M)}
\integer{a=item(1,\C1)}
\integer{b=item(2,\C1)}
\integer{c=item(3,\C1)}
\integer{c=\c=0?randint(1..3)*(random(-1,1))}
\integer{A=item(1,\v)}
\integer{B=item(2,\v)}
\integer{C=item(3,\v)}
Problème si A et B sont nuls (->C=0 donc v=vecteur nul)
\integer{A= \A=0 and \B=0 ? randint(1..3)*(random(-1,1))}
\integer{C=-((\A)*(\a)+(\B)*(\b))}
\integer{A=(\A)*(\c)}
\integer{B=(\B)*(\c)}
\text{v=\A , \B , \C}
\integer{test=\A*(\a)+(\B)*(\b)+(\C)*(\c)}
La normale (à P')
\text{normale=column(2,\M)}
Calcul de la projection
\text{prod=simplify((\A)*(item(1,\normale)) + (\B)*(item(2,\normale)) + (\C)*(item(3,\normale)))}
\text{norm2=simplify((item(1,\normale))^2 + (item(2 , \normale))^2 + (item(3,\normale))^2)}
\rational{vprime1=\A-(\prod)*(item(1,\normale)/\norm2)}
\rational{vprime2=\B-(\prod)*(item(2,\normale)/\norm2)}
\rational{vprime3=\C-(\prod)*(item(3,\normale))/\norm2}
\text{vprime=\vprime1 , \vprime2 , \vprime3}
\text{equation=simplify((\a)*x + (\b)*y + (\c)*z)}
\text{equation2=simplify((item(1,\C2))*x + (item(2 , \C2))*y + (item(3,\C2))*z)}
\text{name_instruction1=Soit \(\RR^3) muni de son produit scalaire usuel,
\(P) le plan défini par l'équation}
\text{name_instruction2=et \(P') le plan défini par l'équation}
\text{name_instruction3=Trouver un vecteur non nul \(v) de \(P) tel que
la projection orthogonale de \(v) sur \(P') soit
}
\matrix{name_answer=vecteur de P
appartient à \(P) et est non nul
La projection orthogonale de (\rep) sur \(P') est }
\if{\lang=en}{
\text{name_instruction1=In \(\RR^3) with its usual scalar product,
let \(P) be the plane given by the equation}
\text{name_instruction2=and let \(P') be the plane given by the equation}
\text{name_instruction3=Find a non-zero vector \(v) in \(P) such that
the orthogonal projection of \(v) onto \(P') is
}
\matrix{name_answer=vector in P
belongs to \(P) and is non-zero
the orthogonal projection of (\rep) onto \(P') is }
}
\if{\lang=es}{
\text{name_instruction1=Sea \(\RR^3) con su producto escalar habitual,
\(P) el plano definido por la ecuación}
\text{name_instruction2=y \(P') el plano definido por la ecuación}
\text{name_instruction3=Encontrar un vector no nulo \(v) de \(P) tal que
la proyección ortogonal de \(v) sobre \(P') sea
}
\matrix{name_answer=vector de P
pertenece a \(P) y es no nulo
La proyección ortogonal de (\rep) sobre \(P') es }
}
\if{\lang=ca}{
\text{name_instruction1=Considerem \(\RR^3) amb el seu producte escalar habitual,
\(P) el pla definit per l'equació}
\text{name_instruction2=i \(P') el pla definit per l'equació}
\text{name_instruction3=Trobeu un vector no nul \(v) de \(P) tal que
la projecció ortogonal de \(v) sobre \(P') és
}
\matrix{name_answer=vector de P
pertany a \(P) i és no nul
La projecció ortogonal de (\rep) sobre \(P') és }
}
\statement{\name_instruction1
<div class="wimscenter">\(\equation=0)</div>
\name_instruction2
<div class="wimscenter">\(\equation2=0).</div>
<p>
\name_instruction3 (\vprime1,\vprime2,\vprime3).
</p>
}
\answer{\name_answer[1;]}{\rep}{type=vector}
\condition{(\rep) \name_answer[2;] ?}{item(1,\rep)*(\a)+(item(2,\rep))*(\b)+
(item(3,\rep))*(\c)=0 and (item(1,\rep))^2+(item(2,\rep))^2+(item(3,\rep))^2 !=0}
\real{N1=(item(1,\rep)-(\vprime1))*(item(2,\normale))
-(item(2,\rep)-(\vprime2))*(item(1,\normale))}
\real{N2=(item(1,\rep)-(\vprime1))*(item(3,\normale))
-(item(3,\rep)-(\vprime3))*(item(1,\normale))}
\real{N3=(item(3,\rep)-(\vprime3))*(item(2,\normale))
-(item(2,\rep)-(\vprime2))*(item(3,\normale))}
\condition{\name_answer[3;] (\vprime) ?}{\N1=0 and \N2=0 and \N3=0}
