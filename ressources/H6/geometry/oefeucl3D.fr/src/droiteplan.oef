%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/droiteplan.cpp.

\author{Bernadette, Perrin-Riou}
\language{fr}
\range{-5..5}
\email{bpr@math.u-psud.fr}
\computeanswer{yes}
\format{html}
\precision{10000}

\text{lang=slib(oef/env lang)}
\title{Distance d'un point à une droite}
\title_en{Distance Between a Point and a Line}
\title_es{Distancia de un punto a una recta}
\title_ca{Distància d'un punt a una recta}
\integer{a=randint(-10..10)}
\integer{b=randint(-10..10)}
\integer{c=randint(-10..10)}
\integer{u=randint(-10..10)}
\integer{v=randint(-10..10)}
\integer{w=randint(-10..10)}
\integer{u1=randint(-10..10)}
\integer{v1=randint(-10..10)}
\integer{w1=randint(-10..10)}
\integer{u= (\u1-(\u))^2+(\v1-(\v))^2+(\w1-(\w))^2=0 ? \u1+1}
\text{QR= \u1 - (\u) , \v1 - (\v) , \w1 - (\w)}
\text{QP= \a - (\u) , \b - (\v) , \c - (\w)}
\real{num= ((item(2,\QR))*(item(3,\QP))-(item(3,\QR))*(item(2,\QP)))^2
+((item(3,\QR))*(item(1,\QP))-(item(1,\QR))*(item(3,\QP)))^2
+((item(1,\QR))*(item(2,\QP))-(item(2,\QR))*(item(1,\QP)))^2
}
\real{sol=((\num)/((item(1,\QR))^2+(item(2,\QR))^2+(item(3,\QR))^2))^(1/2)}
\text{name_instruction=Quelle est la distance du point \(P) de coordonnées (\a , \b , \c)
  à la droite \(D) passant par les deux points}
\matrix{name_vocab=et
distance}
\text{name_aide1=La distance d'un point \(P) à une droite passant par deux points
\(Q) et \(R) est donnée par
}
\text{name_aide2=Pour le voir, on se place dans le plan contenant \(P), \(Q) et \(R).
  Le nombre \(||\vec{QR}\wedge\vec{QP}||) est l'aire du parallélogramme construit
  à partir de \(P), \(Q) et \(R). C'est aussi le produit de la longueur
  \(QR) par la distance de \(P) à la droite \(QR).
}
\if{\lang=en}{
\text{name_instruction=What is the distance between the point \(P) with coordinates (\a , \b , \c)
and the line \(D) passing through the two points}
\matrix{name_vocab=and
distance}
\text{name_aide1=The distance between a point \(P) and the line \(D) passing through
the two points \(Q) and \(R) is given by
}
\text{name_aide2=To see that, one considers the plane containing \(P), \(Q) and \(R).
It contains the parallelogram constructed from \(P), \(Q) and \(R), the area of which is
\(||\vec{Q R}\wedge\vec{Q P}||). It is also the product of the length
\(QR) and the distance from \(P) to the line \(Q R).
}
}
\if{\lang=es}{
\text{name_instruction=¿Cuál es la distancia desde el punto \(P) de coordenadas (\a , \b , \c)
a la recta \(D) que pasa por los dos puntos}
\matrix{name_vocab=y
distancia}
\text{name_aide1=La distancia desde un punto \(P) a una recta que pasa por dos puntos
\(Q) y \(R) viene dada por
}
\text{name_aide2=Para verlo, nos colocamos en el plano que contiene \(P), \(Q) y \(R).
El número \(||\vec{QR}\wedge\vec{QP}||) es el área del paralelogramo construido
a partir de \(P), \(Q) y \(R). También es el producto de la longitud del segemnto
\(QR) por la distancia de \(P) a la recta \(QR).
}
}
\if{\lang=ca}{
\text{name_instruction=Quina és la distància del punt \(P) de coordenades (\a , \b , \c)
a la recta \(D) que passa pels dos punts}
\matrix{name_vocab=i
distància}
\text{name_aide1=La distància d'un punt \(P) a una recta que passa per dos punts
\(Q) i \(R) ve donada per
}
\text{name_aide2=Per veure-ho, ens situem al pla que conté \(P), \(Q) i \(R).
El número \(||\vec{QR}\wedge\vec{QP}||) és l'àrea del paral·lelogram construït
a partir de \(P), \(Q) i \(R). També és el producte de la longitud del segment
\(QR) per la distància de \(P) a la recta \(QR).
}
}
\statement{
\name_instruction
\(Q) = (\u , \v , \w) \name_vocab[1;] \(R) = (\u1 , \v1 , \w1).
}
\answer{\name_vocab[2]}{\sol}{type=numeric}
\hint{\name_aide1
<div class="wimscenter">
\(\frac{||\vec{QR} \wedge \vec{QP}||}{||QR||}).
</div>
\name_aide2
}
