%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/pointplan.cpp.

\author{Bernadette, Perrin-Riou}
\language{fr}
\range{-5..5}
\email{bpr@math.u-psud.fr}
\computeanswer{yes}
\format{html}
\precision{10000}

\text{lang=slib(oef/env lang)}
\title{Distance d'un point à un plan}
\title_en{Distance Between a Point and a Plane}
\title_es{Distancia de un punto a un plano}
\title_ca{Distància d'un punt a un pla}
\real{a=random(1,-1)*randint(10^2..10^4)/100}
\real{b=random(1,-1)*randint(10^2..10^4)/100}
\real{c=random(1,-1)*randint(10^2..10^4)/100}
\integer{u=randint(-10..10)}
\integer{v=randint(-10..10)}
\integer{w=randint(-10..10)}
\integer{u=(\u)^2+(\v)^2+(\w)^2=0 ? \u+randint(1..4)}
\integer{h=randint(-10..10)}
\function{equ = simplify((\u)*x + (\v)*y + (\w)*z)}
\real{sol= abs((\u)*(\a)+(\v)*(\b)+(\w)*(\c)-\h)/((\u)^2+(\v)^2+(\w)^2)^(1/2)}
\text{name_instruction=Calculer la distance du point (\a,\b,\c) au plan de
  \(\RR^3) d'équation
}
\matrix{name_voca=distance
Une distance est un réel positif.}
\if{\lang=en}{
\text{name_instruction=Find the distance between point (\a,\b,\c) and the plane of
  \(\RR^3) of equation
}
\matrix{name_voca=distance
A distance is a nonnegative real number.}
}
\if{\lang=es}{
\text{name_instruction=Calcular la distancia desde el punto (\a,\b,\c) al plano de
  \(\RR^3) de ecuación
}
\matrix{name_voca=distancia
Una distancia es un real positivo.}
}
\if{\lang=ca}{
\text{name_instruction=Calcular la distància del punt (\a,\b,\c) al pla de
 \(\RR^3) d'equació
}
\matrix{name_voca=distància
Una distància és un real positiu.}
}
\statement{\name_instruction \(\equ = \h).
}
\answer{\name_voca[1;]}{\sol}{type=numeric}
\feedback{\reply1< 0}{\name_voca[2;]}
