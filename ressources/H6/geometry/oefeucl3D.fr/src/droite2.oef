%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/droite.cpp.







\author{Bernadette, Perrin-Riou}

\title{Distance entre deux droites 2}
\title_en{Distance Between Two Lines 2}
\title_es{Distancia entre dos rectas 2}
\title_ca{Distància entre dues rectes 2}
\language{fr}
\range{-5..5}
\email{bpr@math.u-psud.fr}
\computeanswer{yes}
\format{html}
\precision{10000}
\text{lang=slib(oef/env lang)}
\integer{a=randint(-10..10)}
\integer{b=randint(-10..10)}
\integer{c=randint(-10..10)}
\integer{c= (\a)^2+(\b)^2+(\c)^2=0 ? randint(1..4)}
\integer{x1=randint(-5..5)}
\integer{y1=randint(-5..5)}
\integer{z1=randint(-1..1)}
\text{con=random(1,0)}
\text{t0=random(1,2,3,-1,-2)}
\integer{x2=\con=1 ? randint(-5..5): (\x1)+(\t0)*(\a)}
\integer{y2=\con=1 ? randint(-5..5): (\y1)+(\t0)*(\b)}
\integer{z2=\con=1 ? randint(-5..5): (\z1)+(\t0)*(\c)}
\text{par=random(1,2)}
\integer{r=randint(1..5)*random(1,-1)}
\integer{u=\par=1 ? randint(-10..10): (\r)*(\a)}
\integer{v=\par=1 ? randint(-10..10): (\r)*(\b)}
\integer{w=\par=1 ? randint(-10..10): (\r)*(\c)}
\integer{u= (\u)^2+(\v)^2+(\w)^2=0 ? randint(1..4)}
\text{equa= \a<>0 or \b<>0 ? simplify(((\b)*x - (\x1)*(\b) - (\a)*y + (\a)*\y1)) = 0 ,
simplify((\c)*x - (\c)*(\x1) - (\a)*z + (\a)*(\z1)) = 0: x = \x1 , y = \y1}
\text{equa1= \u<>0 or \v<>0 ? simplify((\v)*x - (\v)*(\x2) - (\u)*y + (\u)*\y2) = 0 ,
 simplify((\w)*x - (\w)*(\x2) - (\u)*z + (\u)*(\z2)) = 0: x = \x2 , y = \y2}
\real{cond=((\u)*(\b)-(\v)*(\a))^2+((\w)*(\b)-(\v)*(\c))^2+((\u)*(\c)-(\w)*(\a))^2}
\matrix{M= \x1-(\x2),\y1-(\y2), \z1-(\z2)
\a,\b,\c
\u,\v,\w
}
\real{condcon=det(\M)}
\text{para= \cond=0 ? 1:3}
\text{choix= parallèles ou confondues, sécantes, non coplanaires}
\text{texte=para=2 ? cosinus:distance}
\text{name_instruction1=Considérons les deux droites de l'espace données par les
  équations paramétriques}
\matrix{name_instruction2=et
  Ces droites sont
  Calculer la distance entre ces deux droites parallèles.
  Calculer le cosinus de l'angle que forment entre elles ces deux droites sécantes.
  Calculer la distance entre ces deux droites non coplanaires.
  Elles sont }
\text{name_hint1=Deux droites de l'espace sont soit parallèles, soit sécantes,
  soit ni l'un ni l'autre et elles sont alors non coplanaires.}
\text{name_hint2=Pour calculer l'angle que font deux droites sécantes
  dans l'espace, il suffit de calculer l'angle que font deux vecteurs directeurs
  de ces deux droites. }
\text{name_hint3=Pour calculer la distance de deux droites gauches, on considère
  un plan parallèle aux deux droites et contenant une des deux droites.
  On calcule alors la distance d'un point de l'autre droite à ce plan.}
\text{name_hint4=Pour calculer la distance de deux droites parallèles,
  on calcule la distance d'un point de l'une des deux droites à l'autre droite.}
\if{\lang=en}{
\text{choix= parallel or the same, intersecting, non-coplanar}
\text{texte=para=2 ? cosine:distance}
\text{name_instruction1=Consider the two lines in space with
parametric equations}
\matrix{name_instruction2=and
These lines are
Find the distance between these two parallel lines.
Find the cosine of the angle between these two intersecting lines.
Find the distance between these two non-coplanar lines.
They are }
\text{name_hint1=Two lines in space are either parallel, or intersecting,
or neither and then they are non-coplanar.}
\text{name_hint2=To find the angle between two intersecting lines
in space, it suffices to find the angle between two direction vectors
of these lines. }
\text{name_hint3=To find the distance between two non-coplanar lines, one considers
 a plane parallel to the two lines and containing one of them.
 One then computes the distance between a point on the other line and that plane.}
\text{name_hint4=To find the distance between two parallel lines,
 one computes the distance between a point on one line and the other line.}





}
\if{\lang=es}{
\text{choix= paralelas o coincidentes, secantes, no coplanarias}
\text{texte=para=2 ? coseno:distancia}
\text{name_instruction1=Consideremos las dos rectas del espacio dadas por las
ecuaciones}
\matrix{name_instruction2=y
Estas rectas son
Calcula la distancia entre estas dos rectas paralelas.
Calcula el coseno del ángulo entre estas dos rectas que se intersectan.
Calcula la distancia entre estas dos rectas no coplanarias.
Ellas son }
\text{name_hint1=Dos rectas en el espacio son paralelas, o secantes,
o ninguno de los dos y entonces son no coplanarias (se cruzan).}
\text{name_hint2=Para calcular el ángulo formado por dos rectas que se intersectan
en el espacio, es suficiente calcular el ángulo que forman dos vectores directores
de estas dos rectas. }
\text{name_hint3=Para calcular la distancia entre dos rectas no coplanarias (se cruzan), consideramos
 un plano paralelo a las dos rectas y que contiene una de las dos rectas.
 A continuación se calcula la distancia desde un punto de la otra recta a este plano.}
\text{name_hint4=Para calcular la distancia entre dos rectas paralelas,
 se calcula la distancia desde un punto de una de las dos rectas a la otra.}
}
\if{\lang=ca}{
\text{choix= paral·leles o coincidents, secants, no coplanàries}
\text{texte=para=2?cosinus:distància}
\text{name_instruction1=Considera les dues rectes a l'espai amb
equacions}
\matrix{name_instruction2=i
Aquestes rectes són
Trobeu la distància entre aquestes dues rectes paral·leles.
Trobeu el cosinus de l'angle entre aquestes dues rectes secants.
Trobeu la distància entre aquestes dues rectes no coplanàries.
Aquestes rectes són }
\text{name_hint1=Dues rectes a l'espai són paral·leles, o secants (es tallen),
 o són no coplanàries (s'encreuen). }
\text{name_hint2=Per trobar l'angle entre dues rectes que es tallen (secants)
a l'espai, només cal trobar l'angle entre dos vectors directors
d'aquestes rectes. }
\text{name_hint3=Per trobar la distància entre dues rectes no coplanàries, es considera
 un pla paral·lel a les dues rectes i que en conté una d'elles.
 Llavors es calcula la distància entre un punt de l'altra recta i aquest pla.}
\text{name_hint4=Per trobar la distància entre dues rectes paral·leles,
 es calcula la distància entre un punt d'una recta i l'altra recta.}
}
\text{para= \condcon=0 and \cond <>0 ? 2}
\text{sol= item(\para,\choix)}
\if{\para=2}{
\real{sol1= ((\a)*(\u)+(\b)*(\v)+(\c)*(\w))/
(((\a)^2+(\b)^2+(\c)^2)*((\u)^2+(\v)^2+(\w)^2))^(1/2)}
  }
  {
  \text{QR= \a , \b , \c}
  \text{QP= \x2 - (\x1) , \y2 - (\y1) , \z2 - (\z1)}
  \text{QS = \para=3 ? \u , \v , \w}
  \text{QS= \para=1 ? \QP}
  \real{num= ((item(2,\QR))*(item(3,\QS))-(item(3,\QR))*(item(2,\QS)))^2
+((item(3,\QR))*(item(1,\QS))-(item(1,\QR))*(item(3,\QS)))^2
+((item(1,\QR))*(item(2,\QS))-(item(2,\QR))*(item(1,\QS)))^2}
  \real{sol1= \para=1 ? ((\num)/((item(1,\QR))^2+(item(2,\QR))^2+(item(3,\QR))^2))^(1/2)}
 \if{\para=3}{
   \matrix{M=\QR
  \QS
  \QP
  }
  \real{sol1=abs(det(\M))/(\num)^(1/2)}}
}
\steps{choice 1
reply 1}
\statement{\name_instruction1 :
<div class="wimscenter"> \(\equa) </div>
\name_instruction2[1;]
<div class="wimscenter">\(\equa1 )</div>
 \if{\step <=1}{\name_instruction2[2;] \embed{choice 1}
}
\if{\step=2}{
\if{ \para= 1}{\name_instruction2[3;]}
\if{ \para= 2}{\name_instruction2[4;]}
\if{ \para= 3}{\name_instruction2[5;]}
}
}
\choice{\name_instruction2[6;]}{\sol}{\choix}
\answer{\texte}{\sol1}{type=numeric}
\hint{
  \if{\step <=1}{\name_hint1}
  \if{\step =2 and \para=2}{name_hint2}
  \if{\step =2 and \para=3}{
\name_hint3
  }
  \if{\step =2 and \para=3}{
  name_hint4
  }
}
