%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/coord1.cpp.

\language{fr}
\range{-5..5}
\author{Bernadette, Perrin-Riou}
\email{bernadette.m.riou@orange.fr}
\computeanswer{yes}
\format{html}

\precision{10000}

\text{lang=slib(oefenv lang)}


\title{Coordonnées cylindriques I}

\text{name_choicecoord=coordonnées cylindriques,coordonnées sphériques}


\text{choix=un demi-cercle, un cercle, une demi-droite, une droite, deux droites, un point}
\text{name_coord=coordonnées cylindriques,coordonnées sphériques}
\text{name_question=On considère l'ensemble des points de \(\RR^3) vérifiant en}
\matrix{name_question2=De quel type de lieu s'agit-il
  Donner le vecteur directeur unitaire de la demi-droite dans le repère cartésien&nbsp;
  Donner un vecteur unitaire normal au plan du cercle dans le repère cartésien&nbsp;
  Donner les coordonnées du point dans le repère cartésien&nbsp;}
\matrix{name_cond=point sur la droite 0z
  vecteur colinéaire
  Point
  vecteur unitaire
  Le vecteur donné est-il unitaire&nbsp;}
\text{name_type=lieu}
\text{name_hint=Faites un dessin&nbsp;}
\matrix{name_feed=Le point que vous donnez n'est pas sur la droite Oz.
  Le vecteur que vous donnez n'est pas sur la droite normale
  Le vecteur que vous donnez n'est pas unitaire
  La,Une, solution est le vecteur
  Le point est le point}

\text{H=randitem(1,2)}

\text{H=1}

Si H= 1 , coordonnées cylindriques (fonctionne mais pas d'etape 2 pour l'instant)
\text{maph=random(1,2)}
\text{mtheta=\maph=1? theta.gif: varphi.gif}
\text{mphi=\maph=1? varphi.gif:theta.gif}
\text{the=\maph=1 ? \theta: \varphi}
\text{phii=\maph=1 ? \varphi:\theta}
\text{coor=\H=1 ? \name_choicecoord[\H], r,\theta , z : \name_choicecoord[\H] , r, \the, \phii}
\text{coord=item(1,\coor)}
\text{thet=item(3,\coor)}
\matrix{liste= r,\theta , z
 r, \the, \phii
}
\text{liste1= row(\H,\liste)}
\text{choix2=item(1..2,shuffle(1,2,3))}
\integer{u1=item(1,\choix2)}
\integer{u2=item(2,\choix2)}
\text{b=random(1,2,3,4,6,12)}
\integer{c=randint(0..\b)}
\text{b2=random(1,2,3,4,6,12)}
\integer{c2=randint(0..\b2)}
\text{q1=pari(\c/\b)}
\text{q2=pari(\c2/\b2)}
\text{a=\u1=1 ? randint(1..10): \q1}
\text{b=\u2=1 ? randint(1..10): \q2}
\integer{c=randint(1..\b)}
\text{eq1= item(\u1,\liste1)}
\text{eq2= item(\u2,\liste1)}
\text{sol= \H=1 and ((\u1=1 and \u2=3) or (\u2=1 and \u1=3)) ? item(2,\choix)}
\text{sol= \H=1 and ((\u1=1 and \u2=2) or (\u2=1 and \u1=2)) ? item(4,\choix)}
\text{sol= \H=1 and ((\u1=2 and \u2=3) or (\u2=2 and \u1=3)) ? item(3,\choix)}
\text{sol= \H=2 and ((\u1=1 and \u2=3) or (\u2=1 and \u1=3)) ? item(2,\choix)}
\text{sol= \H=2 and ((\u1=1 and \u2=2) or (\u2=1 and \u1=2)) ? item(1,\choix)}
\text{sol= \H=2 and ((\u1=2 and \u2=3) or (\u2=2 and \u1=3)) ? item(3,\choix)}
\text{sol= \H=2 and ((\u1=1 and \u2=3 and \b=0) or (\u2=1 and \u1=3 and \a=0))
  ? item(6,\choix)}
\text{sol= \H=2 and ((\u1=1 and \u2=3 and \b=1) or (\u2=1 and \u1=3 and \a=1))
  ? item(6,\choix)}
\if{\H=2 and ((\u1=1 and \u2=3) or (\u2=1 and \u1=3))}{
  \real{t1= 0}
  \real{t2= 0}
  \real{t3= 1}
}
\if{\H=2 and ((\u1=1 and \u2=2) or (\u2=1 and \u1=2))}{
  \real{t1= \u1=2 ? -sin(\a*pi): -sin(\b*pi)}
  \real{t1= round(10^9*\t1)/1000000000}
  \real{t2= \u1=2 ? cos(\a*pi):cos(\b*pi)}
  \real{t2= round(10^9*\t2)/1000000000}
  \real{t3= 0}
}
\if{\H=2 and ((\u1=2 and \u2=3) or (\u2=2 and \u1=3))}{
  \real{t1= \u1=2 ? cos(\a*pi)*sin(\b*pi): cos(\b*pi)*sin(\a*pi)}
  \real{t1= round(10^9*\t1)/1000000000}
  \real{t2= sin(\a*pi)*sin(\b*pi)}
  \real{t2= round(10^9*\t2)/1000000000}
  \real{t3= \u1=2 ? cos(\b*pi): cos(\a*pi)}
  \real{t3= round(10^9*\t3)/1000000000}
}
\text{c= \u1=1 and \u2=3? \b}
\text{rr= \u1=1 and \u2=3? \a}
\text{c= \u1=3 and \u2=1? \a}
\text{rr= \u1=3 and \u2=1? \b}
\real{uu= \u1*\u2}
\text{test= \uu=3 and (\c=0 or \c=1)? -1:1}
\if{\H=2 and \test=-1}{
  \real{t1= 0}
  \real{t2= 0}
  \real{t3= \c=0? \rr:-\rr}
}

\text{dessin1=
arrow 0,0,3,0,10,black
arrow 0,0,0,3, 10,black
arrow 0,0,-1.6,-1.6, 10,black
text blue, -1.8,-1.8, medium, x
text blue,0.5,2.9, medium, z
text blue,2.8, 0.5,medium, y
}
\if{\H=2}{
  \text{dessin= xrange -3,3
yrange -3,3
\dessin1
linewidth 2
segment 0,0,2.2,2.2,green
segment 0,0, 2.2,-0.5,green
dsegment 2.2,-0.5,2.2,2.2,black
dsegment 0,2.5,2.2,2.2,black
trange 5*pi/4,2*pi-pi/16
plot , cos(t),sin(t)
arrow cos(-pi/16-0.2),sin(-pi/16-0.2),cos(-pi/16),sin(-pi/16),10, red
trange pi/4, pi/2
plot , cos(t),sin(t)
arc 0, 0, 2, 2, 45, 90, red
arc 0, 0, 2, 2, 225,348.75, red
text black, 1.1, 0.8, large, r
arrow cos(pi/4+0.2),sin(pi/4+0.2),cos(pi/4),sin(pi/4),10, red
copy 1.3*cos(3*pi/8),1.3*sin(3*pi/8),-1,-1,-1,-1,mathfonts/109/\mphi
copy 1.3*cos(3*pi/2+0.2),1.3*sin(3*pi/2+0.2),-1,-1,-1,-1,mathfonts/109/\mtheta
  }
}
{\text{dessin= xrange -3,3
yrange -3,3
\dessin1
linewidth 2
segment 0,0,2.2,2.2,green
segment 0,0, 2.2,-0.8,green
dsegment 2.2,-0.8,2.2,2.2,black
dsegment 0,2.8,2.2,2.2,black
arrow cos(-pi/16-0.4),sin(-pi/16-0.4),cos(-pi/16-0.2),sin(-pi/16-0.2),10, red
text black, 1.3, -0.6, medium, r
text black, -0.2, 1.5, medium, z
arc 0, 0, 2, 2, 225,335, red
copy 1.3*cos(3*pi/2+0.2),1.3*sin(3*pi/2+0.2),-1,-1,-1,-1,mathfonts/109/theta.gif
}
}

\matrix{STEP=choice1}
\matrix{STEP=\H=2? \STEP
reply1, reply2, reply3}
\steps{\STEP}
\statement{
<div class="wims_columns">
 <div class="medium_size img_col">\draw{150,150}{\dessin}</div>
 <div class="medium_size text_col">
  \name_question \coord
  \if{\H=1}{\(( r,\thet, z))}{
  	\if{\maph=1}{\((r,\thet,\phii)) }{\((r,\thet,\phii)) }
  }
  <p class="wimscenter">
  \if{(r isin \eq1 and i notin \eq1) or z isin \eq1}{\(\eq1=\a)}{
  	\if{\a<>0 and \a<>1}{\(\eq1=\a.\pi) }
  	\if{\a=0}{\(\eq1=0) }
  	\if{\a=1}{\(\eq1=\pi) }
  },
  \if{(r isin \eq2 and i notin \eq2)
      or z isin \eq2}{\(\eq2= \b\)}{
    \if{\b<>0 and \b<>1}{\(\eq2=\b.\pi)}
    \if{\b=0}{\(\eq2=0 )}
    \if{\b=1}{\(\eq2=\pi) }
  }</p>
  \if{\step <=1}{\name_question2[1;]? \embed{c1}}
  \if{\step=2}{
    \if{\H=2 and ((\u1=2 and \u2=3) or (\u2=2 and \u1=3)) and \test=1}
      {\name_question2[2;]:
    <div class="wimscenter">(\embed{reply 1,10},\embed{reply 2,10},\embed{reply 3,10}) </div>}
    \if{\H=2 and (\u1=1 or \u2=1 ) and \test=1}{
      \name_question2[3;]:
      <div class="wimscenter">(\embed{reply 1,10}, \embed{reply 2,10}, \embed{reply 3,10}) </div>
    }
    \if{\H=2 and \test=-1}{
      \name_question2[4;]:
      <div class="wimscenter">(\embed{reply 1,10}, \embed{reply 2,10}, \embed{reply 3,10}) </div>
    }
  }
  </div></div>
}
\choice{\name_type}{\sol}{\choix}

