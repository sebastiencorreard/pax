%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/basic1.cpp.

\language{fr}
\author{Gang XIAO}
\email{xiao@unice.fr}
\format{html}
\title{Dérivées de base }
\text{name_statement=Calculez la dérivée de la fonction \(f) définie par}
\integer{a1=random(2..8)*random(1,-1)}
\integer{a2=random(2..8)*random(1,-1)}
\integer{c1=random(2..8)*random(1,-1)}
\integer{c2=random(2..8)*random(1,-1)}
\integer{c3=random(2..8)*random(1,-1)}
\integer{c4=random(2..8)*random(1,-1)}
\function{lin=\c1*x+\c2}
\text{cos=random(sin,cos)}
\function{f=randitem(
 \c1*x^random(2,3) + \c2,
 \c1*x^random(2,3) + \c2*x,
 \a1*\cos(x+\c1),
 \a1*\cos(\c1*x),
 sqrt(\c1*x+\c2),
 (x+\c1)^2,
 \a1*exp(x+\c1),
 \a1*exp(\c1*x),
 \a1*log(x+\c1),
 \a1*log(\c1*x)
)}
\function{F=diff(\f,x)}
\statement{\name_statement \(f(x) = \f).
}
\answer{\(f'(x))}{\F}{type=formal}
