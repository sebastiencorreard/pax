!set title=Image réciproque d'un intervalle
!if $wims_read_parm!=$empty
 !goto $wims_read_parm
!endif
oef2wims_version=3.22
author=Bernadette, Perrin-Riou
email=bernadette.m.riou@orange.fr
language=fr
leftrange=-2.000000
rightrange=2.000000
computeanswer=yes
precision=100000
title_fr=Image réciproque d'un intervalle

ansorder=r
varcnt=34
prevarcnt=33
postvarcnt=0
replycnt=1
choicecnt=0
conditioncnt=0
feedbackcnt=0
format=html

val1=$imagedir
val2=$confparm1
val3=$confparm2
val4=$confparm3
val5=$confparm4

!if $testcondition=yes
 !goto test
!endif
!if $status=waiting
 !exit
!endif

!readproc slib/oef/env lang 
tmp0=$slib_out
val6=$(tmp0)
!if $val6=fr 
!endif
tmp=!trim Let \(f\) be the function defined on \(\displaystyle{\RR}\) by	  Let, What can be said of 
val7=!translate internal $	\
$ to ;; in $tmp
tmp=!trim is it equal to the interval \(I\)	  is it strictly contained in \(I\)	  does it contain strictly \(I\)	  none of these possibilities
val8=!translate internal $	\
$ to ;; in $tmp
val9=is always contained in \(I\). Your answer is not possible.
val10=the emtpy set
val11=an non-empty interval
val12=the union of two disjoint maximal intervals
!if $val6=fr 
tmp=!trim Soit la fonction \(f\) définie sur \(\displaystyle{\RR}\) par	  Soit, Que peut-on dire de
val7=!translate internal $	\
$ to ;; in $tmp
tmp=!trim est-il égal à l'intervalle \(I\)	  est-il strictement contenu dans \(I\)	  contient-il strictement \(I\)	  aucune de ces possibilités
val8=!translate internal $	\
$ to ;; in $tmp
val9=est toujours contenu dans \(I\). Votre réponse est donc impossible.
val10=l'ensemble vide
val11=un intervalle non vide
val12=la réunion de deux intervalles disjoints et maximaux
!endif
tmp=!trim Soit la fonction \(f\) définie sur \(\displaystyle{\RR}\) par	  Soit, Que peut-on dire de
val7=!translate internal $	\
$ to ;; in $tmp
tmp=!trim est-il égal à l'intervalle	  est-il strictement contenu dans	  contient-il strictement	  aucune de ces possibilités
val8=!translate internal $	\
$ to ;; in $tmp
val9=est toujours contenu dans \(I\). Votre réponse est donc impossible.
val10=l'ensemble vide
val11=un intervalle non vide
val12=la réunion de deux intervalles disjoints et maximaux
tmp0=!randitem 1,-1

val13=$[rint($(tmp0))]
tmp0=!randint 1, 15

tmp1=!randitem -1,1

val14=$[rint($(tmp0)*$(tmp1))]
val15=$[rint($val14*2)]
val16=$[-($val14)/($val13)]
tmp0=!randint 1, 50

tmp1=!randitem -1,1

val17=$[rint($(tmp0)*$(tmp1))]
val18=!rawmath $val13*x^2 + $val15*x + $val17
evaltmp=$val18

evaltmp=!mathsubst x=($val16) in $evaltmp
tmp0=($evaltmp)
val19=$[$(tmp0)]
tmp0=!randitem 1,2,3

val20=$[rint($(tmp0))]
tmp0=!randint $val19-20, $val19-9

tmp1=!randint $val19-6, $val19-2

tmp2=!randint $val19+2, $val19+8

tmp3=!item $val20 of $(tmp0),	  $(tmp1),	  $(tmp2)

val21=$[rint($(tmp3))]
tmp0=!randint $val19-8, $val19-2

tmp1=!randint $val19+7, $val19+16

tmp2=!randint $val19+9, $val19+20

tmp3=!item $val20 of $(tmp0),	 $(tmp1),	 $(tmp2)

val22=$[rint($(tmp3))]

!ifval $val13=1
val23= $val10,$val11,$val12 
!else
val23=$val12,$val11,$val10
!endif
tmp0=!shuffle 3

val24=$(tmp0)
val23=$(val23[$val24])
tmp0=!positionof item $val20 in $val24

val25=$(tmp0)
replytype1=radio
replyname1=Il est
replygood1=$val25;$val23
question=__EXECUTED_1692754_EXERCISE__

!goto stat

!exit

:question
$()	  $(val7[1;])	  <div class="wimscenter">
!insmath f(x) = $val18
$().</div>	 $(val7[2;1]) 
!insmath I
$() = [$val21 , $val22]. $(val7[2;2]) 
!insmath f^{-1}( I )
$() ? C'est	  <ul>	  <li> 
!read oef/embed.phtml reply1,1 
$()</li>	  <li> 
!read oef/embed.phtml reply1,2 
$()</li>	  <li> 
!read oef/embed.phtml reply1,3 
$()</li>	  </ul>	
$()
!exit

:hint
$()
$()
!exit

:help
$()
$()
!exit

:solution
$()
$()
!exit

:latex
$()
$()
!exit

:feedback

!exit

:test

!exit

:stat
vsavelist=7,18,21,22
embedcnt=3
