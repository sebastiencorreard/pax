%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/acos.cpp.

\language{fr}
\range{-5..5}
\author{Gang, XIAO}
\email{xiao@unice.fr}
\format{html}

\precision{10000}
\computeanswer{yes}

\text{lang=slib(oef/env lang)}

\title{arccos(sin)}
\title_en{arccos(sin)}

\text{name_enonce=Écrire,sous la forme,avec \(a\) et \(b\) des nombres rationnels. }
\text{name_hint=Attention, on demande que \(a\) et \(b\)
  soient des nombres rationnels, ils sont en fait décimaux.
  Vérifier qu'il n'y a alors qu'une seule solution.}



\if{\lang=en}{

\text{name_enonce=Compute,writing it under the form,where \(a\) and \(b\) are rational numbers.}
\text{name_hint=Attention, on demande que \(a\) et \(b\)
  soient des nombres rationnels, ils sont en fait décimaux.
  Vérifier qu'il n'y a alors qu'une seule solution.}

}

\real{x=random(-1,1)*floor(random(10..150))/10}
\integer{ra=random(1,2)}
\real{add=item(\ra,0,pi/2)}
\text{cos=item(\ra,cos,sin)}

\text{sin=item(\ra,sin,cos)}

\real{r=\x+\add}
\real{c=floor(\r/pi)}
\real{d=floor(\c/2)}

\real{a=\c%2=0?-\x:\x}
\real{e2=item(\ra,-1.5,-0.5)}
\real{b=\c%2=0?2*\d+0.5:-2*\d+\e2}
\text{choix=arc\sin}

\statement{\name_enonce[1] \(x = \choix(\cos(\x))\) \name_enonce[2]
  \(x = a + b pi\) \name_enonce[3]
}
\answer{\(a\)}{\a}
\answer{\(b\)}{\b}
\hint{\name_hint}

