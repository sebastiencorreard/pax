\title	{Osnovni odvodi I}
\author	{XIAO Gang}
\email	{xiao@unice.fr}
\format	{html}

\integer{a1=random(2..8)*random(1,-1)}
\integer{a2=random(2..8)*random(1,-1)}
\integer{c1=random(2..8)*random(1,-1)}
\integer{c2=random(2..8)*random(1,-1)}
\integer{c3=random(2..8)*random(1,-1)}
\integer{c4=random(2..8)*random(1,-1)}
\function{f=slib(data/randline src/simple1.data)}
\function{f=maxima(subst(\c1,c1,subst(\c2,c2,subst(\c3,c3,subst(\c4,c4,\f)))))}
\function{F=diff(\f,x)}

\statement{Izraèunajte odvod funkcije \(f(x) = \f).
}

\answer{Odvod \(f'(x))}{\F}{type=formal}
