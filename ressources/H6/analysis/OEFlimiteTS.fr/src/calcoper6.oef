%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/calcoper.cpp.

\author{Régine, Mangeard}
\email{regine@mangeard.fr}
\format{html}


\title{Opérations sur les limites 1}

\text{name_choice=Choisissez la bonne réponse&nbsp;}
\text{infd=\(+\infty)}
\text{infg=\(-\infty)}
\text{zero=\(0)}
\text{sans=sans limite}
\text{size=80x30x1}
\text{listinf=\infd, \infg}
\text{lstlim=a une limite finie nulle, a une limite finie non nulle, a une limite infinie, n'a pas de limite}

\integer{cas=1}
\integer{x0=0}
\integer{chpos=randint(1,2)}
integer{chpos=2}

\if{\cas=1}{
\integer{a1=randint(1..5)*randint(1,-1)}
\integer{a2=randint(1..5)*randint(1,-1)}
\integer{b1=randint(-5..5)}
\integer{b2=randint(-5..5)}
\integer{c1=randint(1..5)*randint(1,-1)}
\integer{c2=randint(1..5)*randint(1,-1)}
\integer{n=randint(1..3)}
\integer{choix=randint(1..4)}
///
integer{choix=4}
\if{\chpos=1}{
 \text{pos=0}
 \text{x0=0}
\rational{coef=\a1/\a2}
\text{indlim=\choix=1?3:2}
\text{indlim=\choix=3?1}
\if{\choix=1}{
  \text{mstep=r1
  r3}
  \if{\coef>0}{\text{liminf=\infd}}
              {\text{liminf=\infg}}
  \integer{n1=\n}
  \integer{n2=\n+randint(2,4)}
  \integer{indlim=3}
}
\if{\choix=2}{
  \text{mstep=r1
  r2}
  \integer{n1=\n}
  \integer{n2=\n}
  \rational{l1=\a1/\a2}
  \integer{indlim=2}
}
 \if{\choix=3}{
    \text{mstep=r1}
    \integer{n2=\n}
    \integer{n1=\n+randint(1..3)}
    \integer{indlim=1}
  }
 \if{\choix=4}{
    \text{mstep=r1}
    \integer{n1=\n}
    \integer{n2=\n+randint(1,3)}
    \integer{indlim=4}
  }
 }{
 \integer{indpos=randint(1,2)}
 \text{pos=\indpos=1?+\infty:-\infty}
 \text{x0=\indpos=1?+\infty:-\infty}
\rational{coef=\c1/\c2}
\text{indlim=\choix=1?3:2}
\text{indlim=\choix=3?1}
\if{\choix=1}{
  \text{mstep=r1
                 r3}
  \if{\coef>0}{\text{liminf=\infd}}
              {\text{liminf=\infg}}
  \integer{n2=\n}
  \integer{n1=\n+randint(2,4)}
  \integer{indlim=3}
  }
  \if{\choix=2}{
    \text{mstep=r1
             r2}
    \integer{n1=\n}
    \integer{n2=\n}
    \rational{l1=\c1/\c2}
    \integer{indlim=2}
  }
  \if{\choix=3}{
    \text{mstep=r1}
    \integer{n1=\n}
    \integer{n2=\n+randint(1..3)}
    \integer{indlim=1}
  }
  \if{\choix=4}{
    \text{mstep=r1
        r3}
    \integer{n2=\n}
    \integer{n1=\n+randint(1,3)}
    \integer{indlim=3}
     \if{(\coef>0 and \indpos=1) or (\coef < 0 and \indpos=2)}{\text{liminf=\infd}}
              {\text{liminf=\infg}}
  }
}
\text{f=(maxima(expand(x^\n1*(\c1*x^2+\b1*x+\a1))))/(maxima(expand(x^\n2*(\c2*x^2+\b2*x+\a2))))}
\text{ft=texmath(\f)}
}
\if{\cas=2}{
  \integer{a=randint(1..5)}
  \integer{c=randint(1..5)}
  \integer{cc=\c^2}
  \integer{d=\c^2-\a}
  \integer{e=randint(1..5)}
  \integer{f=\c*\a+\cc}
  \integer{b=\c+\e}
  \integer{g=\b*\a+\cc}
  \text{data=(sqrt(\d+x)-\c)/(\a-x),\a,-1/(2*\c),sqrt(\d+x)+\c,2
(sqrt(\d+x)-\c)/(x-\a),\a,1/(2*\c),sqrt(\d+x)+\c,2
(\a-x)/(sqrt(\d+x)-\c),\a,-2*\c,sqrt(\d+x)+\c,2
(x-\a)/(sqrt(\d+x)-\c),\a,2*\c,sqrt(\d+x)+\c,2
(x^2-\cc*x)/(sqrt(x)-\c),\cc,2*\cc*\c,sqrt(\x)+\c,2
(x^2-\cc*x)/(\c-sqrt(x)),\cc,-2*\cc*\c,sqrt(x)+\c,2
(sqrt(\f-\c*x)-sqrt(\g-\b*x))/(x-\a),\a,\e/(2*\c),sqrt(\f-\c*x)+sqrt(\g-\b*x),2
(x-\a)/(sqrt(\f-\c*x)-sqrt(\g-\b*x)),\a,2*\c/\e,sqrt(\f-\c*x)+sqrt(\g-\b*x),2
(sqrt(\f-\c*x)-sqrt(\g-\b*x))/(x-\a)^2,\a,\e/(2*\c),sqrt(\f-\c*x)+sqrt(\g-\b*x),4
(x-\a)^2/(sqrt(\f-\c*x)-sqrt(\g-\b*x)),\a,0,sqrt(\f-\c*x)+sqrt(\g-\b*x),1
(sqrt(\f-\c*x)-sqrt(\g-\b*x))/(x-\a)^3,\a,\e/(2*\c),sqrt(\f-\c*x)+sqrt(\g-\b*x),3
(x-\a)^3/(sqrt(\f-\c*x)-sqrt(\g-\b*x)),\a,0,sqrt(\f-\c*x)+sqrt(\g-\b*x),1
}
\integer{i=randint(1..rows(\data))}
///
integer{i=4}
\text{cedata=row(\i,\data)}
\text{f=\cedata[1]}
\text{ft=texmath(\f)}
\integer{x0=\cedata[2]}
\rational{l1=\cedata[3]}
\text{indet=texmath(maxima(\cedata[4]))}
\integer{indlim=\cedata[5]}
\if{\indlim=1 or \indlim=4}{
  \text{mstep=r1}
}
\if{\indlim=2}{
  \text{mstep=r1
              r2}
 }
\if{\indlim=3}{
  \text{mstep=r1
              r3}
  \text{liminf=\infd}
 }
}
\if{\cas=3}{
\integer{a=randint(1..5)}
\integer{c=randint(1..5)}
\integer{cc=\c^2}
\integer{d=\c^2-\a}
\integer{e=randint(1..5)}
\integer{f=\c*\a+\cc}
\integer{b=\c+\e}
\integer{g=\b*\a+\cc}
\text{data=(sqrt(1+sin(x))-1)/x,0,1/2,sqrt(1+sin(x))+1,2
(sqrt(1+sin(x))-sqrt(1-sin(x)))/x,0,1,sqrt(1+sin(x))+sqrt(1-sin(x)),2
(sqrt(\cc+sin(\a*x))-sqrt(\cc-sin(\a*x)))/(\e*x),0,\a/(\e*\c),sqrt(\cc+sin(\a*x))+sqrt(\cc-sin(\a*x)),2
(sqrt(1+sin(x))-1)/x^2,0,1/2,sqrt(1+sin(x))+1,4
(sqrt(1+sin(x))-sqrt(1-sin(x)))/x^2,0,1,sqrt(1+sin(x))+sqrt(1-sin(x)),4
(sqrt(\cc+sin(\a*x))-sqrt(\cc-sin(\a*x)))/(\e*x^2),0,\a/(\e*\c),sqrt(\cc+sin(\a*x))+sqrt(\cc-sin(\a*x)),4
(sqrt(1+sin(x))-1)/x^3,0,1/2,sqrt(1+sin(x))+1,3
(sqrt(1+sin(x))-sqrt(1-sin(x)))/x^3,0,1,sqrt(1+sin(x))+sqrt(1-sin(x)),3
(sqrt(\cc+sin(\a*x))-sqrt(\cc-sin(\a*x)))/(\e*x^3),0,\a/(\e*\c),sqrt(\cc+sin(\a*x))+sqrt(\cc-sin(\a*x)),3
(sqrt(1+sin(x))-1)/sqrt(x),0^+,1/2,sqrt(1+sin(x))+1,1
(sqrt(1+sin(x))-sqrt(1-sin(x)))/sqrt(x),0^+,1,sqrt(1+sin(x))+sqrt(1-sin(x)),1
(sqrt(\cc+sin(\a*x))-sqrt(\cc-sin(\a*x)))/(\e*sqrt(x)),0^+,\a/(\e*\c),sqrt(\cc+sin(\a*x))+sqrt(\cc-sin(\a*x)),1
}
\integer{i=randint(1..rows(\data))}
///
integer{i=10}
\text{cedata=row(\i,\data)}
\text{f=\cedata[1]}
\text{ft=texmath(\f)}
\text{x0=\cedata[2]}
\rational{l1=\cedata[3]}
\text{indet=texmath(maxima(\cedata[4]))}
\integer{indlim=\cedata[5]}
\if{\indlim=1 or \indlim=4}{
  \text{mstep=r1}
 }
\if{\indlim=2}{
  \text{mstep=r1
              r2}
 }
\if{\indlim=3}{
  \text{mstep=r1
              r3}
  \text{liminf=\infd}
 }
}

\text{mstep=r4,r5
\mstep}
\text{nc=\chpos=1?\a1*x^(\n1):maxima(\c1*x^(\n1+2))}
\text{dc=\chpos=1?\a2*x^(\n2):maxima(\c2*x^(\n2+2))}

\steps{\mstep
}

\statement{
<div class="wims_smallhelp">Cet exercice comporte plusieurs étapes.</div>
<p>On veut déterminer la limite éventuelle en \(\pos) de la fonction \(f) définie par</p>
<div class="wimscenter"> \(f(x)=\ft).</div>
\if{\step=1}{
En \(\pos), quels sont les termes prépondérants au numérateur et au dénominateur:
<div class="wimscenter">
<label for="reply4">Au numérateur :</label> \embed{reply4,7}<br>
<label for="reply5">Au dénominateur :</label> \embed{reply5,7}
</div>
}
{
En \(\pos), les termes prépondérants au numérateur et au dénominateur sont&nbsp;:
<div class="wimscenter">
Au numérateur \(\nc)<br>
Au dénominateur \(\dc).
</div>
}
\if{\step=2}{
\name_choice:
<div class="wimscenter">
En \(\pos), la <label for="reply1">fonction \(f)</label> \embed{reply 1}
</div>
}
\if{\step=3}{
  \if{\indlim=2}{
Quelle est la limite finie de \(f) en \(\pos)&nbsp;?
<div class="wimscenter">
<label for="reply2">\(\displaystyle{\lim_{x \rightarrow \pos}f(x) = })</label> \embed{reply 2}
</div>
  }{
\name_choice:
<div class="wimscenter">
\(\displaystyle{\lim_{x \rightarrow \pos}f(x) = }) \embed{reply 3}
</div>
  }
}
}
\answer{type limite}{\indlim;\lstlim}{type=menu}
\answer{}{\l1}{type=numexp}
\answer{}{\liminf;\infg,\infd}{type=clickfill}
\answer{numérateur}{\nc}{type=formal}
\answer{dénominateur}{\dc}{type=formal}

