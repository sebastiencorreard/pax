%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/frise1.cpp.

\title{Frise F1}
\title_nl{Strookpatroongroep F1}
\title_en{Frieze group F1}
\title_it{Fregi F1}
\title_es{Friso F1}
\title_ca{Fris F1}

\author{Bernadette, Perrin-Riou}
\email{bernadette.m.riou@orange.fr}
\text{lang=slib(oef/env lang)}

\integer{s=6}
\integer{t=3}
\text{colors=shuffle(blue,red,yellow,green,purple,blue,red,yellow,green,purple)}
\text{point=wims(makelist wims(makelist x:y for x = 0 to \s-1) for y=-\t+1 to \t)}
\text{point=shuffle(\point)}
\integer{N=5}
\matrix{frise=\point[1..\N]}
\matrix{frise=wims(replace internal , by ; in \frise)}
\matrix{frise=wims(replace internal : by , in \frise)}
\integer{n=3}
\text{Rep=}
\text{Frise=}
\for{u=1 to \N}{
  \text{rep1=}
  \for{j=1 to \n}{
    \text{h=[\frise[\u;1],\frise[\u;2]]}
    \text{rep=pari(\h+[\j*\s,0])}
    \text{rep=wims(replace internal , by : in \rep)}
    \text{rep1=wims(append item \rep to \rep1)}
    \text{frise1=wims(replace internal , by : in \frise[\u;])}
  }
 \text{Frise=wims(append line square \colors[\u],\frise1 to \Frise)}
 \text{Rep=wims(append line \colors[\u],\rep1 to \Rep)}
}
matrix{Rep=\Rep}
\integer{x1=(\n+1)*\s}
\integer{s1=2*\s}
\integer{width=100*(\n+1)}
\integer{height=100}
\text{xrange=0,\x1}


\text{translation=draw(\width,\height
xrange \xrange
yrange -\t,\t
arrow 1,-\t+1.5, 1+\s-0.3,-\t+1.5,10,black
transparent white
linewidth 6
\points)
}

\text{dessin=xrange \xrange
yrange -\t,\t
\Frise
background_color lightgreen
copy 0,0,\translation}

\text{wims_version=slib(oef/env wims_version)}
\text{wims_version=wims(char 1 to 4 of \wims_version)}
\if{\wims_version > 4.08}{
 \text{warning=Cliquer sur le bouton pour changer de couleur.}
 \if{\lang=en}{
  \text{warning=Click on the button to change the colour}
 }
 \if{\lang=nl}{
  \text{warning=Klik op de knop om van kleur te veranderen.}
 }
 \if{\lang=it}{
  \text{warning=Cliccare sul pulsante per cambiare il colore dei quadretti}
  }
 \if{\lang=es}{
  \text{warning=Clicar sobre el botón para cambiar el color}
 }
 \if{\lang=ca}{
  \text{warning=Clicar sobre el botó per canviar el color}
 }
}
{
 \text{warning=\wims_version Utilisez le click droit pour changer de couleur.}
 \if{\lang=en}{
  \text{warning=The right mouse click changes the colour.}
 }
 \if{\lang=nl}{
  \text{warning=De rechter muisknop gebruiken om de kleur te veranderen.}
 }
 \if{\lang=it}{
  \text{warning=Utilizzare il tasto destro del mouse per cambiare il
colore dei quadretti.}
 }
\if{\lang=es}{
  \text{warning=El clic derecho del mouse cambia el color.}
 }
\if{\lang=ca}{
  \text{warning=El clic dret del ratolí canvia el color.}
 }
}

\text{instruction=En cliquant sur les carrés, prolonger la figure en une frise
par translation du vecteur dessiné. }

\if {\lang=en}{
 \text{instruction=Complete, by clicking the squares,
 the pattern [a Frieze group] by repetitive translation in the direction and distance dictated by the vector. }
}


\if {\lang=nl}{
 \text{instruction=Klik met je muis op de vierkantjes
  en maak zo het patroon [een strookpatroongroep of Frieze] verder af d.m.v. herhaalde translatie in richting en afstand met de getoonde vector.}
}

\if{\lang=it}{
\text{instruction=Utilizzando il minor numero di quadretti, prolungare la figura in un fregio
con traslazioni corrispondenti al vettore indicato. }
}

\if {\lang=es}{
 \text{instruction=Haciendo clic en los cuadrados, extender la figura en un friso
por traslación del vector dibujado.}
}

\if {\lang=ca}{
 \text{instruction=Clicant en els quadrats, estén la figura en un fris
per translació del vector dibuixat.}
}

\statement{\instruction
<div class="wimscenter">
\embed{r1, \width x \height
[\dessin]}
</div>
<div class="wims_instruction">\warning</div>
}
\answer{}{\Rep}{type=clicktile}

