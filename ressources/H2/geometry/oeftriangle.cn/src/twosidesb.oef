\title{两条锐角边}
\language{cn}
\range{-5..5}
\author{Gang XIAO}
\email{xiao@unice.fr}
\computeanswer{no}
\format{html}
\precision{10000}

\integer{ab=random(10..40)}
\integer{bc=random(10..40)}
\integer{max=max(\ab,\bc)}
\integer{min=min(\ab,\bc)}
\text{long=\ab>\bc?AB:BC}
\text{short=\ab>\bc?BC:AB}
\real{sq1=sqrt(\max^2+\min^2)}
\real{sq2=sqrt(\max^2-\min^2)}
\integer{n1=(ceil(\sq1)-1) - (floor(\sq2)+1) + 1}
\integer{n2=(\ab+\bc-1) - (floor(\sq1)+1) + 1}
\integer{n3=(ceil(\sq2)-1) - (\max-\min+1) + 1}
\integer{n3=\n3<0?0}
\integer{o=random(1,2)}
\text{type=item(\o,锐角, 钝角)}
\integer{n=item(\o,\n1,\n2+\n3)}
\text{ans=\o=1?为使 ABC 成为锐角三角形, 根据勾股定理, AC 必须大于
sqrt(\long<sup>2</sup>-\short<sup>2</sup>)=\sq2 且小于
sqrt(AB<sup>2</sup>+BC<sup>2</sup>)=\sq1.
}
\text{ans=\o=2?为使 ABC 成为三角形, AC 至少是
\max-\min+1 至多是 \ab+\bc-1. <p>
当三角形的最大边是 \long 时, 根据勾股定理, AC 必须小于 \sq2. 
当 AC 是最大边时, 再次根据勾股定理, 它必须大于 \sq1. <p>
所以有 \n3 (相应地, \n2) 个钝角三角形其最大边是
\long (相应地, AC)}

\statement{有多少个\type三角形 ABC 满足以下条件
<p><center>
    AB = \ab , BC = \bc , AC = 整数 ?
</center>}


\answer{三角形个数}{\n}{type=default}


\solution{\ans}
