!set title=Rectangle
!if $wims_read_parm!=$empty
 !goto $wims_read_parm
!endif
oef2wims_version=3.22
author=Gang XIAO
email=xiao@unice.fr
keywords=pbsolving,linear_system
leftrange=-5.000000
rightrange=5.000000
computeanswer=no
precision=10000
title_fr=Rectangle
title_ca=Rectangle
title_es=Rectangle
title_nl=Rechthoek

ansorder=r,r
varcnt=26
prevarcnt=25
postvarcnt=0
replycnt=2
choicecnt=0
conditioncnt=0
feedbackcnt=0
format=html

val1=$imagedir
val2=$confparm1
val3=$confparm2
val4=$confparm3
val5=$confparm4

!if $testcondition=yes
 !goto test
!endif
!if $status=waiting
 !exit
!endif

!readproc slib/oef/env lang 
tmp0=$slib_out
val6=$(tmp0)
val7=We have a rectangle whose perimeter is,	  mm. Given that the length is ,	  mm longer than the width, find out the length and the width (in mm).
val8=Length,Width
val9=Let \(x\)  the length, \(y\) the width. Then	  we have the following system.
val10=And the solution of this system is
!if $val6=fr 
val7=Un rectangle a un périmètre de ,	  mm. Sachant que sa longueur fait,	  mm de plus que sa largeur, trouver sa longueur et sa largeur (en mm).	
val8=Longeur, Largeur
val9=Soit \(x\) la longueur et \(y\) la largeur. Alors	  nous avons le système suivant.
val10=La solution de ce système est
!endif
!if $val6=ca 
val7=El perímetre d'un rectangle és,	  mm. Saben que és , mm més llarg que ample, troba la longitud	  i l'amplada del rectangle (en mm).
val8=Longitut,Amplada
val9=Sigui \(x\) = longitud, \(y\) mplada. Llavors	  tenim el següent sistema. 
val10=I la solució d'aquest sistema és
!endif
!if $val6=es 
val7=We have a rectangle whose perimeter is,	  mm. Given that the length is ,	  mm longer than the width, find out the length and the width (in mm).
val8=Length,Width
val9=Let \(x\)  the length, \(y\) the width. Then	  we have the following system.
val10=And the solution of this system is
!endif
!if $val6=nl 
val7=We hebben een rechthoek met een omtrek van,	  mm. Gegeven :  de lengte is ,	  mm langer dan de breedte, bereken nu de lengte en de breedte (in mm).
val8=Lengte,Breedte
val9=Neem \(x\) voor de lengte, \(y\) voor de breedte. Dan	  hebben we het volgende.
val10=En de oplossing van dit systeem is
!endif
tmp0=!randint 20, 50

val11=$[rint($(tmp0))]
tmp0=!randint 20, 50

val12=$[rint($(tmp0))]
val13=$[rint($val11+$val12)]
val14=$[rint(2*($val13+$val11))]
replytype1=default
replyname1=$(val8[1])
replygood1=$val13
replytype2=default
replyname2=$(val8[2])
replygood2=$val11
question=__EXECUTED_1692754_EXERCISE__
solution=__EXECUTED_1692754_EXERCISE__

!goto stat

!exit

:question
$()$(val7[1]) $val14 $(val7[2]) $val12	  $(val7[3]), $(val7[4])	
$()
!exit

:hint
$()
$()
!exit

:help
$()
$()
!exit

:solution
$()$val9	<div class="wimscenter">	  
!insmath \left\lbrace \begin{array}{ccccc}	 2x &+& 2y &=& $val14\\\  x &-& y &=& $val12\end{array}\right .
$()		</div>	$val10 
!insmath x = $val13
$(), 
!insmath y = $val11
$().	
$()
!exit

:latex
$()
$()
!exit

:feedback

!exit

:test

!exit

:stat
vsavelist=7,9,10,11,12,13,14
embedcnt=0
