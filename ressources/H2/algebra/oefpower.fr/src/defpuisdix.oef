%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/defpuisdix.cpp.

\text{lang=slib(oef/env lang)}
\title{Définition puissance de 10}
\title_en{Definition of a power of 10}
\title_es{Definición potencia de 10}
\title_it{Definzione di potenza di 10}
\title_nl{Van Macht naar Decimaal getal}
\author{Jean-Baptiste, Frondas}
\email{ambali@free.fr}
\text{name_statement=Déterminer la valeur du nombre }
\text{name_instruction=On écrira la réponse sous la forme d'un nombre entier ou décimal.}
\if{\lang=en}{
\text{name_statement=Determine the value of the number }
\text{name_instruction=You will write the answer as an integer or decimal number.}
}
\if{\lang=es}{
\text{name_statement=Determinar el valor del número}
\text{name_instruction=On écrira la réponse sous la forme d'un nombre entier ou décimal.}
}
\if{\lang=it}{
\text{name_statement=Scrivi il valore del numero }
\text{name_instruction=Scrivi il numero come numero intero oppore come numero decimale.}
}
\if{\lang=nl}{
\text{name_statement=Schrijf als een decimaal getal }
\text{name_instruction=You will write the answer as an integer or decimal number.}
}
\integer{ a1=randint(1..10)}
\integer{ b1=randint(1..10)*(-1)}
\integer{ b2=((-1*\b1)-1)}
\text{ result1=pari( (10)^(\a1))}
\text{result1=\result1}
\text{ result2=wims(makelist 0 for i = 1 to \b2)}
\text{ result2=wims(items2words \result2)}
\text{ result2=wims(nospace 0, \result2 1)}
\integer{expos = randint(\a1,\b1)}
\if{\expos=\a1}{\text{result=\result1}}
\if{\expos=\b1}{\text{result=\result2}}
\statement{
\name_statement \(A = 10^{\expos}\).
<div class="wimscenter">
<label for="reply1">\(A = \)</label> \embed{reply1,11}.
</div>
<div class="wims_instruction">\name_instruction</div>
}
\answer{\(A\) }{\result}{type=case}
