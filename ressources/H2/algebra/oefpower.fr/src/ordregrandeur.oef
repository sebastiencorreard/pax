%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/ordregrandeur.cpp.

\text{lang=slib(oef/env lang)}
\title{Ordre de grandeur}
\title_en{Order of magnitude}
\title_es{Orden de magnitud}
\title_it{Ordine di grandezza}
\title_nl{Orde grootte}
\author{Jean-Baptiste, Frondas}
\email{ambali@free.fr}
\text{name_statement=Déterminer un ordre de grandeur du nombre }
\text{name_instruction=Si besoin est, l'exposant de la puissance de 10 peut être zéro.}
\if{\lang=en}{
\text{name_statement=Determine an order of magnitude for the number}
\text{name_instruction=If necessary, the exponent in the power of 10 can be zero.}
}
\if{\lang=es}{
\text{name_statementDeterminar un orden de magnitud del número A siguiente,
  como eso se mostró en el curso}
\text{name_instruction=Si es necesario, el exponente de la potencia puede ser cero. }
}
\if{\lang=it}{
\text{name_statement=Determina l'ordine di grandezza del numero \(A\) dato.}
\text{name_instruction=Se necessario, l'esponente di 10 può essere zero.}
}
\if{\lang=nl}{
\text{name_statement=Bepaal de orde grootte van getal \(A) in:}
\text{name_instruction=Let op: de macht kan ook 0 worden.}
}
\integer{ n1 = random(3..6)}
\integer{ p = random(randint(5..7),randint(1..3))}
\real{ a = randint(10^(\n1)..10^(\n1 + 1)-1)}
\real{ b = \a/10^(\p)}
\real{ result =\a/10^(\n1)}
\real{ expos=\n1-\p}
\real{ exposa=\expos}
\integer{ pe=\result}
\if{\pe=10}{\real{ pe=1} \real{ exposa=\exposa+1}}{}
\statement{
  \name_statement \( A = \b\).
<div class="wimscenter">
\(A = \) \embed{ reply1 ,3}\( \times 10 \) <sup>\embed{reply2,3}</sup></div>
<div class="wims_instruction">
\name_instruction
</div>
}
\answer{}{\pe}
\answer{}{\exposa}
