%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/operation2.cpp.

\text{lang=slib(oef/env lang)}
\title{Opération 2}
\title_en{Operation 2}
\title_es{Operación 2}
\title_it{Operazioni 2}
\title_nl{Bewerkingen 2}
\author{Jean-Baptiste, Frondas}
\email{ambali@free.fr}
\computeanswer{no}
\text{name_statement=Compléter l'égalité afin qu'elle soit vérifiée&nbsp;}
\text{name_instruction=Donner le résultat sous la forme}
\if{\lang=en}{
\text{name_statement=Complete the equality so that it becomes true}
\text{name_instruction=Give the result in the form}
}
\if{\lang=es}{
\text{name_statement=Completar la igualdad para que esté comprobada}
\text{name_instruction=Dar el resultado en la forma}
}
\if{\lang=it}{
\text{name_statement=Completa l'eguaguaglianza}
\text{name_instruction=Scrivi il risultato nella forma}
}
\if{\lang=nl}{
\text{name_statement=Reken uit en schrijf in de vorm}
\text{name_instruction=Geef het resultaat in de vorm}
}
\integer{p1=random(1..20)*randint(-1,1)}
\integer{q1=random(1..20)*randint(-1,1)}
\integer{pp1=random(1..20)*randint(-1,1)}
\integer{qq1=random(1..20)*randint(-1,1)}
\rational{ a1=\p1 / \q1}
\rational{ b1=\pp1 / \qq1}
\integer{ n1=random(1..10)*randint(-1,1)}
\rational{ quotient = pari( (\a1) / (\b1))}
\real{bon=pari((\quotient)^(\n1))}
\statement{
\name_statement:
<div class="wimscenter">
 \( (\a1)^{\n1} \div (\b1)^{\n1} = \) \embed{ reply1 ,8}<sup>\embed{reply2,3}</sup>
</div>
<div class="wims_instruction">\name_instruction \(a^p\).</div>
}
\answer{}{\rep1}
\answer{}{\rep2}
\real{H=pari((\rep1)^(\rep2))}
\condition{}{(\rep1=\quotient or \rep1=-1*\quotient or \rep1=1/\quotient or \rep1=-1/\quotient) and (\rep2=\n1 or \rep2=-1*\n1) and (\H =\bon)}
