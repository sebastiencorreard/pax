%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/defecritscien.cpp.

\text{lang=slib(oef/env lang)}
\title{Définition écriture scientifique}
\title_en{Definition of scientific notation}
\title_es{Definición escritura científica}
\title_it{Definizione notazione scientifica}
\title_nl{De wetenschappelijke notatie}
\author{Jean-Baptiste, Frondas}
\email{ambali@free.fr}
\text{name_statement=Donner l'écriture scientifique du nombre}
\if{\lang=en}{
\text{name_statement=Write the following number \(A\) using scientific notation:}
}
\if{\lang=es}{
\text{name_statement=Dar la escritura científica del número}
}
\if{\lang=it}{
\text{name_statement=Scrivi il numero dato utilizzando la notazione scientifica:}
}
\if{\lang=nl}{
\text{name_statement=Geef de wetenschappelijke schrijfwijze van \(A\) in}
}
\integer{ n1 = randint(3..6)}
\integer{ p = randitem(randint(5..7),randint(1..3))}
\real{ a = randint(10^(\n1)..10^(\n1 + 1)-1)}
\real{ b = \a/10^(\p)}
\real{ result =\a/10^(\n1)}
\real{ expos=\n1-\p}
\statement{
\name_statement \( A = \b\):
<div class="wimscenter">
 <label for="reply1">\(A = \)</label>
  \embed{ reply1 ,8} <label for="reply2">\(\times 10\)</label>
  <sup>\embed{reply2,3}</sup>
 </div>
}
\answer{}{\result}
\answer{}{\expos}
\latex{
\begin{statement}
  \name_statement \( A = \b\):
\begin{center}
 \(A = \cdots \times 10^{\cdots}\)
\end{center}
\end{statement}
}
