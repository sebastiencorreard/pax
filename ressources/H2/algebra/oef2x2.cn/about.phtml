!set email=!trim $email
!set author=!trim $author

!if $author!=$empty
本练习的作者&nbsp;:
 !if $email!=$empty
  <a href="mailto:$email">$author</a>.
 !else
  $author.
 !endif
!endif
!if $solution!=$empty and $hint!=$empty
本练习既包含带有解释的解答, 也包含提示. 
!endif
!if $solution!=$empty and $hint=$empty
本练习包含带有解释的解答, 但没有提示. 
!endif
!if $solution=$empty and $hint!=$empty
本练习包含提示, 但不含带有解释的解答. 
!endif
!if $solution$hint=$empty
本练习既无带有解释的解答, 也无提示. 
!endif
<p>
关于这组练习的信息. <p>
$module_about
<p>
练习的版权属于各作者. 把练习的内容安放在网站上需得到作者的书面授权.

!tail

