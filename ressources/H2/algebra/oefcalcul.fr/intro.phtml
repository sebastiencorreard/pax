!header
!read Exindex
!if $lang=en
  This module actually contains $exototal exercises on  calculation
  (addition of fractions, product of signed numbers, rules of priority).
  !goto tail
!endif
!if $lang=es
  Este módulo agrupa por el momento $exototal ejercicios sobre el cálculo (adición de fracciones,
  producto de números relativos, reglas de prioridad).
  !goto tail
!endif
Ce module regroupe pour l'instant $exototal exercices sur le calcul
  (addition de fractions, produit de nombres relatifs, règles de priorité).

:tail
!set latex_exists=yes
!read oef/intromenu.phtml
!tail
