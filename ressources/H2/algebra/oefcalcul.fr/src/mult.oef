%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/mult.cpp.

\text{lang=slib(oef/env lang)}
\title{Multiplication d'entiers relatifs }
\title_en{Multiplication of signed integers }
\title_es{Multiplicación de enteros relativos }
\title_nl{Vermenigvuldiging van negatieve getallen }
\author{Bernadette, Perrin-Riou}
\email{bernadette.m.riou@orange.fr}
\language{fr}
\range{-5..5}
\computeanswer{no}
\format{html}
\text{name_statement=Effectue le calcul suivant&nbsp;}
\if{\lang=en}{
\text{name_statement=Calculate}
}
\if{\lang=es}{
\text{name_statement=Efectúa el cálculo siguiente}
}
\if{\lang=nl}{
\text{name_statement=Bereken}
}
\precision{10000}
\integer{n=random(2..10)*random(1,-1)}
\text{n=\n >0 ? +\n}
\integer{m=random(2..10)*random(1,-1)}
\text{m=\m >0 ? +\m}
\integer{p=random(2..10)*random(1,-1)}
\text{p=\p >0 ? +\p}
\integer{sol= \n*(\m)*(\p)}
\text{sol = \sol>0 ? +\sol}
\statement{<p>\name_statement: </p>
<div class="wimscenter"><label for="reply1">
\((\n)\times (\m)\times (\p)\)</label> = \embed{reply 1, 12}
</div>}
\answer{}{\sol}{type=numeric}
\latex{
\begin{statement}
\name_statement:
\begin{center}
\((\n)\times (\m)\times (\p)\) = \(\dots\dots\)
\end{center}
\end{statement}
}
