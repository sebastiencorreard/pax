\title{De wetenschappelijke notatie}
\language{nl} 
\email{ambali@free.fr}
\integer{ n1 = random(3..6)}
\integer{ p = random(randint(5..7),randint(1..3))}
\real{ a = randint(10^(\n1)..10^(\n1 + 1)-1)}
\real{ b = \a/10^(\p)}	
\real{ result =\a/10^(\n1)}
\real{ expos=\n1-\p}
\statement{<p style="text-align : center;
background-color : #FFFFFF;
padding : 1%;
margin : 5% 15%; ">
Geef de wetenschappelijke schrijfwijze van \(A) in : \( A = \b)<br/>
 \(A = ) \embed{ reply1 ,8} \(\times 10)<sup>\embed{reply2,3}</sup>
</p>
}
\answer{}{\result}
\answer{}{\expos}
