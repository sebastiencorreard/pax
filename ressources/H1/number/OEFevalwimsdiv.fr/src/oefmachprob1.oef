%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/oefmachprob.cpp.

\keywords{division,pbsolving}
\langage{fr}
\author{Jean-Luc,Donadoni}
\email{jluc.donadoni@laposte.net}
\format{html}
\title{Problèmes avec utilisation de la calculatrice 1}
\text{name_question=Trouver la solution du problème en utilisant la calculatrice&nbsp;}
\text{name_cond=Bonne réponse&nbsp;}
\text{name_sol=\(\v1 \minus \v2=\res\)<p>Il lui reste à payer \(\res\)&nbsp;euro.</p>}
\text{name_instr=La réponse sera donnée sans espace.}
\css{
  <style>
.wrap {
  display: flex;
  flex-wrap: wrap;
  align-items: flex-start;
  gap: 1rem;
}
.bloc {
  flex: 0 0 auto;
  width: max-content;
   max-width: 100%;
  border: none;
  padding: 1rem;
}
.divposee {
  width: auto;
  border-collapse: collapse;
  font-size: large;
  display: table;
  table-layout: auto;
}
.divposee td,.divposee th {
  padding: .45em .7em;
  text-align: center;
  vertical-align: middle;
  }
.top{vertical-align: top }
.vline-left {
  border-left: 1px solid #000;
}
.vline-right {
  border-right: 1px solid #000;
}
.bord-bas {
  border-bottom: 1px solid #000;
}
.border-bottom {
  border-bottom:2px solid;
    }
.border-thin {
      border-bottom:1px solid gray;
    }
.border-right {
      border-right:2px solid;
    }
.pre {
      font-family:monospace;
      font-size:20px;
    }
.espace-ligne {
      line-height: 2.5;
}
.mark_label{
    margin-left:1.2em;
    margin-right:1.2em;
}
.inline {
  display: inline-block;
  margin: 1em;
}
.solution-wrap {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
  align-items: start;
}
.solution-bloc {
  min-width: 0;
}
.solution-bloc.tableau {
  text-align: center;
  font-size: 2em;
  line-height: 1.5;
}
.solution-bloc.tableau > table{
  display: table;
}
.solution-bloc.tableau table.divposee {
  width: 75%;
  max-width: none;
  table-layout: auto;
  text-align: center;
  border-collapse: collapse;
}
.solution-bloc.tableau table.divposee td,
.solution-bloc.tableau table.divposee th {
  padding: .45em .7em;
  text-align: center;
  vertical-align: middle;
  box-sizing: border-box;
  background:#fff;
}
.solution-bloc.tableau table.divposee td.sgn-cell,
.solution-bloc.tableau table.divposee td.sgn-cell {
  text-align: right;
  vertical-align: middle;
  padding: 0em;
}
.centre{
  text-align: center;
}
.mul-line.navy-hl {
    font-weight: 600;
}
.blue{color:#0b63ff;
  background:#fff;
}
.red{color:#d60000;
  background:#fff;
}
.black{color:#000;
  background:#fff;
}
.ghost{
    visibility: hidden;
    border-bottom: none ;
    background:#fff;
}
.green{
  color:#1a7f37;
  font-weight:700;
}
.match-green{
  color:#1a7f37;
  font-weight:700;
  background:#eaf7ea;
}
.mul-line.match-green{
  color:#1a7f37;
}
.stmt-scroll {
  position:relative;
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  max-width: 100%;
}
.stmt-scroll td,
.stmt-scroll th {
  position: relative;
}
.stmt-scroll td embed,
.stmt-scroll td object,
.stmt-scroll td iframe,
.stmt-scroll td img,
.stmt-scroll td svg {
  position: static;
  display: inline-block;
  max-width: none;
  vertical-align: middle;
}
.stmt-row {
  display: flex;
  align-items: flex-start;
  gap: .5rem;
}
.col-left {
  flex: 0 0 auto;
  overflow-x: visible;
  -webkit-overflow-scrolling: touch;
}
.col-left > table {
  width: max-content;
  table-layout: auto;
  border-collapse: collapse;
  white-space: nowrap;
}
.col-right {
  flex: 0 1 auto;
  min-width: 16rem;
  text-align:center;
}
.col-right .centre {
  display: inline-block;
  text-align: center;
  width: auto;
  max-width: min(42ch, 100%);
}
.stmt-scroll {
  display: block;
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  max-width: 100%;
}
.stmt-scroll > table.divposee {
  display: inline-block;
  width: max-content;
  max-width: none;
  table-layout: auto;
  border-collapse: collapse;
  white-space: nowrap;
}
.stmt-mult {
  flex: 0 0 auto;
  min-width: 10ch;
  max-width: 40ch;
}
.stmt-mult .mul-line {
  margin: 0 0 .25em 0;
  line-height: 1.2;
  white-space: nowrap;
}
 /**** div avec liserés de couleur et background ****/
.euler_quest_nobg {
    border-left:0.3em solid blue;
    border-radius:3px;
    padding:0.3em 0.3em 0.3em 0.5em;
    margin-top: 0.3em;
    margin-bottom: 0.3em;
}
.euler_rep_good_nobg {
    border-left:0.3em solid green;
    border-radius:3px;
    padding:0.3em 0.3em 0.3em 0.5em;
    margin-top: 0.3em;
    margin-bottom: 0.3em;
    margin-left:0.3em;
}
.euler_answer {
    border-left:0.3em solid grey;
    border-radius:3px;
    background-color:#f0f0f0;
    padding:0.3em 0.3em 0.3em 0.5em;
    margin-top: 0.3em;
    margin-bottom: 0.3em;
    margin-left:0.3em;
    -moz-box-shadow: 6px 6px 12px #888; -webkit-box-shadow: 6px 6px 12px #888; box-shadow:6px 6px 12px #888;
}
.euler_answer_nobg {
    border-left:0.3em solid grey;
    border-radius:3px;
    padding:0.3em 0.3em 0.3em 0.5em;
    margin-top: 0.3em;
    margin-bottom: 0.3em;
    margin-left:0.3em;
}
.euler_rep_bad_nobg {
    border-left:0.3em solid red;
    border-radius:3px;
    padding:0.3em 0.3em 0.3em 0.5em;
    margin-top: 0.3em;
    margin-bottom: 0.3em;
    margin-left:0.3em;
}
 .tick::before{
  content : "\002713";
  color:#008000;
  font-weight:bold;
  font-size:1.3em;
  margin-left:0.4em;
  margin-top:0;
  margin-right:0.4em;
}
.cross::before{
  content : "\002717";
  color:#b50000;
  font-weight:bold;
  font-size:1.3em;
  margin-left:0.4em;
  margin-top:0;
  margin-right:0.4em;
}
div.tick::before {
  font-size:2em;
  position: absolute;
  left: 0;
  top: 0;
  display: grid;
  place-items: center;
  box-sizing:border-box;
}
div.cross::before {
  font-size:2em;
  position: absolute;
  left: 0;
  top: 0;
  display: grid;
  place-items: center;
  box-sizing:border-box;
}
div.tick,div.cross {
  position: relative;
  background-size: 100% 1px;
  background-repeat: no-repeat;
  background-position: 0 0;
  overflow: hidden;
  padding-left:2.5em;
}
  </style>
}
\precision(1000000)
\integer{a=randint(1..2)}
\integer{b=randint(1..2)}
\text{sex=boy,girl}
\text{z=0}
\text{nom1=slib(lang/fname fr,\sex[\a])}
\text{nom2=slib(lang/fname fr,\sex[\b])}
\text{nom3=slib(lang/fname fr,girl)}
\if{\a=1}{\text{ar1=il}}{\text{ar1=elle}}
\if{\a=1}{\text{art1=Il}}{\text{art1=Elle}}
\if{\b=1}{\text{ar2=il}}{\text{ar2=elle}}
\if{\b=1}{\text{art2=Il}}{\text{art2=Elle}}
\text{\text{ar3=elle}}
\text{\text{art3=Elle}}
\text{tvres=}
\text{listeuro=1,3,5,6,8,9,10,11,12,15,16,17,21,22,24}
\integer{ch=randint(1..4)}
\matrix{prob=wims(record \ch of src/problemes)}
\text{enonc=row(2,\prob)}
\text{question=row(3,\prob)}
\text{d1=row(4,\prob)}
\text{d2=row(5,\prob)}
\integer{v1=\d1[1]+\d1[2]*randint(1..\d1[3])}
\integer{v2=\d2[1]+\d2[2]*randint(1..\d2[3])}
\text{u=\d1[4]}
\text{op=row(6,\prob)}
\if{\op[1]=/}{\integer{res=\v2}
              \integer{v2=\v1 * \v2}}
               {\integer{res=abs(\v1 \op[1] \v2)}}
\text{v1=slib(numeration/ecriturenombre \v1,comma tex)}
\text{v2=slib(numeration/ecriturenombre \v2,comma tex)}
\text{enonc=wims(replace internal v1 by \(\v1\) in \enonc)}
\text{enonc=wims(replace internal v2 by \(\v2\) in \enonc)}
\integer{l=10}
\real{vres=\res}
\text{enonc=wims(replace internal nom1 by \nom1 in \enonc)}
\text{enonc=wims(replace internal nom2 by \nom2 in \enonc)}
\text{enonc=wims(replace internal nom3 by \nom3 in \enonc)}
\text{enonc=wims(replace internal ar1 by \ar1 in \enonc)}
\text{enonc=wims(replace internal art1 by \art1 in \enonc)}
\text{question=wims(replace internal nom1 by \nom1 in \question)}
\text{question=wims(replace internal nom2 by \nom2 in \question)}
\text{question=wims(replace internal nom3 by \nom3 in \question)}
\text{question=wims(replace internal ar1 by \ar1 in \question)}
\text{question=wims(replace internal art1 by \art1 in \question)}
\if{\tvres=}{\text{tvres=\vres}}
\text{tvres=slib(numeration/ecriturenombre \vres,comma tex)}
\if{\ch isin \listeuro}{\real{testres=abs(pari(\vres-floor(\vres)))}
                        \if{\testres>0}{\text{tres=wims(exec moneyprint \res)}}
                                        {\text{tres=\res}}
                       }{
                       \text{tres=\res}
                       }
\text{tres=slib(numeration/ecriturenombre \tres,comma tex)}
\text{reply_class=euler_quest_nobg}
\if{\ch=1}{\text{name_sol=\(\v1 \minus \v2=\tvres\)<p>Il lui reste <span class="nowrap">\(\tres\) €</span> à payer.</p>}
           \text{name_ans=Il lui reste à payer}
          }
\if{\ch=2}{\text{name_sol=\(\v1 \times \v2=\tvres\)<p>Il y a \(\tres\) cartons.</p>}
           \text{name_ans=Il y a}
          }
\if{\ch=3}{\text{name_sol=\(\v2 \div \v1=\tvres\)<p>Chacun aura <span class="nowrap">\(\tres\) €</span>.</p>}
           \text{name_ans=Chacun aura}
          }
\if{\ch=4}{\text{name_sol=\(\v2 \div \v1=\tvres\)<p>Chaque bouquet sera composé de \(\tres\) roses.</p>}
          \text{name_ans=Chaque bouquet est composé de}
          }
\statement{
\enonc
<div class="\reply_class">
    <div class="spacer">\question</div>
    <label for="reply1">\name_ans</label> \embed{r1,\l} \u. </div>
}
\answer{}{\repel}
\text{repel=wims(replace internal * by in \repel)}
\text{repel=wims(replace internal , by . in \repel)}
\real{repel=pari(\repel)}
\condition{\name_cond}{\repel=\res}
\if{\repel=\res}{\text{reply_class=euler_rep_good_nobg tick}}{\text{reply_class=euler_rep_bad_nobg cross"}}
\solution{\name_sol}
