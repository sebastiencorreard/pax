%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/eclipse.cpp.

\author{Bernadette, Perrin-Riou}
\email{bernadette.m.riou@orange.fr}



\title{Éclipse (sans aide graphique) }

\text{name_yesno=oui,non}
\text{name_none=aucune}
\text{name_hint=Pour s'aider, il est possible de tracer un segment.
  Il n'en sera pas tenu compte dans la notation.}

\text{name_choix1=une éclipse de soleil, une éclipse de Lune,ni l'un ni l'autre}
\text{name_question1=Ce schéma représente}
\text{name_question2=Elle est}
\text{name_question3=Dans la zone verte de la Terre, le Soleil est}
\matrix{name_choix2=entièrement masqué,partiellement masqué,vu comme un anneau autour de la Lune
  totale,partielle,pénombrale}
\text{name_warning=Le dessin n'est pas à l'échelle et la situation
 a été simplifiée.}

Si la lune passe entre le soleil et la terre, c'est une éclipse de soleil:
le soleil est caché par la lune. L'ombre de la lune sur la terre donne l'impression
qu'il fait nuit en plein jour.
Si la lune traverse l'ombre de la terre, c'est une éclipse de lune;
la lune n'est plus éclairée par le soleil, elle semble disparaitre.
distance soleil-terre=23400*rT
distance terre-lune=60*rT en moyenne
rayon soleil=109*rT
rayon lune=0.27*rT
cone d'ombre: longueur= TS*rT/(rS-rT)
\text{choix=randitem(1,2)}
\text{STEP=r1}
\text{choixe=\choix}
\if{\choix=1}{

utiliser dans eclipse.cpp
\text{range=35}
\text{rS=randint(17..20)}
\text{rL=randint(4..6)}
\text{L=randint(100..150)}
\real{rT=4*\rL}
\text{La=\L,0}

\text{tangente=slib(tangente \S,0,\rS,\La,\rL)}
\text{data=\tangente[1..4]}
\text{tangente=\tangente[5]}
\text{dessin_tangente=wims(declosing \tangente)}

\text{listT=\data[4]+2*\rT,\data[4]+3*\rT}
\if{\data[4]-\L>\rT+2*\rL}{
  \text{listT=\listT, \data[4]+\rT/2}
}
\text{cnt=items(\listT)}
\integer{T1=randint(1..\cnt)}
\real{T=\listT[\T1]}
\integer{l=ceil((\T+\rT+\rS)/(2*\rS))}

\text{ch=randitem(1,2,2,2,3)}
\text{bary=randitem(0.3,0.5,0.7)}

\real{sl1=\data[1]}
\real{Z1=\data[3]}
\real{sl2=\data[2]}
\real{Z2=\data[4]}
\real{disc=(\T+(\sl1)^2*\Z1)^2-(\T^2+(\sl1)^2*\Z1^2-\rT^2)*((\sl1)^2+1)}
\real{lim1=(\T+(\sl1)^2*\Z1-\disc^(1/2))/((\sl1)^2+1)}
\text{dessin_tangente=wims(replace internal XXXXXX1 by \lim1 in \dessin_tangente)}
\real{disc=(\T+(\sl2)^2*\Z2)^2-(\T^2+(\sl2)^2*\Z2^2-\rT^2)*((\sl2)^2+1)}
\real{lim2=(\T+(\sl2)^2*\Z2-\disc^(1/2))/((\sl2)^2+1)}
\text{dessin_tangente=wims(replace internal XXXXXX2 by \lim2 in \dessin_tangente)}

\text{listangle=pari(B=[arccos((\T-\lim1)/\rT),arccos((\T-\lim2)/\rT)];concat(-B,B))}
\text{listangle=wims(sort numeric item \listangle)}
\text{listarc=arc \T,0,2*\rT,2*\rT,pari([\listangle[1]+pi,\listangle[2]+pi]*180/pi),green
arc \T,0,2*\rT,2*\rT,pari([\listangle[2]+pi,\listangle[3]+pi]*180/pi),green
arc \T,0,2*\rT,2*\rT,pari([\listangle[3]+pi,\listangle[4]+pi]*180/pi),green}
\text{dessin_zone=linewidth 5
row(\ch,\listarc)}
\text{the=\listangle[\ch]}
\real{the=\bary*\listangle[\ch]+(1-\bary)*\listangle[\ch+1]}
\text{P=pari([\T-\rT*cos(\the),-\rT*sin(\the)])}
\text{LP2=pari(norml2([\P]-[\L,0]))}
\real{limx1=0}
\real{limx2=0}
\ifval{abs(\P[2])>10^-5}{
  \text{t1=pari(sqrt(\LP2-\rL^2)/\rL)}
  \text{t2=\P[2]!=0?(\P[1]-\L)/\P[2]:0}
  \real{slx1=pari((\t1-\t2)/(1+\t1*\t2))}
  \real{slx2=(\t1+\t2)/(1-\t1*\t2)}
  \real{slx2=-\slx2}
  \real{Zx1=\P[1]-(\P[2])/\slx1}
  \real{discx=\rS^2*(1+(\slx1)^2)-(\slx1*\Zx1)^2}
  \if{\discx<0}{
    \real{limx1=0}
  }{
    \real{limx1=((\slx1)^2*\Zx1+(\discx)^(1/2))/((\slx1)^2+1)}
  }
  \real{Zx2=\P[1]-(\P[2])/\slx2}
  \real{discx2=\rS^2*(1+(\slx2)^2)-(\slx2*\Zx2)^2}
  \if{\discx2<0}{
    \real{limx2=0}
  }{
    \real{limx2=((\slx2)^2*\Zx2+(\discx2)^(1/2))/((\slx2)^2+1)}
  }
}{
  \real{slx1=\rL/((\P[1]-\L)^2-\rL^2)^(1/2)}
  \real{slx2=-\slx1}
  \real{Zx1=\P[1]}
  \real{discx=\rS^2*(1+(\slx1)^2)-(\slx1*\Zx1)^2}
  \if{\discx<0}{
    \real{limx1=0}
  }{
    \text{limx1=((\slx1)^2*\Zx1+(\discx)^(1/2))/((\slx1)^2+1)}
  }
  \real{limx2=\limx1}
}
\text{PS1=pari([\limx1,\slx1 *(\limx1-\P[1])+\P[2]])}
\text{PS2=pari([\limx2,\slx2 *(\limx2-\P[1])+\P[2]])}

\text{dessin_coneombre=segments black, \P,\PS1
  segments black, \P,\PS2}

\text{choixp=\ch iswordof 1 3? 2}
\if{\ch=2 and \T> \data[4]+\rT}{
  \text{choixp=3}
}
\text{choixp=\ch=2 and \T<=\data[4]+\rT ? 1}

}
\if{\choix=2}{

utiise dans eclipse
\text{range=30}
\text{l=5}
\text{rS=20}
\real{the=randint(80..83)*pi/180}
\real{A=\rS/cos(\the)} extremite du cone
\text{n=0.6,0.8}
\real{T=\A*\n[1]}
\real{L=\A*\n[2]}
\real{rT=\rS*(1-\T/\A)}
\real{rL=\rT/4}
\real{dist=\L-\T}
\text{tangente=slib(tangente \S,0,\rS,\T,0,\rT)}
\text{data=\tangente[1..4]}
\text{tangente=\tangente[5]}
\text{dessin_tangente=wims(declosing \tangente)}
\text{dessin_tangente=wims(replace internal XXXXXX1 by 1000 in \dessin_tangente)}
\text{dessin_tangente=wims(replace internal XXXXXX2 by 1000 in \dessin_tangente)}
\text{sl1=\data[1]}
\text{sl2=\data[2]}
\text{Z1=\data[3]}
\text{Z2=\data[4]}
real{limite1=(\sl1)^2/(1+(\sl1)^2)*\Z1}
real{limite2=(\sl2)^2/(1+(\sl2)^2)*\Z2}

\text{ang3=pari([pi-asin(\rT/\dist)-atan(\sl1),\the+pi/2,
  -\the-pi/2,-pi+asin(\rT/\dist)+atan(\sl1)])}
\text{angmil=pari(ang=[\ang3]; [ang[2]-pi/4,(ang[1]+ang[2])/2,pi+(ang[4]+ang[1])/2,2*pi-(ang[1]+ang[2])/2,2*pi-(ang[1]+ang[2])/2+pi/6])}
\text{ang=\angmil,\ang3[1],\ang3[2],-\ang3[1],-\ang3[2]}
\text{ang=\ang[1,6,2,7,3,9,4,8,5]}
text{ang=\ang[1,3,5,7,9]}

1 rien
3 pénombrale
4 partielle
5 totale
totale, partielle, pénombrale

\integer{angcnt=items(\ang)}
\text{orbite=}
\text{verif=}
\for{i=1 to \angcnt}{
  \text{ang2=\ang[\i]}
  \text{orbite=\orbite,\T-cos(\ang2)*\dist,sin(\ang2)*\dist,\rL}
  \text{verif=\verif
  text black,\T-cos(\ang2)*\dist,sin(\ang2)*\dist,small,\i}
}

\text{choixang=randitem(1,3,4,5,6,7,9)}
\text{La=\T-cos(\ang[\choixang])*\dist,sin(\ang[\choixang])*\dist}
\text{dessin_circles=fcircles yellow,0,0,\rS
fcircles blue,\La,\rL
circles red,\T,0,\rT}

\text{choixe=\choixang iswordof 1 9?3:2}
\text{choixp=\choixang=5?1}
\text{choixp=\choixang iswordof 3 7 ?3}
\text{choixp=\choixang iswordof 4 6 ?2}
\text{choixp=\choixang iswordof 5 ?1}


}
\text{dessin_circles=fcircles yellow,0,0,\rS
fcircles lightblue,\La,\rL
circles red,\T,0,\rT}
\text{size=pari([(\l+1)/2*400,400])}
\text{dessin_prel=xrange -\range,\l*\range
yrange -\range,\range
hline 0,0,black
text black,0,0,giant,S
text black,\La,giant, L
text black,\T,0,giant, T}
\if{\choix=1}{
  \text{tmp1=\slx1*(\limx1-(\P[1]))+(\P[2])>0?acos(\limx1/\rS):-acos(\limx1/\rS)}
  \text{tmp2=\slx2*(\limx2-(\P[1]))+(\P[2])>0?acos(\limx2/\rS):-acos(\limx2/\rS)}
  \text{tmp=\tmp1>\tmp2 ?pari(180/pi*[\tmp2,\tmp1]): pari(180/pi*[\tmp1,\tmp2])}
  \text{dessin_hidden=arc 0,0,2*\rS,2*\rS,\tmp,black}
  \text{dessinout1=\dessin_prel
\dessin_circles}
  \text{dessinout10=\dessinout1
\dessin_tangente}
  \text{dessinout2=\dessin_prel
\dessin_circles
linewidth 2
\dessin_zone}
\text{dessinout20=\dessinout2
\dessin_coneombre
  linewidth 5
\dessin_hidden}
}
\if{\choix=2}{
  \text{dessinout1=\dessin_prel
  \dessin_circles}
\text{dessinout10=\dessinout1
  \dessin_tangente}
  circles red \orbite
\text{dessinout2=\dessinout1}
\text{dessinout20=\dessinout10}
}
\text{name_choix2=\name_choix2[\choix;]}
\text{STEP=\choixe!=3? \STEP
  r2}
\steps{\STEP}
\statement{
\if{\step=1}{<div class="wims_question">
\name_question1
<ul><li>\embed{r1,1}</li>
<li>\embed{r1,2}</li>
<li>\embed{r1,3}</li>
</ul>
</div>
}
\if{\step>=2}{
  <p>\name_question1 \name_choix1[\choixe].</p>
  <div class="wims_question">
\if{\choix=1}{\name_question3}{\name_question2}
<ul>
<li>\embed{r2,1}</li>
<li>\embed{r2,2}</li>
<li>\embed{r2,3}</li>
</ul>
</div>}
<div class="wimscenter">

\if{\step=1}{
  \draw{\size}{\dessinout1}}{
  \draw{\size}{\dessinout2}}

</div>
<div class="wims_warning">\name_warning</div>
}
\answer{}{\choixe;\name_choix1}{type=radio}
\answer{}{\choixp;\name_choix2}{type=radio}

\feedback{1=1}{
\draw{\size}{\dessinout20}
}

\draw{\size}{\dessin
\dessincircle
\dessin1
\dessin3}

