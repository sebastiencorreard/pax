%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/ecran.cpp.

\author{Bernadette, Perrin-Riou}
\email{bernadette.m.riou@orange.fr}



\title{Écran 1}

\text{name_yesno=oui,non}
\text{name_none=aucune}
\text{name_hint=Pour s'aider, il est possible de tracer un segment.
  Il n'en sera pas tenu compte dans la notation.}

\text{name_enonce=L'observateur (B) peut-il voir la source lumineuse
  (point S) malgré l'écran opaque E}

\text{range=10}
\text{S=0,10}
\text{ecran=randint(3..4)}
\text{trou=pari([\ecran-0.5,\ecran+2.5])}

\text{n=1}
\text{m=1}
\text{List=S}
\text{list=B}

\text{b=wims(makelist 2*x for x=1 to \range/2-1)}
\text{b=shuffle(\b)}
\text{obs=\b[1..\n]} abscisse range
\text{obj=\b[-\m..-1]} abscisse 0
\text{dessin=xrange -2,\range+2
yrange -2,12
frectangle -1,-1,\range+1,11,lightgrey}
\text{mid=5}
\text{dessin=\dessin
linewidth 6
segments black, \mid,0,\mid,\trou[1],\mid,\trou[2],\mid,10
string black,\mid,\range+1,E
linewidth 2}
\for{j=1 to \n}{
  \text{dessin=\dessin
crosshair \range,\obs[\j],orange
string black,\range+0.5,\obs[\j],\List[\j]}
}
\for{i=1 to \m}{
  \text{dessin=\dessin
crosshair 0,\obj[\i],blue
string black,-2,\obj[\i],\list[\i]}
}
\text{RES=}
\text{cnt1=0}
\text{cnt2=0}
\for{i=1 to \m}{
  \text{res=\i}
  \for{j=1 to \n}{
    \real{test=\obj[\i]+(\obs[\j]-\obj[\i])/\range*\mid}
    \if{\test>\trou[1] and \test <\trou[2]}{
      \if{\cnt1=0}{
        \text{dessinarrows=linewidth 1
arrows blue,10}
      }
      \integer{cnt1=\cnt1+1}
      \text{res=\res,\j}
      \text{dessinarrows=\dessinarrows,\range,\obs[\j],0,\obj[\i]}
    }{
      \if{\cnt2=0}{
        \text{dessinarrows2=linewidth 1
arrows red,10}
      }
      \integer{cnt2=\cnt2+1}
      \text{dessinarrows2=\dessinarrows2,\range,\obs[\j],5,\test}
    }
  }
  \text{res=wims(nonempty items \res)}
  \matrix{RES=\RES
\res}
}

\text{rep=\RES[1;2]=1 ? 1:2}

\statement{
  \name_enonce?
  \canvasdraw{200,200}{
  \dessin
  userdraw segment,blue
  }
<div class="wims_instruction">\name_hint</div>
}

\answer{}{\rep;\name_yesno}{type=radio}

\feedback{1=1}{
\canvasdraw{200,200}{
\dessin
\dessinarrows
\dessinarrows2
}
}

