%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/eparall.cpp.

\language{fr}
\computeanswer{no}
\format{html}
\precision{10000}
\title{Equations de droites parallèles}
\text{ll=shuffle(6)}
\text{A=(\ll[1]+1)*random(1,-1)}
\text{B=(\ll[2]+1)*random(1,-1)}
\text{ff=shuffle(8)}
\text{f1=(\ff[1]+1)*random(1,-1)}
\text{f2=(\ff[2]+1)*random(1,-1)}
\integer{a=\ff[1]*\A}
\integer{b=-(\ff[1])*\B}
\integer{c=random(1..30)*random(1,-1)}
\integer{d=\ff[2]*\A}
\integer{e=\ff[2]*\B}
\text{coef=\a,\b,\d,\e}
\integer{rr=random(1..4)}
\text{r=\coef[\rr]}
\text{coef=wims(replace item number \rr by k in \coef)}
\statement{Trouver le coefficient \(k) pour que les deux droites suivantes soient
  parallèles :
<ul>
  <li>\(\coef[2]*y = \coef[1]*x + \c)</li>
  <li>\(\coef[3]*x + \coef[4]*y + \c = 0)</li>
</ul>
}
\reply{\(k)}{\r}{type=default}
