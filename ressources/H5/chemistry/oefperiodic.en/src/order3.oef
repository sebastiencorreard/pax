%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/order.cpp.

\language{en}
\author{XIAO Gang}
\email{xiao@unice.fr}
\format{html}

\text{data=wims(record 0 of table.\lang)}
\text{data=\data=? wims(record 0 of table)}
\integer{end=rows(\data)}
\integer{confparm1=\confparm1 notitemof 1,2,3,4?0}
\integer{confparm2=\confparm2 notitemof 1,2,3,4?0}
\text{empty=}
\integer{confparm3=\confparm3 issametext \empty or \confparm3<7 or
  \confparm3>\end?54}
\text{list=wims(values x for x=1 to \end)}
\text{confparm4=wims(listintersect \confparm4 and \list)}

\integer{given=\confparm1>0?\confparm1:randint(1..4)}
\integer{cp=\confparm2=\confparm1?0:\confparm2}
\text{remain=wims(listcomplement \given in 1,2,3,4)}
\text{remain=\candouble issametext yes and \given != 1?\given,\remain}
\integer{ask=\cp>0 and \cp isitemof \remain?\cp:randitem(\remain)}


\text{lang=slib(oef/env lang)}

\title{Order 3}
\title_fr{Ordre 3}
\title_nl{Volgorde 3}

\text{namelist=atomic numbers,symbols,names,atomic masses in \(u\)}
\text{name_instruction=Arrange the following atoms in the increasing order, by dragging them
  in to the empty boxes.}
\text{name_answer=Element}

\if{\lang=fr}{

\text{namelist=numéros atomiques,symboles,noms,masses atomiques en \(u\)}
\text{name_instruction=Rangez les atomes suivants dans l'ordre croissant des numéros atomiques, en les tirant
  dans les cases vides.}
\text{name_answer=Élément}

}
\if{\lang=nl}{

\text{namelist=atoom nummers,symbolen,namen,massas}
\text{name_instruction=Rangschik de volgende atomen in oplopende volgorde,
  door de plaatjes met je muis te lepen naar de juiste open plekken.}
\text{name_answer=Element}

}

\integer{cnt=3}

\text{Data=wims(record 0 of table.\lang)}
\text{Data=\Data=\empty? wims(record 0 of table)}

\text{data=\Data}
\text{empty=}
\if{\confparm4 notsametext \empty and \selective notsametext no}{
  \text{data=row(\confparm4,\data)}
}
\text{totname=column(3,\data)}
\text{totsymbole=column(2,\data)}

\integer{datacnt=min(\confparm3,rows(\data))}

\text{given=\given notitemof 2,3?randitem(2,3)}
\text{sxs=60,60,120,100}
\text{sh=item(1..\cnt,shuffle(\datacnt))}
\text{sh=wims(sort numeric items \sh)}
\text{data=row(\sh,\data)}
\text{data=column(\given,\data)}
\statement{\name_instruction
  <div class="wimscenter">
  \for{i=1 to \cnt}{
    \embed{r\i,\sxs[\given]x30}
  }
  </div>
}
\answer{\name_answer 1}{\data[1]}{type=dragfill}
\answer{\name_answer 2}{\data[2]}{type=dragfill}
\answer{\name_answer 3}{\data[3]}{type=dragfill}

