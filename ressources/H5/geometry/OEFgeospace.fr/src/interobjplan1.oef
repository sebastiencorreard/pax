%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/interobjplan.cpp.

\title{Intersection Plan / Objet de l'espace II}
\language{fr}
\range{-5..5}
\author{Régine, Mangeard}
\email{regine@mangeard.fr}
\computeanswer{no}
\format{html}
\precision{100}
\css{<style>
.special1 {font-size:x-large;background-color:lightblue;}
.special2 {font-size:x-large;background-color:MistyRose;}
.nonspecial {}
</style>
}
\text{style1=\confparm1!=1 ? wims_question:wimscenter special1}
\text{style2=\confparm1!=1 ? wims_question:wimscenter special2}
\matrix{oliste=sphère, cylindre de révolution, cône de révolution}
\matrix{natliste=ensemble vide, point, cercle, 1 droite, 2 droites}
\matrix{name_question=Donner les coordonnées de ce point.
Donner l'équation de ce cercle dans le plan (P).
Donner l'équation de cette droite dans le plan (P).
Donner l'équation de chacune des droites dans le plan (P).}
\text{name_answer=nature de l'intersection,
  abscisse, ordonnée, côte,cercle,droite,
  première droite,deuxième droite }
\matrix{axeliste= (Ox),(Oy),(Oz)}
\text{coeflist=\frac{1}{3},1, 3}
\text{rcoeflist=sqrt(3)/3,1,sqrt(3)}
\text{anglelist= 30, 45, 60}
\text{equatsList=}
\text{natureList=}
\text{interList=}
\integer{plandir=random(1..3)}
\integer{plancoord=random(-2..2)}
%%% equation du plan
\text{planlist= x=\plancoord, y=\plancoord, z=\plancoord, x=0, y=0, z=0}
\text{equaplan2=\planlist[\plandir +3]}
\integer{maxobj=20}
\integer{i=random(1..\maxobj)}
\text{equaplan=\planlist[\plandir]}
\integer{r=random(2..5)}
\integer{rr=-\r}
\integer{x=random(-5..5)}
\integer{y=random(-5..5)}
\integer{z=random(-5..5)}
\integer{d1=random(1..3)}
\integer{d2=random(1..3)}
\integer{d3=random(1..3)}
\rational{xc=(\x)/(\d1)}
\rational{xc=(\x)}
\rational{yc=(\y)/(\d2)}
\rational{yc=(\y)}
\rational{zc=maxima((\z)/(\d3);)}
\rational{zc=(\z)}
\text{kanglelist=1,2,3,1,2,3,1,2,3}
\integer{axe=random(1..3)}
\if{\i<6}{
  \integer{k=1}
   on choisit des sphères
  \rational{rep2=\xc}
  \rational{rep3=\yc}
  \rational{rep4=\zc}
  \rational{rep5=\r}
  \text{equat=simplify((x-\xc)^2+(y-\yc)^2+(z-\zc)^2-\r^2=0)}
  \text{nature= sphère de centre (\xc;\yc;\zc) et de rayon \r}
  %%% détermination de l'intersection
  \if{\plandir==1}{
    \rational{a=\xc-\plancoord}
    \rational{b=\plancoord-\xc }
    \if{\a> \r or \a < \rr}{
       \text{intersect= \(\emptyset)}
       \integer{natintersect=1}
    }
  \if{\r==\b or \r==\a}{
    \text{intersect= Point (\plancoord;\yc;\zc)}
    \rational{xp=\plancoord}
    \rational{yp=\yc}
    \rational{zp=\zc}
    \integer{natintersect=2}
  }
  \if{\a < \r and \a > \rr}{
    \text{interequat=simplify((\plancoord-\xc)^2+(y-\yc)^2+(z-\zc)^2-\r^2=0)}
    \text{intersect= cercle d'équation \(\interequat)}
    \integer{natintersect=3}
   }
 }
 \if{\plandir==2}{
   \rational{a=\yc-\plancoord}
   \rational{b=\plancoord-\yc }
   \if{\a> \r or \a < \rr}{
      \text{intersect= \(\emptyset)}
      \integer{natintersect=1}
   }
   \if{\r==\b or \r==\a}{
     \text{intersect= Point(\xc;\plancoord;\zc)}
     \rational{yp=\plancoord}
     \rational{xp=\xc}
     \rational{zp=\zc}
     \integer{natintersect=2}
   }
   \if{\a < \r and \a > \rr}{
     \text{interequat=simplify((x-\xc)^2+(\plancoord-\yc)^2+(z-\zc)^2-\r^2=0)}
     \text{intersect= cercle d'équation \(\interequat)}
     \integer{natintersect=3}
   }
 }
 \if{\plandir==3}{
   \rational{a=\zc-\plancoord}
   \rational{b=\plancoord-\zc }
   \if{\a> \r or \a < \rr}{
      \text{intersect= \(\emptyset)}
      \integer{natintersect=1}
   }
   \if{\r==\b or \r==\a}{
     \text{intersect= Point(\xc;\yc;\plancoord)}
     \rational{zp=\plancoord}
     \rational{xp=\xc}
     \rational{yp=\yc}
     \integer{natintersect=2}
   }
   \if{\a < \r and \a > \rr}{
     \text{interequat=simplify((x-\xc)^2+(y-\yc)^2+(\plancoord-\zc)^2-\r^2=0)}
     \text{intersect= cercle d'équation \(\interequat)}
     \integer{natintersect=3}
   }
 }
  %%# fin détermination
} %%# fin sphères
\if{\i>5 and \i<12}{
  \integer{k=2}
  on choisit des cylindres
\if{\i==6 or \i==7}{
  \integer{axe=1}
  \text{equat=simplify((y-\yc)^2+(z-\zc)^2-(\r)^2=0)}
  \text{rep7= texmath(y=\yc)}
  \text{rep8= texmath(z=\zc)}
}
\if{\i==8 or \i==9}{
  \integer{axe=2}
  \text{equat=simplify((x-\xc)^2+(z-\zc)^2-(\r)^2=0)}
  \text{rep7= texmath(x=\xc)}
  \text{rep8= texmath(z=\zc)}
}
\if{\i==10 or \i==11}{
  \integer{axe=3}
  \text{equat=simplify((x-\xc)^2+(y-\yc)^2-(\r)^2=0)}
  \text{rep7= texmath(x=\xc)}
  \text{rep8= texmath(y=\yc)}
}
\text{rep6=\axeliste[\axe]}
\rational{rep9=\r}
\text{nature= cylindre d'axe parallèle à \rep6 d'équations \rep7 et \rep8 et de rayon \r}
%%% détermination de l'intersection
\if{\axe==1}{
  \if{\plandir==1}{
    \text{interequat=simplify((y-\yc)^2+(z-\zc)^2-\r^2=0)}
    \text{intersect= cercle d'équation \(\interequat)}
    \integer{natintersect=3}
  }
  \if{\plandir==2}{
    \rational{a=\yc-\plancoord}
    \rational{b=\plancoord-\yc }
    \if{\a < \r and \a > \rr}{
      \rational{r2=(\r)^2-(\plancoord-\yc)^2}
      \text{interequat1=simplify(z-\zc-sqrt(\r2)=0)}
      \text{interequat2=simplify(z-\zc+sqrt(\r2)=0)}
      \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
      \integer{natintersect=5}
    }{
      \if{\r==\b or \r==\a}{
        \text{interequat1= simplify(z-\zc=0)}
        \text{intersect= droite \(\interequat1) }
        \integer{natintersect=4}
      }{
        \text{intersect= \(\emptyset)}
        \integer{natintersect=1}}
      }
    }
    \if{\plandir==3}{
      \rational{a=\zc-\plancoord}
      \rational{b=\plancoord-\zc }
      \if{\a < \r and \a > \rr}{
        \text{interequat1=simplify(y-\yc-sqrt((\r)^2-(\plancoord-\zc)^2)=0)}
        \text{interequat2=simplify(y-\yc+sqrt((\r)^2-(\plancoord-\zc)^2)=0)}
        \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
        \integer{natintersect=5}
      }{
        \if{\r==\b or \r==\a}{
          \text{interequat1= simplify(y-\yc=0)}
          \text{intersect= droite \(\interequat1) }
          \integer{natintersect=4}
           }{ \text{intersect= \(\emptyset)}
             \integer{natintersect=1}}
         }
     }
  } fin axe=1
  \if{\axe==2}{
     \if{\plandir==2}{
     \text{interequat=simplify((x-\xc)^2+(z-\zc)^2-\r^2=0)}
     \text{intersect= cercle d'équation \(\interequat)}
     \integer{natintersect=3}
     }
     \if{\plandir==1}{
       \rational{a=\xc-\plancoord}
       \rational{b=\plancoord-\xc }
       \if{\a < \r and \a > \rr}{
         \text{interequat1=simplify(z-\zc-sqrt((\r)^2-(\plancoord-\xc)^2)=0)}
         \text{interequat2=simplify(z-\zc+sqrt((\r)^2-(\plancoord-\xc)^2)=0)}
         \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
          \integer{natintersect=5}
        }{
          \if{\r==\b or \r==\a}{
            \text{interequat1= simplify(z-\zc=0)}
            \text{intersect= droite \(\interequat1) }
            \integer{natintersect=4}
           }{
            \text{intersect= \(\emptyset)}
            \integer{natintersect=1}}
          }
        }
        \if{\plandir==3}{
          \rational{a=\zc-\plancoord}
          \rational{b=\plancoord-\zc }
          \if{\a < \r and \a > \rr}{
            \text{interequat1=simplify(x-\xc-sqrt((\r)^2-(\plancoord-\zc)^2)=0)}
            \text{interequat2=simplify(x-\xc+sqrt((\r)^2-(\plancoord-\zc)^2)=0)}
            \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
            \integer{natintersect=5}
          }{
            \if{\r==\b or \r==\a}{
              \text{interequat1= simplify(x-\xc=0)}
              \text{intersect= droite \(\interequat1) }
              \integer{natintersect=4}
            }{
              \text{intersect= \(\emptyset)}
              \integer{natintersect=1}}
            }
          }
        } fin axe=2
  \if{\axe==3}{
     \if{\plandir==3}{
     \text{interequat=simplify((x-\xc)^2+(y-\yc)^2-\r^2=0)}
     \text{intersect= cercle d'équation \(\interequat)}
     \integer{natintersect=3}
     }
     \if{\plandir==2}{
       \rational{a=\yc-\plancoord}
       \rational{b=\plancoord-\yc }
       \if{\a < \r and \a > \rr}{
         \text{interequat1=simplify(x-\xc-sqrt((\r)^2-(\plancoord-\yc)^2)=0)}
         \text{interequat2=simplify(x-\xc+sqrt((\r)^2-(\plancoord-\yc)^2)=0)}
         \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
         \integer{natintersect=5}
        }{
          \if{\r==\b or \r==\a}{
           \text{interequat1= simplify(x-\xc=0)}
           \text{intersect= droite \(\interequat1) }
           \integer{natintersect=4}
           }{ \text{intersect= \(\emptyset)}
             \integer{natintersect=1}}
         }
     }
     \if{\plandir==1}{
       \rational{a=\xc-\plancoord}
       \rational{b=\plancoord-\xc }
       \if{\a < \r and \a > \rr}{
         \text{interequat1=simplify(y-\yc-sqrt((\r)^2-(\plancoord-\xc)^2)=0)}
         \text{interequat2=simplify(y-\yc+sqrt((\r)^2-(\plancoord-\xc)^2)=0)}
         \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
         \integer{natintersect=5}
        }{
          \if{\r==\b or \r==\a}{
           \text{interequat1= simplify(y-\yc=0)}
           \text{intersect= droite \(\interequat1\) }
           \integer{natintersect=4}
           }{ \text{intersect= \(\emptyset\)}
             \integer{natintersect=1}}
         }
     }
  } fin axe=3
} %%# fin cylindre
\if{\i>11}{
  on choisit des cones
  \integer{k=3}
  \integer{kangle=\kanglelist[\i - 11]}
  \text{coef=\coeflist[\kangle]}
  \text{rcoef=\rcoeflist[\kangle]}
  \if{\coef=1}{
    \text{\coef2=simplify((\plancoord)^2)}
  }{
    \if{\coef=3}{
      \text{\coef2=simplify((\plancoord)^2*3)}
    }{
     \text{\coef2=simplify((\plancoord)^2/3)}
    }
  }
\text{deg=\anglelist[\kangle]}
\text{rep11=\deg}
  \if{\i==12 or \i==13 or \i==14}{
    \integer{axe=1}
    \text{equat=\coef=1?y^2+z^2=x^2:y^2+z^2=\coef x^2}
  }
  \if{\i==15 or \i==16 or \i==17}{
    \integer{axe=2}
    \text{equat=\coef=1?x^2+z^2=y^2:x^2+z^2=\coef y^2}
  }
  \if{\i==18 or \i==19 or \i==20}{
    \integer{axe=3}
    \text{equat=\coef=1?x^2+y^2=z^2:x^2+y^2=\coef z^2}
  }
  \text{rep10=\axeliste[\axe]}
  \text{nature=cône d'axe parallèle à \rep10 et d'angle \deg degré}
%%% détermination de l'intersection
  \if{\axe==1}{
     \if{\plandir==1}{
     \text{interequat=y^2+z^2=\coef2}
     \text{intersect= cercle d'équation \(\interequat)}
     \integer{natintersect=3}
     \if{\plancoord==0}{
       \text{intersect= Point(0;0;0)}
       \rational{zp=0}
       \rational{xp=0}
       \rational{yp=0}
       \integer{natintersect=2}}
     }
     \if{\plandir==2}{
         \text{equaplan=\equaplan2}
         \text{interequat1=\coef=1?z-x=0:z-\rcoef *x=0}
         \text{interequat2=\coef=1?z+x=0:z+\rcoef *x=0}
         \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
         \integer{natintersect=5}
     }
     \if{\plandir==3}{
         \text{equaplan=\equaplan2}
         \text{interequat1=\coef=1?y-x=0:y-\rcoef *x=0}
         \text{interequat2=\coef=1?y+x=0:y+\rcoef *x=0}
         \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
         \integer{natintersect=5}
     }
  } fin axe=1
  \if{\axe==2}{
     \if{\plandir==2}{
     \text{interequat=x^2+z^2=\coef2}
     \text{intersect= cercle d'équation \(\interequat)}
     \integer{natintersect=3}
     \if{\plancoord==0}{
       \text{intersect= Point(0;0;0)}
       \rational{zp=0}
       \rational{xp=0}
       \rational{yp=0}
       \integer{natintersect=2}}
     }
     \if{\plandir==1}{
         \text{equaplan=\equaplan2}
         \text{interequat1=\coef=1?z-y=0:z-\rcoef *y=0}
         \text{interequat2=\coef=1?z+y=0:z+\rcoef *y=0}
         \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
         \integer{natintersect=5}
     }
     \if{\plandir==3}{
         \text{equaplan=\equaplan2}
         \text{interequat1=\coef=1?x-y=0:x-\rcoef *y=0}
         \text{interequat2=\coef=1?x+y=0:x+\rcoef *y=0}
         \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
         \integer{natintersect=5}
     }
  } fin axe=2
  \if{\axe==3}{
     \if{\plandir==3}{
     \text{interequat=x^2+y^2=\coef2}
     \text{intersect= cercle d'équation \(\interequat)}
     \integer{natintersect=3}
     \if{\plancoord==0}{
       \text{intersect= Point(0;0;0)}
       \rational{zp=0}
       \rational{xp=0}
       \rational{yp=0}
       \integer{natintersect=2}}
     }
     \if{\plandir==2}{
         \text{equaplan=\equaplan2}
         \text{interequat1=\coef=1?x-z=0:x-\rcoef *z=0}
         \text{interequat2=\coef=1?x+z=0:x+\rcoef *z=0}
         \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
         \integer{natintersect=5}
     }
     \if{\plandir==1}{
         \text{equaplan=\equaplan2}
         \text{interequat1=\coef=1?y-z=0:y-\rcoef *z=0}
         \text{interequat2=\coef=1?y+z=0:y+\rcoef *z=0}
         \text{intersect= droite \(\interequat1) et droite \(\interequat2)}
         \integer{natintersect=5}
     }
  } fin axe=3
} fin cone
%%%# Préparation de la deuxième question
\if{\natintersect=2}{
  \text{question2=\name_question[1;]}
  \text{mstep2=r2,r3,r4}
}
\if{\natintersect=3}{
  \text{question2=\name_question[2;]}
  \text{mstep2=r5}
}
\if{\natintersect=4}{
  \text{question2=\name_question[3;]}
  \text{mstep2=r6}
}
\if{\natintersect=5}{
 \text{question2=\name_question[4;]}
 \text{name_instruction=La première équation sera de la forme : <span class="tt">t-sqrt(a)+/-b=0</span>
  et la deuxième de la forme : <span class="tt">t+sqrt(a)+/-b=0</span>
  où t représente une des 3 variables x, y ou z}
 \text{mstep2=r7,r8}
}
\text{taille=100 x 350}
\text{\mstep=r1}
\nextstep{\mstep}
%%%%%# enoncé de l'exo %%%%%#
\statement{
<div class="\style1">
  Dans l'espace muni d'un repère orthonormé \((O,\vec{i},\vec{j},\vec{k})\), on considère un plan (P)
  d'équation \(\equaplan\) et un objet (Q) d'équation \(\equat\).
</div>
\if{\step==1}
{
<div class="\style2">
Déterminez la nature de leur intersection.
<div class="wimscenter">
  \embed{reply 1,\taille}
</div>
</div>
}{
<div class="\style2">
\question2
</div>
\if{\natintersect=5}{
<div class="wims_instruction">\name_instruction</div>}}
}
\text{mstep=\step=2?\mstep2:}
%%% soumission de réponse %%%
\answer{\name_answer[1]}{\natintersect;\natliste}{type=menu}
\answer{\name_answer[2]}{\xp}{type=numeric}
\answer{\name_answer[3]}{\yp}{type=numeric}
\answer{\name_answer[4]}{\zp}{type=numeric}
\answer{\name_answer[5]}{\interequat}{type=equation}
\answer{\name_answer[6]}{\interequat1}{type=equation}
\answer{\name_answer[7]}{\interequat1}{type=equation}
\answer{\name_answer[8]}{\interequat2}{type=equation}
