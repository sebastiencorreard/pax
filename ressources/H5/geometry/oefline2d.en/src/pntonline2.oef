%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/pntonline2.cpp.

\language{fr}
\range{-5..5}
\author{Gang, XIAO}
\email{xiao@unice.fr}
\computeanswer{no}
\format{html}
\text{lang=slib(oef/env lang)}
\title{Point on line II}
\if{\lang=fr}{
\title{Point sur une droite II}
}
\if{\lang=fr}{
}
!set lang_exists=yes
\text{name_instruction=We consider in the plane the line \(L) defined by the parametric equations}
\matrix{name_question=The point \(M\) with coordinates
  is on \(L\) if and only if \(m\) is equal to}
\if{\lang=fr}{
!set lang_exists=yes
\text{name_instruction=Dans le plan muni d'un repère, on considère la droite \(L\)
dont une représentation paramétrique de paramètre réel \(t\) est}
\matrix{name_question=Le point \(M\) de coordonnées
  appartient à \(L) si et seulement si \(m\) est égal à}
}
\if{\lang=nl}{
!set lang_exists=yes
\text{name_instruction=We consider in the plane the line \(L) defined by the parametric equations}
\matrix{name_question=The point \(M\) with coordinates
  is on \(L\) if and only if \(m\) is equal to}
}
\precision{1000}
\integer{a=random(2..20)*random(-1,1)}
\integer{b=random(2..20)*random(-1,1)}
\integer{c=random(1..20)*random(-1,1)}
\integer{d=random(1..20)*random(-1,1)}
\integer{g=random(-20..20)}
\text{ex=texmath(\a*t + \c)}
\text{ey=texmath(\b*t + \d)}
\integer{ch=random(1,2)}
\text{x=item(\ch,\g,m)}
\text{y=item(\ch,m,\g)}
\rational{sol=item(\ch,
  \d+(\b)*(\g-(\c))/(\a),
  \c+(\a)*(\g-(\d))/(\b)
  )}
\statement{\name_instruction
  <div class="wimscenter">
  \(\left \lbrace \begin{array}{rcl}
  x &=&\ex \\ y &=&\ey.
  \end{array}\right.\)
  </div>
  <label for="reply1">
  \name_question[1;] \((\x, \y)\) \name_question[2;]
  </label>
  \embed{reply1,10}.
}
\answer{\(m)}{\sol}{type=numexp}
