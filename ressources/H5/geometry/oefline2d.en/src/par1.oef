%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/1.cpp.

\precision{1000}
\language{fr}
\range{-5..5}
\author{Gang, XIAO}
\email{xiao@unice.fr}
\computeanswer{no}
\format{html}
\text{lang=slib(oef/env lang)}
\title{Parallel (éq. cartésienne)}
\if{\lang=fr}{
\title{Parallèle (éq. cartésienne)}
}
\if{\lang=fr}{
}
!set lang_exists=yes
\text{name_instruction=We consider in the plane the line \(L\)
  defined by the equation}
\matrix{name_question=An equation of the line passing through the point with coordinates
  and parallel to \(L) is}
\text{name_answer=Equation of the line}
\if{\lang=fr}{
!set lang_exists=yes
\text{name_instruction=Dans le plan muni d'un repère, on considère la droite \(L)
  d'équation cartésienne&nbsp;}
\matrix{name_question=Une équation de la droite passant par le point de coordonnées
  et parallèle à \(L) est}
\text{name_answer=Équation de la droite}
}
\if{\lang=nl}{
!set lang_exists=yes
\text{name_instruction=We consider in the plane the line \(L)
  defined by the equation}
\matrix{name_question=An equation of the line passing through the point with coordinates
  and parallel to \(L\) is}
\text{name_answer=Equation of the line}
}
\integer{a=random(2..20)*random(-1,1)}
\integer{b=random(2..20)*random(-1,1)}
\integer{c=random(-20..20)}
\integer{x=random(-20..20)}
\integer{y=random(-20..20)}
\integer{g=gcd(\a,\b)}
\integer{g=gcd(\g,\c)}
\integer{a=\a/\g}
\integer{b=\b/\g}
\integer{c=\c/\g}
\integer{t=\a*(\x) + (\b)*(\y)}
\text{L=pari(\a*x + \b*y) = \c}
\function{eq=\a*x + \b*y = \t}
\statement{\name_instruction:
  <div class="wimscenter">\(\L\).</div>
  <label for="reply1">\name_question[1;] \((\x, \y)) \name_question[2;]</label> \embed{reply1,15}.
}
\answer{\name_answer}{\eq,x,y}{type=equation}
