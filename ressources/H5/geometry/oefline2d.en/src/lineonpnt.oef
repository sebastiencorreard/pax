%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/1.cpp.

\precision{1000}
\language{fr}
\range{-5..5}
\author{Gang, XIAO}
\email{xiao@unice.fr}
\computeanswer{no}
\format{html}
\text{lang=slib(oef/env lang)}
\title{Line through point I}
\if{\lang=fr}{
\title{Droite passant par un point I}
}
\if{\lang=fr}{
}
!set lang_exists=yes
\text{name_instruction=We consider in the plane the line \(L\)
  defined by the equation}
\matrix{name_question=The line \(L) passes through the point \(M\) with coordinates
  if and only if \(m\) is equal to}
\if{\lang=fr}{
!set lang_exists=yes
\text{name_instruction=Dans le plan muni d'un repère, on considère la droite \(L)
  d'équation cartésienne&nbsp;}
\matrix{name_question=La droite \(L) passe par le point de coordonnées
  si et seulement si \(m\) est égal à}
}
\if{\lang=nl}{
!set lang_exists=yes
\text{name_instruction=We consider in the plane the line \(L)
  defined by the equation}
\matrix{name_question=The line \(L) passes through the point \(M\) with coordinates
  if and only if \(m\) is equal to}
}
\integer{a=random(2..20)*random(-1,1)}
\integer{b=random(2..20)*random(-1,1)}
\integer{c=random(-20..20)}
\integer{x=random(-20..20)}
\integer{y=random(-20..20)}
\integer{x=random(1..20)*random(-1,1)}
\integer{y=random(1..20)*random(-1,1)}
\integer{sol=\a*(\x)+(\b)*(\y)}
\text{L=\a*x + \b*y = m}
\statement{\name_instruction:
<div class="wimscenter">\(\L\).</div>
<label for="reply1">\name_question[1;] \((\x, \y)\) \name_question[2;]</label> \embed{reply1,10}.
}
\answer{\(m) }{\sol}{type=numexp}
