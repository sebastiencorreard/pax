\title{Vzporednica I}
\language{si}
\range{-5..5}
\author{Gang XIAO}
\email{xiao@unice.fr}
\computeanswer{no}
\format{html}
\precision{1000}

\integer{a=random(2..20)*random(-1,1)}
\integer{b=random(2..20)*random(-1,1)}
\integer{c=random(-20..20)}
\integer{x=random(-20..20)}
\integer{y=random(-20..20)}
\integer{t=\a*(\x) + (\b)*(\y)}
\text{L=\a*x + \b*y = \c}
\function{eq=\a*x + \b*y = \t}

\statement{Premica \(p) je doloèena z enaèbo
<p><center>\(\p).</center> <p>
Poi¹èite enaèbo njej vzporedne premice, ki vsebuje toèko
\((\x , \y)).
}

\answer{Enaèba vzporednice}{\eq}{type=equation}
