%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/parm2eq.cpp.

\language{fr}
\range{-5..5}
\author{Gang, XIAO}
\email{xiao@unice.fr}
\computeanswer{no}
\format{html}
\title{Rep. paramétrique vers éq. cartésienne}
\text{name_instruction=Dans le plan muni d'un repère, on considère la droite \(L\)
dont une représentation paramétrique de paramètre réel \(t\) est}
\matrix{name_question=Une équation de \(L\) est }
\text{name_inst=L'équation doit être de la forme}
\text{name_answer=Équation de la droite}
\precision{100}
\integer{a1=random(2..20)*random(-1,1)}
\integer{a2=random(2..20)*random(-1,1)}
\integer{b1=random(-1,1)*random(1..10)*2}
\integer{b2=random(-10..9)*2+1}
\integer{a=-(\a2)}
\integer{b=\a1}
\integer{c=(\a)*(\b1)+(\b)*(\b2)}
\integer{g=gcd(\a,\b)}
\integer{g=gcd(\g,\c)}
\integer{a=\a/\g}
\integer{b=\b/\g}
\integer{c=\c/\g}
\function{eq=\a*x + \b*y = \c}
\text{ex=texmath(\a1*t + \b1)}
\text{ey=texmath(\a2*t + \b2)}
\statement{\name_instruction:
<div class="wimscenter">
\(\left \lbrace \begin{array}{rcl}
  x &=&\ex \\ y &=&\ey .
\end{array}\right.\)
</div>
\name_question \embed{reply1,15}.
<div class="wims_instruction">
\name_inst \(a x + b y = c).
</div>
}
\answer{\name_answer \(L)}{\eq,x,y}{type=equation}
