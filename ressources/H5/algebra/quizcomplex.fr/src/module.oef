%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/module.cpp.






\author{XIAO Gang}
\email{xiao@unice.fr}

\format{html}
\range{-5..5}
\text{lang=slib(oef/env lang)}
\title{Module}
\text{name_ask=sont vraies,ne sont pas vraies}
\text{name_cond=bonnes,mauvaises}
\matrix{name_amongst=Parmi les
suivantes, lesquelles}
\text{name_answer=Les,sont}
\text{propsname=formules}
\text{name_instruction=Soient \(z1,z2) deux nombres complexes.}
\text{z12=shuffle(z1,z2)}
\text{z1=item(1,\z12)}
\text{z2=item(2,\z12)}
\text{z3=z3}
\text{i=random(i,-i)}
\text{good=shuffle(
 \(abs(-\z1) = abs(\z1)),
 \(abs(-conj(\z1)) = abs(\z1)),
 \(abs(conj(\z1)) = abs(\z1)),
 \(abs(\z1*\z2) = abs(\z1)*abs(\z2)),
 \(abs(\z1*conj(\z2)) = abs(\z1)*abs(\z2)),
 \(abs(conj(\z1)*\z2) = abs(\z1)*abs(\z2)),
 \(abs(\z1/\z2) = abs(\z1)/abs(\z2)),
 \(abs(conj(\z1)/\z2) = abs(\z1)/abs(\z2)),
 \(abs(\z1/conj(\z2)) = abs(\z1)/abs(\z2)),
 \(abs(1/\z1) = 1/abs(\z1)),
 \(abs(-1/\z1) = 1/abs(\z1)),
 \(abs(1/conj(\z1)) = 1/abs(\z1)),
 \(abs(\z1 + \z2) = abs(\z2 + \z1)),
 \(abs(conj(\z1) + conj(\z2)) = abs(\z2 + \z1)),
 \(abs(conj(\z1) - conj(\z2)) = abs(\z2 - \z1)),
 \(abs(\i*\z1) = abs(\z1)),
 \(abs(\i/\z1) = 1/abs(\z1)),
 \(abs(\z1^2) = abs(\z1)^2),
 \(abs(\z1)^2 = \z1*conj(\z1)),
 \(\z1*conj(\z1) = abs(\z1)^2),
 \(abs(\z1*conj(\z1)) = \z1*conj(\z1)),
 \(abs(\z1*conj(\z1)) = abs(\z1)^2),
)}
\text{bad=shuffle(
 \(abs(-\z1) = -abs(\z1)),
 \(abs(-\z1) = 1/abs(\z1)),
 \(abs(conj(\z1)) = - abs(\z1)),
 \(abs(conj(\z1)) = 1/abs(\z1)),
 \(abs(1/conj(\z1)) = abs(\z1)),
 \(abs(\z1+\z2) = abs(\z1) + abs(\z2)),
 \(abs(\z1-\z2) = abs(\z1) - abs(\z2)),
 \(abs(\z1*\z2) = abs(\z1) + abs(\z2)),
 \(abs(\z1*conj(\z2)) = abs(\z1) / abs(\z2)),
 \(abs(conj(\z1)*\z2) = abs(\z2) / abs(\z1)),
 \(abs(\z1*conj(\z2)) = abs(\z1) - abs(\z2)),
 \(abs(conj(\z1)*\z2) = abs(\z2) - abs(\z1)),
 \(abs(\z1/\z2) = abs(\z1) - abs(\z2)),
 \(abs(1/\z1) = - abs(\z1)),
 \(abs(-1/\z1) = - abs(\z1)),
 \(abs(1/\z1) = - 1/abs(\z1)),
 \(abs(1/\z1) = abs(conj(\z1))),
 \(abs(\z1 + conj(\z2)) = abs(\z1 + \z2)),
 \(abs(conj(\z1) + \z2) = abs(\z1+\z2)),
 \(abs(\z1-conj(\z2)) = abs(\z1-\z2)),
 \(abs(conj(\z1) - \z2) = abs(\z1 - \z2)),
 \(abs(\i*\z1) = \i*abs(\z1)),
 \(abs(\i+\z1) = \i + abs(\z1)),
 \(abs(\i/\z1) = \i/abs(\z1)),
 \(abs(\z1^2) = 2*abs(\z1)),
 \(abs(\z1)^2 = \z1^2),
)}
\text{good=wims(nonempty items \good)}
\text{bad=wims(nonempty items \bad)}
\integer{badcnt=random(1,1,2,2,3)}
\integer{goodcnt=4-\badcnt}
\text{bads=item(1..\badcnt,1,1,1,1,1,1)}
\text{gb=item(1..4,\bads,0,0,0,0,0,0)}
\text{bads=item(1..\badcnt,\bad)}
\text{goods=item(1..4-\badcnt,\good)}
\text{order=shuffle(4)}
\text{list=item(\order,\bads,\goods)}
\text{gb=item(\order,\gb)}
\text{bad=wims(positionof item 1 in \gb)}
\text{good=wims(positionof item 0 in \gb)}
\text{l1=item(1,\list)}
\text{l2=item(2,\list)}
\text{l3=item(3,\list)}
\text{l4=item(4,\list)}
\if{random(1,2) = 1}{
  \text{ask=\name_ask[2]}
  \text{cond=\name_cond[2]}
  \text{ans=\bad}
}{
  \text{ask=\name_ask[1]}
  \text{cond=\name_cond[1]}
  \text{ans=\good}
}
\statement{\name_instruction
\name_amongst[1;] \propsname \name_amongst[2;] \ask?
<ul>
 <li>\embed{reply 1,1}. \l1</li>
 <li>\embed{reply 1,2}. \l2</li>
 <li>\embed{reply 1,3}. \l3</li>
 <li>\embed{reply 1,4}. \l4</li>
</ul>
}
\reply{\name_answer[1] \cond \propsname \name_answer[2]}{\ans;1,2,3,4}{type=checkbox}
