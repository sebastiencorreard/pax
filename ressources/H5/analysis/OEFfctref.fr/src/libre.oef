%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/fctref.cpp.

%%%%% MB modifs faites :
ajout de mapsto à la place de rightarrow
ajout d'un type de fonction x-->k^x
modification des coeff a dans le cas où il y a une seule fonction de sélectionné (items(\confparm1)==1)
 et qu'il faut utiliser un seul type de fct and (\confparm2=2)
 et que l'on utilise que les fct af (\confparm3 issametext A)
      Eviter de faire un choix uniquement à partir de l'ordonnée à l'origine
ajout de feedback pour choix1, choix2 et libre
Utilisation de maxima pour affichage correct des fcts
%%%%%%%%
\title{Écrire la fonction}
\author {Chantal, Causse}
\email {Chantal.Causse@noos.fr}
\format {html}
\integer{nb = \confparm4 = ? 4 : \confparm4}
\text{confparm2 = \confparm2 = ? 1,2,3 : \confparm2}
\text{confparm1 = \confparm1 = ? A,B,C : \confparm1}
\text{confparm3 \confparm3 = ? B : \confparm3}
\text{Fref = \confparm3 issametext A ? randitem(\confparm1) : \confparm1}
***rectification d'incohérences
\integer{nf0 = items(\confparm1)}
\integer{nf0 = F isitemof \confparm1 ? \nf0+1}
\integer{nf1 = items(\Fref)}
\integer{nf1 = F isitemof \Fref ? \nf1+1}
\if{\confparm2 issametext 1 and \nf1<\nb }{
\if{\nf0 >= \nb}{\text{Fref = \confparm1}}
{\if{\nf0>=3}{\text{Fref = \confparm1}\text{nb = \nf0}}
{\text{Fref = wims(listuniq A,B,C,\confparm1)}
\text{nb = 3}}}
}
******* Etablissement de la liste des fonctions
%%%% modif MB %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\text{q=random(0.5,0.1,0.2,0.8)} %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\real{invq=1/\q} %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\text{listFref = wims (replace internal A by x in \Fref)}
\text{listFref = wims (replace internal B by (x)^2 in \listFref)}
\text{listFref = wims (replace internal C by 1/(x) in \listFref)}
\text{listFref = wims (replace internal D by abs(x) in \listFref)}
\text{listFref = wims (replace internal E by sqrt(x) in \listFref)}
\text{listFref = wims (replace internal F by sin(x),cos(x) in \listFref)}
\text{listFref = wims (replace internal G by (x)^3 in \listFref)}
%%%% modif MB %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\text{listFref = wims (replace internal H by (\q)^x,(\invq)^x in \listFref)} %%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\text{listModel = wims(replace internal (x)^ by x^ in \listFref)}
%%% Modif MB right arrow en \mapsto %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\text{listModel = wims(makelist \(x \mapsto)\( g \) for g in \listModel)} %%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\integer{Nmod = items(\listModel)}
\text{ff = \Nmod = 1 ? de la fonction de référence : d'une des fonctions de référence}
\text{fcts = 1 isitemof \confparm2 ? \listFref :}
\text{op = wims(listcomplement 1 in \confparm2 )}
\integer{nop = items(\op)}
\if{\nop > 0}{
\text{a = shuffle(-1,1/2,3/2,2,-1/2,-3/2,-2) }
%%%%%%modif MB %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
suivant la config %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\if{(\confparm1 issametext H ) and (\confparm2=2) and (\confparm3 issametext A)}{
\if{\confparm4<5}{
\text{a=\a[1],\a[1],\a[2],\a[2]}
}{\text{a=\a[1],\a[1],\a[2],\a[2],\a[3],\a[3]}}
}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\text{b = shuffle(1,-1,1/2,3/2,2,-1/2,-3/2,-2)}
\text{k = shuffle(1,-1,1/2,3/2,2,-1/2,-3/2,-2)}
\for{i = 1 to \nb}{
\function{f = randitem(\listFref)}
%%%%%% modif MB %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
suivant la config
\if{\confparm1 issametext H}{
\function{f = \listFref[\i%2+1]}
}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\text{xx = pari(x+\k[\i])}
\text{f5 = wims(replace internal x by \xx in \f)}
\text{f6 = wims(replace internal x by \xx in \f+\b[\i])}
\text{f7 = wims(replace internal x by \xx in \a[\i]*\f+\b[\i])}
\text{modeles = \f, \a[\i]*\f,\f+\b[\i],\a[\i]*\f+\b[\i],\f5,\f6,\f7 }
\integer{choix = randitem(\op)}
\if{\f issametext x}{
\integer{choix = \choix = 5 ? 3}
\integer{choix = \choix = 6 ? 3}
\integer{choix = \choix = 7 ? 4}
}
\text{F = \modeles[\choix]}
%%%%%%%%%%%%%%%%%%%%%%%%%%% MB %%%%%%%%%%%%%%%%%%%
%%%%% affichage correct des fcts sans les parenthèses inutiles
\text{F = maxima(expand(\modeles[\choix]))}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\text{fcts = wims(append item \F to \fcts)}
}}
\text{fcts = shuffle(\fcts)}
\text{fcts = \fcts[1..\nb]}
\text{fcts = wims(replace internal (x)^ by x^ in \fcts)}
\text{fcttext = wims(makelist \(j\) for j in \fcts)}
%%%% ajout MB pour avoir la flèche d'attribution
\text{fcttext2 = }
\for{hj=1 to \nb}{
\text{fcttext2=wims(append item \(x \mapsto {}\)\( \fcts[\hj] \) to \fcttext2)}
}
%%%%
******** Fond des graphiques
\text{gridcolor=grey}
\text{cadre=
xrange -3,3
yrange -3,3
linewidth 1
vline 1,0,\gridcolor
vline 2,0,\gridcolor
vline -1,0,\gridcolor
vline -2,0,\gridcolor
hline 0,1,\gridcolor
hline 0,2,\gridcolor
hline 0,-1,\gridcolor
hline 0,-2,\gridcolor
linewidth 1
vline 0,0,black
hline 0,0,black
linewidth 2
segment 1,-0.1,1,0.1,black
segment -0.1,1,0.1,1,black
text red,0.9,-0.1,medium,I
text red,-0.4,1.2,medium,J
text black,-0.2,-0.1,medium,O
}
********Liste des courbes
\text{size=180}
\text{graphList=}
\text{grbordList=}
\for{k=1 to \nb}{
\text{courbe = draw(\size,\size
    \cadre
    linewidth 2
    plot blue,\fcts[\k] ) }
  \text{graph= <img src='\courbe' alt=''>
    }
  \text{grbord=
    <table class="wimscenter">
      <tr>
        <td>
    \graph
  </td>
      </tr>
    </table>
  }
  \text{graphList=wims(append item \graph to \graphList)}
  \text{grbordList=wims(append item \grbord to \grbordList)}
}
\text{taille=\size x \size x \size}
\text{repere = repère \((O,I,J)\) }
\text{ETAPE = wims(makelist r x for x = 1 to \nb)}
\steps{\ETAPE}
\statement{
Écrire en dessous de chaque courbe la fonction représentée dans le \repere \if{\confparm2 notsametext 1}{, sachant que chacune de ces fonctions est construite à l'aide d'opérations simples à partir \ff \listModel}
 \if{\confparm2 issametext 1}{sachant que ce sont toutes des fonctions de référence}.
<br>
\for {n = 1 to \nb}{<div class = "float_left spacer">
<table class="wimsborder wimscenter">
      <tr><td>\graphList[\n] </td></tr><tr><td> \(f(x) = \)\embed{reply\n,15}</td></tr></table></div>}
<br class="clearall" >
}
\help{Pour la fonction \(abs(x)\), il faut taper abs(x)<br>
Pour la fonction \(sqrt(x)\), il faut taper sqrt(x)}
\answer{}{\fcts[1]}{type = formal}
\answer{}{\fcts[2]}{type = formal}
\answer{}{\fcts[3]}{type = formal}
\answer{}{\fcts[4]}{type = formal}
\answer{}{\fcts[5]}{type = formal}
\answer{}{\fcts[6]}{type = formal}
\feedback{\sc_reply1<1}{<div class="feedback">Ci dessous, les bonnes associations
<br>
\for {n = 1 to \nb}{<div class = "float_left spacer">
<table class="wimsborder wimscenter">
      <tr><td>\graphList[\n] </td></tr><tr><td> \fcttext2[\n]</td></tr></table></div>}
<br class="clearall" >
</div>
}
