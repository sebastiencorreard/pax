%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/term1.cpp.

%% Expression ou équation ou proposition
\language{fr}
\computeanswer{yes}
\precision{1000}
\format{html}

\author{Véronique, Royer}
\email{royer.veronique@orange.fr}




\title{Terminologie 1}


\text{name_enonce=Choisir le bon terme dans les énoncés suivants.}
\matrix{phrases= &#171; <span style="color:blue"> \(UUUU = (VVVV)^2\)</span> &#187; est une
 &#171; <span style="color:blue">il existe un seul réel \(x\), tel que \( UUUU = VVVV\)</span> &#187; est une
 Dans &#171; <span style="color:blue">\(VVVV\) est une somme</span> &#187;, le sujet est une
 Dans &#171; <span style="color:blue">calculer les racines de \(WWWW\)</span> &#187;, le complément du mot "racines" est une
 Dans &#171; <span style="color:blue">l'ensemble des solutions de \(E\) est vide</span> &#187;, la lettre E doit désigner une
 &#171; <span style="color:blue">\(w\) change de signe deux fois</span> &#187; est une
 &#171; <span style="color:blue"> \( UUUU = VVVV\) \(\Longleftrightarrow \)\( x = \sol\) </span> &#187; est une
 le premier membre de l'égalité &#171; <span style="color:blue"> \(f(x) = 0\)</span> &#187; est une
 écrire &#171; <span style="color:blue"> \(WWWW\) \(\Longleftrightarrow\) 0</span> &#187; est incorrect, car \(WWWW) est une
}
\text{liste=expression, équation, proposition }

\integer{a = random(2..5)}
\integer{b = (-1)^random(0,1)*random(1..5)}
\integer{c = (-1)*random(2..5)}
\integer{d = (-1)^random(0,1)*random(1..5)}
\integer{min = (-1)^random(0,1)*random(1..10)}
\integer{max = \min + random(1..5)}
\function{u=\a*x + \b}
\function{v= \d+\c*x}
\function{w=\a*x^2 + \b*x + \c}
\rational{sol= (\d-\b)/(\a-\c)}
\text{reps= 2,3,1,1,2,3,3,1,1}
\text{ind=shuffle(9)}
\text{phrases=wims(replace internal UUUU by \u in \phrases)}
\text{phrases=wims(replace internal VVVV by \v in \phrases)}
\text{phrases=wims(replace internal WWWW by \w in \phrases)}
\statement{
<div class="monenonce">
\name_enonce
<ol>
\for{i = 1 to 6}{<li> \phrases[\ind[\i];] \embed{reply\i}</li>}
</ol>
</div>
}
\answer{}{\reps[\ind[1]] ; \liste}{type=menu}
\answer{}{\reps[\ind[2]] ; \liste}{type=menu}
\answer{}{\reps[\ind[3]]; \liste}{type=menu}
\answer{}{\reps[\ind[4]] ; \liste}{type=menu}
\answer{}{\reps[\ind[5]] ; \liste}{type=menu}
\answer{}{\reps[\ind[6]] ; \liste}{type=menu}

