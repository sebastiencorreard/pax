%% This file is generated by preprocessor!
%% Do not edit it by hand.
%% Edit the source file cpp/deriverPoly.cpp.

\language{fr}
\range{-5..5}
\computeanswer{no}
\format{html}
\precision{100}
\author{Véronique, Royer}
\email{royer.veronique@orange.fr}
\text{lang=slib(oef/env lang)}
\title{Dérivée d'une fonction polynôme}
\title_en{Derivative of a polynomial}
\text{name_answer=Dérivée}
\text{name_ifthen=Si,alors}
\text{name_enonce= Soit \(f\) la fonction polynôme définie sur \(\,\RR\) par,
  La fonction \(f\) est dérivable sur}
\text{name_question=Calculer sa fonction dérivée.}
\text{name_forall=Pour tout réel}
\text{name_hint=On utilise les règles de dérivation suivantes,
  Pour dériver un polynôme, on dérive donc terme à terme.,
  On remarquera&nbsp;}
\if{\lang=en}{
\text{name_answer=Derivative}
\text{name_ifthen=If,then}
\text{name_enonce= Let \(f\) be the polynomial function defined in \(\,\RR\) by,
  The function \(f\) is differentiable in}
\text{name_question=Calculate its derived function.}
\text{name_forall=For all real numbers}
\text{name_hint=The following derivation rules are used,
  The derivative of a polynomial is determined term by term.,
  We remark}
}
\integer{a = randitem(-1,1)*random(1..10)}
\integer{b = randitem(-1,1)*random(1..10)}
\integer{c = randitem(-1,1)*random(1..10)}
\integer{d = randitem(-1,1)*random(1..10)}
\integer{e = randitem(-1,1)*random(1..10)}
\rational{a1 = \a/randint(2..5)}
\rational{b1 = \b/randint(2..5)}
\rational{c1 = \c/randint(2..5)}
\rational{d1 = \d/randint(2..5)}
\real{a2 = randitem(-1,1)*randint(1..100)/100}
\real{b2 = randitem(-1,1)*randint(1..100)/100}
\real{c2 = randitem(-1,1)*randint(1..100)/100}
\real{d2 = randitem(-1,1)*randint(1..100)/100}
\function{f=randitem(\e,
\a *x^4 + \b*x^3+ \c*x^2 + \d*x + \e,
\a*x^3+ random(\c*x^2 , \d*x) + \e,
 \a*x^2 + \d*x+ \e,
 \a*x^2 + \e,
 \a*x + \e,
\a1*x^3+ random(\b1*x^2 , \c1*x) + \d1,
\a1*x^2 + \c1*x + \d1,
\a1*x^2 + \d1,
\a1*x+\d1,
\a2*x^3+ random(\b2*x^2 , \c2*x) + \d2,
\a2*x^2 + \c2*x + \d2,
\a2*x^2 + \d2,
\a2*x + \d2
)}
/** bonnes réponses **/
\function{derivee = diff(\f,x)}
\text{derivee=pari(default(format,"f.4"); \derivee)}
\statement{
<div class="wims_question">
<p>\name_enonce[1] \(f(x) = \f\).</p>
<p>\name_enonce[2] \(\, \RR\). \name_question</p>
</div>
<div class="wimscenter">
\name_forall \(x\), <label for="reply1">\(f'(x)\)</label> = \embed{reply1,30}
</div>
}
\answer{\name_answer}{\derivee}{type=function}
\hint{\name_hint[1]:
<ul><li>
\name_ifthen[1] \(f(x)=x^n) \name_ifthen[2] \(f'(x)= n x^{n-1})
</li><li>
\name_ifthen[1] \(f(x)=k u(x)) \name_ifthen[2] \(f'(x)=k u'(x))
</li><li>
\name_ifthen[1] \(f(x)= u(x)+v(x)) \name_ifthen[2] \(f'(x)=u'(x)+v'(x)).
</li></ul>
\name_hint[2]
<p>\name_hint[3]:</p>
<ul><li>
  \name_ifthen[1] \(f(x)=a x^n) \name_ifthen[2]
    \(f'(x)=a \times (n x^{n-1}) = (n \times a) x^{n-1}\)
  </li><li>\name_ifthen[1] \(f(x) = a = a x^0)
    \name_ifthen[2] \(f'(x) = a \times 0 = 0\).
</li></ul>
}
