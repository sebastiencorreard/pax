!set title=Fonction 15
!if $wims_read_parm!=$empty
 !goto $wims_read_parm
!endif
oef2wims_version=3.22
author=Julien, Lyotard
email=Julien.Lyotard@ac-dijon.fr
language=fr

ansorder=r,r,r
varcnt=76
prevarcnt=65
postvarcnt=10
replycnt=3
choicecnt=0
conditioncnt=0
feedbackcnt=1
format=html

val1=$imagedir
val2=$confparm1
val3=$confparm2
val4=$confparm3
val5=$confparm4

!if $testcondition=yes
 !goto test
!endif
!if $status=waiting
 !exit
!endif

val6=
val7=Initialiser correctement
val8=Jouer le code et envoyer la réponse.
val9=dans le programme afin d'obtenir une représentation graphique identique.
val10=<img src="$val1/attention.png" alt="Attention"> La variable	  \(\text{a}\) doit impérativement être comprise entre 0.01 et 1.
val11=<ul><li>Il est indispensable d'appeler la librarie mathématique de Python en utilisant	  l'instruction <code>from math import *</code>	  au début du programme si l'on souhaite utiliser la fonction racine carrée <code>sqrt(x)</code>.	  </li><li>	  L'instruction <code>assert</code> suivie d'une condition permet d'effectuer une	  <strong>exception</strong> sur des valeurs définies par la condition.</li>	 </ul>
val12=Quelle(s) conjecture(s) pouvez-vous effectuer par lecture du graphique ?

!ifval ($val2==)
val13=$[rint(1)]
!else
val13=$[rint($val2)]
!endif
tmp=!trim ambiance,neat;rubyblue,eclipse;the-matrix,3024-day
val14=!translate internal $	\
$ to ;; in $tmp
val15=()
tmp0=!char 1 of $val15

val16=$(tmp0)
tmp0=!char 2 of $val15

val17=$(tmp0)
val18=[]
tmp0=!char 1 of $val18

val19=$(tmp0)
tmp0=!char 2 of $val18

val20=$(tmp0)
val21=??
tmp=!nonempty items -1,1

tmp0=!randitem $tmp
tmp1=!randint 1, 5

val22=$[rint($(tmp0)*$(tmp1))]
val23=$val22
tmp0=!randint 1, 2

val24=$[rint($(tmp0))]

!ifval ($val24==1)
val25=+
!else
val25=-
!endif
tmp0=!randint -3, -1

val26=$[rint($(tmp0))]
tmp0=!randint 4, 6

val27=$[rint($val26+$(tmp0))]
tmp0=!randint 1, 2

val28=$[rint($val26+$(tmp0))]
val29=$[rint($val27-1)]
val30=$val25(t-$val28)*(t-$val29)+$val22
tmp=!translate ";': to $     $ in $val30

tmp=!lower $tmp
tmp0=!exec maxima fullratsimp($tmp);
t_=!replace internal \( by ( in $(tmp0)
tmp1=!exec maxima $t_

val30=$(tmp1)
tmp0=!replace t by x in $val30

val31=$(tmp0)
tmp=!translate ";': to $     $ in $val31

tmp=!lower $tmp
tmp0=!exec maxima fullratsimp($tmp);
t_=!replace internal \( by ( in $(tmp0)
tmp1=!exec maxima $t_

val31=$(tmp1)
val32=$val28
val33=$val28
tmp=!nonempty items 0.01,0.02,0.05

tmp0=!randitem $tmp
val34=$[$(tmp0)]
val35=??
val36=??
val37=??
val38=instruction=[x**2,y_list[n]==k,abs(y_list[n]-(k))&lt;10**(-9)]
tmp0=!randint 1, 2

val39=$[rint($(tmp0))]

!ifval ($val39==1)
val40=red
!else
val40=blue
!endif

!ifval ($val39==1)
val41=blue
!else
val41=red
!endif
val6=if(abs(yl[nn]-($val22))<10^-(9),nzero=nn;);	if(abs(yl[nn]-($val22))<10^-(9),colorl=concat(colorl,[$val40]);,colorl=concat(colorl,[$val41]););
val42=\colorl[\kj]
val43=n=0
val44=color_list=[]
val45=if ?? :	      color_list.append('$val40')	  else:	      color_list.append('$val41')	  n=n+1
val46=print(color_list[\:10])
val47=color_liste=Sk.ffi.remapToJs(Sk.globals["color_list"]);
val48=Résolution d'équation \(\underline{f(x) = k}\) sur un intervalle
val49=On souhaite conjecturer les intervalles solution de	 l'équation :	<div class="wimscenter"> \( $val31 = $val22\) sur l'intervalle \(\left $val19 $val26; $val27 \right $val20\).</div>	$val7 les valeurs des variables \(\text{x}\),	 \(\text{a}\), \(\text{xm}\). Déterminer la fonction. Compléter l'instruction conditionnelle "if else" avec une	 égalité ou une inégalité afin d'obtenir une représentation graphique identique.	 $val8	<br> $val10
t_=!replace internal \( by ( in f(t)=$val30;
tmp0=!exec pari $t_

val50=$(tmp0)
t_=!replace internal \( by ( in default(format, "f.5");	nn=1;	xi=$val26;	ymin=f(xi);	ymax=f(xi);	pas=$val34;	xl=[];	yl=[];	colorl=[];	colorll=[black];	while(xi<=$val27 || abs(xi-$val27)<10^(-4),	yi=f(xi);	xl=concat(xl,xi);	yl=concat(yl,yi);	xi=xi+pas;	$val6	if(f(xi)<ymin && abs(xi)>0.0001,ymin=f(xi););	if(f(xi)>ymax && abs(xi)>0.0001,ymax=f(xi));	nn=nn+1;);	
tmp0=!exec pari $t_

val51=$(tmp0)
t_=!replace internal \( by ( in print(xl);
tmp0=!exec pari $t_

val52=$(tmp0)
t_=!replace internal \( by ( in print(yl);
tmp0=!exec pari $t_

val53=$(tmp0)
t_=!replace internal \( by ( in print(ymin);
tmp0=!exec pari $t_

val54=$(tmp0)
t_=!replace internal \( by ( in print(ymax);
tmp0=!exec pari $t_

val55=$(tmp0)
t_=!replace internal \( by ( in print(colorl);
tmp0=!exec pari $t_

val56=$(tmp0)
t_=!replace internal \( by ( in print(colorll);
tmp0=!exec pari $t_

val57=$(tmp0)
t_=!replace internal \( by ( in print(nzero);
tmp0=!exec pari $t_

val58=$(tmp0)
val59=$[rint(floor(($val27-$val26)/$val34)+1)]
val60=$[rint(min(10,$val59))]
val61=Le code Python donné ci-dessous permet de construire le tableau de valeurs	  d'une fonction dans 2 listes \(\text{x_list}\) et \(\text{y_list}\). La représentation	  graphique du tableau de valeurs est construite automatiquement lorsque le code est joué.	  Compléter cet algorithme afin de réaliser la représentation graphique proposée. Afin de	  vous aider, les coordonnées exactes des $val60 premiers points	  sont données dans le tableau de valeurs ci-dessous.
val62=
!for val63 =1 to $val59 
tmp0=!append line board.create('point', [$(val52[$val63]),$(val53[$val63])],{color:"$(val56[$val63])"}); to $val62

val62=$(tmp0)
!next
val64=\#Initialisation des variables#	$val43	x=$val35	a=$val37	xm=$val36	\#Définition d'une fonction#	def f(x):	  return $val21	\#Initialisation des listes#	x_list=[]	y_list=[]	$val44	\#Boucle afin de construire les listes#	while x<=xm:	  y=f(x)	  x_list.append(x)	  y_list.append(y)	  x=x+a	  $val45	\#Affichage des 10 premiers termes des listes#	print(x_list[\:10])	print(y_list[\:10])	$val46
!readproc slib/geo2D/jsxgraph jsxbox1,400 x 400,JXG.Options.point.fixed = true;	JXG.Options.point.showInfobox=false;	JXG.Options.point.highlight=false;	JXG.Options.point.name = "";	JXG.Options.point.face = "[]";	JXG.Options.point.size = 60/$val59;	JXG.Options.point.withLabel=true;	var board = JXG.JSXGraph.initBoard('jsxbox1', {axis:true, boundingbox: [$val26-1,$val55+1,$val27+1,$val54-1],	shownavigation:false,showCopyright : false,	showZoom : true,keepaspectratio : false,	  zoom: {factorX: 1.25,factorY: 1.25,wheel: true,needShift: false,eps: 0.001},	  pan: {needShift: false,enabled: true,needTwoFingers: true,pinchHorizontal: true, pinchVertical: true,pinchSensitivity: 7}});	$val62	 
tmp0=$slib_out
val65=$(tmp0)
!readproc slib/geo2D/jsxgraph jsxbox,400 x 400,JXG.Options.point.fixed = true;	JXG.Options.point.highlight=false;	JXG.Options.point.showInfobox=false;	JXG.Options.point.name = "";	JXG.Options.point.face = "[]";	JXG.Options.point.withLabel=true; 
tmp0=$slib_out
val66=$(tmp0)
replytype1=runcode

replyoption1=precision=100 $val38 
replyweight1=10 
replyname1=
replygood1=[x_list,[$val52]],[y_list,[$val53]],[color_list,[$val56]]
replytype2=numexp
replyoption2=noanalyzeprint 
replyweight2=1 
replyname2=
replygood2=$val28
replytype3=numexp
replyoption3=noanalyzeprint 
replyweight3=1 
replyname3=
replygood3=$val29
question=__EXECUTED_1692754_EXERCISE__

!goto stat

!exit

:question
$()	<div class="encadre">	<p class="problematique">$val48</p>	<div id="boited">	<img src="$val1/python.png" alt="Logo de Python">	</div>	<div class="enonce">	$val61	<table class="wimscenter wimsborder">	<tr><th>
!insmath x_i
$()</th>	 
!for m_ii=1 to $val60 
$()<td>$(val52[$m_ii])</td> 
!next
$()	</tr>	<tr><th>
!insmath y_i
$()</th>	 
!for m_ii=1 to $val60 
$()<td>$(val53[$m_ii])</td> 
!next
$()	</tr>	</table>	<div class="question">	$val49	</div>	</div>	<div>	 
!read oef/embed.phtml reply1,[python,[$val64]];[idtest2,fullscreen theme=[$(val14[$val13;])] canvas init $val38,Jouer le code !] 
$()	</div>	$m_inst	<br>	<div class="wimscenter wimsborder box">	<div>Représentation graphique du tableau de valeurs</div>	$val66	</div>	<div class="wimscenter wimsborder box">	<div>Représentation graphique à effectuer</div>	$val65	</div>	<div class="question">$val12	<br>La résolution de l'équation 
!insmath  $val31 = $val22 
$() sur l'intervalle 
!insmath \left $val19 $val26; $val27 \right $val20
$() a pour solution :	<div class="wimscenter">	 
!read oef/special.phtml mathmlinput [\text{S}=\left \lbrace \, {reply2};{reply3} \, \right \rbrace],2,	reply 2,,color:blue;font-size:20px	reply 3,,color:blue;font-size:20px 
$()	</div>	<div class="wims_instruction">Pour le symbole infini, taper inf.	  Donner les solutions dans l'ordre croissant.</div>	</div>	</div>	<script>	function graph(){	x_liste=Sk.ffi.remapToJs(Sk.globals["x_list"]);	y_liste=Sk.ffi.remapToJs(Sk.globals["y_list"]);	$val47	const xlen=x_liste.length;	function findMinMax(arr,ar) {let xmin = arr[0], xmax = arr[0],ymin = ar[0], ymax = ar[0];	  for (let i = 1, len=arr.length; i < len; i++){	    let v = arr[i];	    let vv = ar[i];	    xmin = (v < xmin && Math.abs(v)>0.0001) ? v : xmin;	    xmax = (v > xmax && Math.abs(v)>0.0001) ? v : xmax;	    ymin = (vv < ymin && Math.abs(v)>0.0001) ? vv : ymin;	    ymax = (vv > ymax && Math.abs(v)>0.0001) ? vv : ymax;}	  return [Math.round(xmin)-1,Math.round(xmax)+1,Math.round(ymin)-1,Math.round(ymax)+1];}	let xyminmax=findMinMax(x_liste,y_liste);	JXG.Options.point.size =60/$val59;	var brd = JXG.JSXGraph.initBoard('jsxbox', {axis:true, boundingbox: [xyminmax[0],xyminmax[3],xyminmax[1],xyminmax[2]],	shownavigation:false,showCopyright : false,	showZoom : true,keepaspectratio : false,	  zoom: {factorX: 1.25,factorY: 1.25,wheel: true,needShift: false,eps: 0.001},	  pan: {needShift: false,enabled: true,needTwoFingers: true,pinchHorizontal: true, pinchVertical: true,pinchSensitivity: 7}});	brd.suspendUpdate();	for(var jj = 0; jj < xlen; jj=jj+1) {	brd.create('point', [x_liste[jj],y_liste[jj]],{color:color_liste[jj]});	}	brd.unsuspendUpdate();}	execidtest2.addEventListener('click',graph);	</script>	
$()
!exit

:hint
$()
$()
!exit

:help
$()
$()
!exit

:solution
$()
$()
!exit

:latex
$()
$()
!exit

:postdef
val35=$val26
val37=$val34
val36=$val27
tmp0=!replace t by x in $val30

val67=$(tmp0)
tmp0=!replace \^ by ** in $val67

val21=$(tmp0)
val68=\#Initialisation des variables#	x=$val35	a=$val37	xm=$val36	\#Définition d'une fonction#	def f(x):	  return $val21	\#Initialisation des listes#	x_list=[]	y_list=[]	\#Boucle afin de construire les listes#	while x<=xm:	  y=f(x)	  x_list.append(x)	  y_list.append(y)	  x=x+a	\#Affichage les 3 derniers termes des listes#	print(x_list[-3\:])	print(y_list[-3\:])
!readproc slib/runcode [python,[$val68]],id3,init,Jouer le code ! 
tmp0=$slib_out
val69=$(tmp0)
tmp=!trim $m_reply1
val70=!translate internal $	\
$ to ;; in $tmp
tmp0=!declosing $(val70[1;2])

tmp=!trim $(tmp0)
val71=!translate internal $	\
$ to ;; in $tmp
val72=$[$(val52[$val59])]

!exit

:feedback
!ifval $(val71[$val59])== 
 <div class='oef_feedbacks'><div class="enonce">Votre réponse est évaluée	  comme étant fausse car c'est effectivement le cas. Néanmoins la note reste excellente car	  les notions abordées dépassent le référentiel de seconde. Les conditions sur	  la boucle While sont à durcir. En effet la construction des nombres sous Python ne permet	  pas d'évaluer correctement l'inégalité <code>While x&#60;=xm :</code>. Si on exécute le code	  ci-dessous qui renvoit les 3 dernières valeurs du tableau alors on constate que la prochaine	  itération n'est pas possible. En effet la valeur de 
!insmath \text{x + a}
$() dépasse 
!insmath \text{xm}
$()	  donc l'algorithme s'arrête. Afin de remédier à cette problématique, on peut rajouter	  une condition supplémentaire	  <code>While x&#60;xm or abs(x-xm)&#60;=0.0001:</code>.	  <div>	  $val69	  </div>	  <div class="solution"> Cette condition d'arrêt de l'algorithme qui est mal écrite n'a pas	  d'importance dans l'exemple abordé. Néanmoins il faut avoir conscience que des programmes	  mal codés peuvent avoir des conséquences désastreuses. Deux exemples qui matérialisent	  ces erreurs de programmation extraits du rapport de recherche "Arithmétique flottante" sont :	  <ul><li> L'importance du traitement des exceptions est parfaitement illustré par le crash du vol	  501 d'Ariane en 1996, qui a coûté quelques 500 millions de dollars.</li>	  <li>L'importance de l'arrondi est illustré par l'échec du missile Patriot en 1991. Pendant	  la guerre du Golfe, un anti-missile Patriot tiré de Dahran (Arabie Saoudite) ayant manqué	  l'interception d'un missile irakien Scud, ce dernier a tué 28 soldats et blessé quelques 100	  autres personnes.</li></ul>	  </div>	  </div>	
$()</div>
!endif

!exit

:test

!exit

:stat
vsavelist=1,12,13,14,19,20,21,22,26,27,30,31,34,35,36,37,38,47,48,49,52,53,59,60,61,64,65,66
embedcnt=2
