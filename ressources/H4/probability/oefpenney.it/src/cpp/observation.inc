#if defined COMMENTS
"observation" is not localised, only English
#endif
#if defined TARGET_partita || defined TARGET_giocatore || defined TARGET_computer
\observation{These games can be used to introduce the problem. The assigned score is symbolic.}
#endif
