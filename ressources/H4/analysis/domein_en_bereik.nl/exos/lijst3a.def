# kwadratische functie 
# domein: 1 goede en daarna 3 foute oplossingen domein 
# bereik: 1 goede en daarna 3 foute oplossingen bereik ?
# elke functie twee domein/bereik schrijfwijzen: x>1 en [1;-->> alles in TeX
# deze file [a;b> schijfwijze
:$b*x^2 
\mathbb{R}
\mathbb{Z}
[$b;\rightarrow ] 
[$b;\leftarrow \rangle 
[0;\rightarrow \rangle 
\langle 0;\rightarrow ] 
\langle \rightarrow ; 0 \rangle 
[0;\leftarrow \rangle

:-$b*x^2 
\mathbb{R}
\mathbb{Z}
[ 0 ;\rightarrow ] 
[ 0 ;\leftarrow \rangle 
\langle  \leftarrow ; 0 ] 
[ \leftarrow ; 0 \rangle 
\langle  \leftarrow ; 0 \rangle 
\langle  0 ; \leftarrow \rangle

:$a + $b*x^2
\mathbb{R}
\mathbb{Z}
[ \leftarrow ;\rightarrow ] 
[ $b ; \leftarrow \rangle 
[ $a ; \rightarrow \rangle
\langle  $b ; \rightarrow \rangle
[ 0 ; \rightarrow \rangle
[ -$a ; \rightarrow \rangle

:$a - $b*x^2 
\mathbb{R}
\mathbb{Z}
\langle  \rightarrow ; \leftarrow  \rangle 
[ -$b ; \leftarrow \rangle 
\langle  \leftarrow ; $a ]
\langle  \leftarrow ; -$a ]
[ $a ; \rightarrow \rangle
[ -$a ; \rightarrow \rangle

:$b*x^2 - $a
\mathbb{R}
\mathbb{Z}
\langle  \rightarrow ; \leftarrow  \rangle 
[ $b ; \leftarrow \rangle 
[ -$a ; \rightarrow \rangle
[ $a ; \rightarrow \rangle
\langle  \leftarrow ; -$a ]
\langle  \leftarrow ; $a ]

:-$b*x^2 - $a
\mathbb{R}
\mathbb{Z}
\langle  \rightarrow ; \leftarrow  \rangle 
[ $b ; \leftarrow \rangle 
\langle  \leftarrow ; -$a ]
\langle  \leftarrow ; $a ]
[ $a ; \rightarrow \rangle
[ -$b ; \rightarrow \rangle

:(x - $a)*(x + $a) 
\mathbb{R}
\mathbb{Z}
[ 0 ;\rightarrow \rangle
\langle  -$a ;\rightarrow \rangle 
[ $[-1*($a*$a)] ; \rightarrow \rangle
[ 0 ; \rightarrow \rangle
[ $a ; \rightarrow \rangle
[ $[($a*$a)] ; \rightarrow \rangle


