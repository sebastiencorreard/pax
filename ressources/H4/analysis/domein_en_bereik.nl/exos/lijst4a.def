# abs functie 
# domein: 1 goede en daarna 3 foute oplossingen domein x 
# bereik: 1 goede en daarna 3 foute oplossingen bereik y
# elke functie twee domein/bereik schrijfwijzen: x>1 en [1;-->> alles in TeX
# deze file [a;b> schijfwijze
:$a*abs(x) @ $a*abs(s)
\mathbb{R}  
\mathbb{Z}  
\langle $a ; \rightarrow \rangle 
[  0 ; \leftarrow \rangle 
[  0 ; \rightarrow \rangle 
\langle $a ; \rightarrow \rangle 
\langle \rightarrow ; 0 \rangle 
[  0 ; \leftarrow \rangle

:-$a*abs(x) @ -$a*abs(x) 
\mathbb{R}  
\mathbb{N}  
\langle $a ; \rightarrow \rangle 
[  0 ; \leftarrow \rangle 
\langle \leftarrow ; 0  ] 
\langle \leftarrow $a  ] 
\langle \rightarrow ; \rightarrow \rangle 
[  0 ; \leftarrow \rangle


:$a*abs(-x) @ $a*abs(-x) 
\mathbb{R}  
[  $a ; \rightarrow \rangle 
\langle 0 ; \rightarrow \rangle 
\langle $a ; \rightarrow \rangle 
[  0 ; \rightarrow \rangle 
\langle 0 ; \rightarrow  ] 
\langle \rightarrow ; 0 \rangle 
[  0 ; \leftarrow \rangle

:-$a*abs($b -x) @ -$a*abs($b -x) 
\mathbb{R}  
[  $a ; \rightarrow \rangle 
\langle $b ; \rightarrow \rangle 
\langle \leftarrow ; -$b \rangle 
\langle \leftarrow ; 0  ] 
\langle \leftarrow $a  ] 
\langle \rightarrow ; \rightarrow \rangle 
[  0 ; \leftarrow \rangle

:$a - $b*abs(x) @ $a - $b*abs(x)
\mathbb{R}  
[  $a ; \rightarrow \rangle 
\langle 0 ; \rightarrow \rangle 
\langle -$b ; \rightarrow \rangle 
\langle \leftarrow ; $a  ]
\langle \leftarrow ; $b \rangle
[ $a  ; \rightarrow \rangle
[ -$b  ; \rightarrow \rangle

:$b*abs(x) - $a  @ $b*abs(x) - $a 
\mathbb{R}  
[  0 ; \leftarrow \rangle 
\langle $a ; \rightarrow \rangle 
[  0 ; \leftarrow \rangle 
[  -$a ; \rightarrow \rangle
[  $a ; \rightarrow \rangle
\langle \leftarrow ; $a  ]
\langle \leftarrow ; $b  ]

:abs(sqrt($a*x)) @ sqrt($a*x)
[  0 ; \rightarrow \rangle 
[  0 ; \leftarrow \rangle 
\langle $a ; \rightarrow \rangle 
\langle 0 ; \leftarrow \rangle 
[  0 ; \rightarrow \rangle 
\langle $a ; \rightarrow \rangle 
\langle \rightarrow ; 0 \rangle 
\langle 0 ; \leftarrow \rangle

:abs($a - x^2) @ abs($a - x^2) 
\mathbb{R}
[  0 ; \leftarrow \rangle 
\langle $a ; \rightarrow \rangle 
\langle 0 ; \leftarrow \rangle 
[  0 ; \rightarrow \rangle
\langle \leftarrow ; -$a  ]
[  $a ; \rightarrow \rangle
[  -$a ; \rightarrow \rangle

