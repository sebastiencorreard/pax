\title{Ensemble de définition d'une fonction quotient (1)}
\author{Marie-Claude, David}
\email{mcld@math.u-psud.fr}
\computeanswer{no}

données
\integer{a=randint(2..10)}
\integer{b=randint(-10..10)}
\integer{c=random(1..3)}
\integer{d=random(-5..5)}

\integer{dd=\d*\c}
\integer{b=\d=\b/\a ? \b+1}
\function{f=simplify((\a*x + \b)/(\c*x+\dd))}

\text{g=texmath(\f)}

questions et réponses de la première étape
\text{liste=strictement inférieurs à, inférieurs ou égaux à, différents de, supérieurs ou égaux à, strictement supérieurs à}
\integer{rep1=3}
\integer{s=-\d}
\real{rep2=\s}

\statement{Soit \(f\) une fonction de la variable réelle \(x\) définie par \(f(x) = \f \).<br/>
La fonction est définie pour tous les \(x\)\embed{reply1} \embed{reply2,5}
et seulement pour ceux-ci.
}


\answer{}{\rep1;\liste}{type=menu}
\answer{}{\rep2}{type=numeric}

\help{On appelle <span class="bold">ensemble de définition de la fonction \(f\)</span> l'ensemble
 des nombres réels possédant une image par \(f\).}
\hint{Le quotient par zéro n'existe pas.}
