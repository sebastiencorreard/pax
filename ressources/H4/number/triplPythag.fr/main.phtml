!header

!! Contenu du module par ici. Ce qui suit est un modele d'organisation de ce
!! fichier.

!if $compute=yes
 !if $error!=$empty
  !read msg.phtml
  !read $prov
  !goto end
 !else
  !read answer.phtml
 !endif
!else
 !if $menu=1
   !let prov=!nospace menu $method .phtml
   !read $prov
 !else
  !read menu.phtml
 !endif
!endif compute=yes?


:end
!tail
