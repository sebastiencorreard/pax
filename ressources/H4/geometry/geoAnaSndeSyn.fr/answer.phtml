!! Fichier answer.phtml
!!! Placez ici le code pour montrer le résultat final (quand status est Done).

!if $mode1 isitemof exo1,exo2 or (mode1=exo3 || $mode1=exo4)
  !if $mode1 isitemof exo1,exo2
    !if $niveau=9
      <h2 class="small">Conclusion</h2>
      les points \(H\) et \(G\) sont confondus ; les triangles
      \(A B C\) et \(D E F\) ont donc le même centre de gravité.
    !endif
  !endif
  !if $mode1=exo3 || $mode1=exo4
    <h2 class="small">Conclusion</h2>
    $answer
  !endif
!endif
  <h2 class="small">Score final</h2>
   Vous avez obtenu un total de $module_score points (sur 20).
  <h2 class="small">Scores obtenus</h2>
  <ol>$affsco</ol>
