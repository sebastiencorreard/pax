n=$counter
!if ? isin $(reply$n)
 maxscore=0
 remark$n=$NOK
 goback=0
 !exit
!endif
A=$empty
!for p=1 to 4
 A$p=!item $p of $(reply$n) 
!next p

reply$n=($A1:$A2) ($A3:$A4)

!readproc $checkdir/coord.proc
!exit

