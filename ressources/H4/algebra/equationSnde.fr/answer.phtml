!!! Placez ici le code pour montrer le résultat final (quand le Status est Done).

<div style="background_color:#FFCC66">
<p>Voici les résultats&nbsp;:
<ul>
!if $mode=exo3
  <li>Valeur interdite&nbsp;:
  la valeur interdite de l'équation est \($repvalint).
  L'équation est définie sur l'ensemble \(D = \RR \backslash \{$repvalint}\).
  </li>
!endif
<li>Nombre de solutions: $(name_nbsol[$[$repnbsol]+1])
</li><li>
!if $isfact=1
  Après factorisation et éventuelle simplification, l'équation équivaut à :
  !insmath $eqsimp = 0
!else
  L'équation n'a aucune solution, car un carré ne peut pas être négatif.
!endif
</li><li>
L'ensemble des solutions est
!if $[$repnbsol]=0
  \(S = \emptyset\).
!endif
!if $[$repnbsol]=1
  \(S = \{ $repsol1 \}\).
!endif
!if $[$repnbsol]=2
   \(S = \{ $repsol1; $repsol2 \}\).
!endif
!if $[$repnbsol]=3
   \(S = \,\RR\).
!endif
</li>
</ul>
!!endif
<p class="bold">
Vous avez obtenu $module_score (sur 10) points pour cet exercice.
</p>
</div>
