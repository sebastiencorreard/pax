!if $cmd=reply and $status!=waiting
    error=double_reply
    !exit
!endif

# to avoid nested reads when using the "about" in the intro page
!set procdir=authors/jm.evers/proc

!if $special=1
    # use this for special purposes
    !exit
!else
    !readproc $procdir/var.proc
!endif
