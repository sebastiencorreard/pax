# Production .env template. Copy to .env and fill all values.
# This file is consumed by docker-compose.prod.yml (variable substitution).

POSTGRES_USER=changeme
POSTGRES_PASSWORD=changeme
POSTGRES_DB=changeme

# REQUIRED. Generate with: openssl rand -hex 32
# Must be ≥32 chars and not start with a known placeholder
# (changeme/dev-only/secret/test/your-). The backend refuses to start
# otherwise.
SECRET_KEY=changeme

CORS_ORIGINS=["https://your-frontend-domain.com"]
NUXT_PUBLIC_API_BASE=https://host.changeme
NUXT_PUBLIC_DEBUG_OEF=false

# Corrigé : écrire la bonne réponse dans la convention décimale de la langue
# de l'exercice (« 4,76 » en français) plutôt qu'avec le point brut du moteur.
# Confort d'affichage — la notation accepte les deux écritures quoi qu'il
# arrive. Mettre 0 pour garder le point partout, comme WIMS.
PAX_LOCALIZE_FEEDBACK=1
